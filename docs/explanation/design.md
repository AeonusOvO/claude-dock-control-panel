# ClaudeDock 设计规范

当前设计版本：**5.0.0-rc.30**（2026-08-26）。旧版设计计划、路线图、缺陷清单与分阶段修复提示词
统一归档在 [`docs/archive/`](../archive/)；它们仅用于历史追溯，不得覆盖本文的当前设计契约。

## 工作区优先导航与启动引导（2026-08-25）

- 顶层信息架构固定为“工作区 / 独立对话 / 接入 / 扩展”，设置留在活动栏底部。“插件”和“MCP
  服务”只在“扩展”页内作为同级分段，不再占用两个顶层入口；接入与扩展使用完整内容画布，工作区与
  独立对话保留侧栏 + 主工作面的生产布局。再次点击当前接入或扩展入口必须回到工作区，保留空间来源。
- 项目开发引擎在工作区侧栏中只显示一行当前值，展开后才出现 Claude Code / Codex 图形卡；打开与
  收起使用同一 `details` 容器及逆向旋转，不长期占用项目列表上方空间。空工作区同时提供“添加项目”
  主路径和“打开入门向导”次路径，不用终端术语要求新用户先理解内部实现。
- 新用户启动引导固定为“选择引擎 → 选择模型 → 自动准备 → 打开项目 → 准备完成”五步。引擎只在
  Claude Code / Codex 中选择，与模型来源解耦；Claude Code 标题右侧显示 micro 级“推荐”标签。
  模型来源固定为 Claude 官方订阅、ChatGPT 官方订阅、国产模型与 API；国产模型选中后才在卡片下方
  展开紧凑选择框，并常驻“当前已选择 DeepSeek / 千问 / GLM …”文字状态。检测结果必须区分
  “已就绪 / 待安装 / 项目后检测 / 自动”，未知状态不得伪装为成功。
- 引导进度由 main 进程版本化持久化；检测到旧版工作区或偏好数据的用户默认为已完成，不强制遮挡。
  新用户可跳过、续接，设置与空工作区都可重新打开；本地进度不得包含密钥、令牌或项目正文。
- 动效遵循“来源可追踪、返回路径对称”的空间规则：引导从触发控件的 `transform-origin` 展开并沿同一
  原点收回，步骤前进向左释放、下一步从右进入，返回方向完全相反；插件与 MCP 分段同样按左右关系
  运动。步骤后退控件统一使用“上一步”；退场层不得参与滚动高度计算，不得在切换中制造瞬时滚动条。
  顶层接入/扩展切换只做短促淡化和容器变化，避免整屏横移造成错误层级。
- 运动原则参考 [Apple Human Interface Guidelines: Motion](https://developer.apple.com/design/human-interface-guidelines/motion)
  的上下文连续性、方向一致、短促与可减少要求，以及
  [Fluent 2 Motion](https://fluent2.microsoft.design/motion) 的自然惯性、顶层淡化、容器变形与可访问性规则；
  引用只提供判断依据，实际数值仍由 ClaudeDock 四主题令牌授权。
- 运动只能使用主题授权的 `--dur-*` 与 `--ease-*`，禁止 `linear` 作为交互缓动；长循环旋转除外。
  进入使用 `--ease-spring` / `--ease-enter`，退出使用 `--ease-exit`，用户不必等待动画结束才能继续。
  `prefers-reduced-motion` 继续由唯一全局块把位移、缩放和循环压到瞬时状态，但保留焦点、禁用和状态文字。
- `.button`、`.dialog-primary`、`.extension-switcher__tab` 等每个按钮族各自只有一套字号、行高、图标
  几何与内边距；页面只能选择语义变体，不得给同一按钮族局部改字号。四主题分别通过
  `--font-ui/display/mono`、排版阶梯、表面、圆角、阴影和运动令牌完整接管组件，不写主题分支业务逻辑。

## 5.0 安全终端与原生对话工作区

- 项目默认进入真实 PowerShell/ConPTY 终端；项目标题旁的 `+` 是唯一新建对话入口，每次点击都立即在
  左栏插入独立任务行。设备并发槽未满时显示“正在新建”并在后台创建独立 session；超过槽位时显示
  “排队中 · 第 N 位”，尾部 `×` 只取消该排队任务，不弹“关闭并归档”。新建和历史恢复共用 FIFO，切换
  页面、项目或活动对话不取消已准入的后台任务。启动/恢复失败且自动回滚失败的红色行同样由 `×` 直接
  移除临时会话，不提供归档选项。继续、选择历史和历史记录点击仍走
  终端主路径。结构化原生 DOM 对话只能通过工具栏的“原生对话”显式进入，恢复记录不得自动打开原生
  界面；进入后，同一按钮显示“返回终端”。按钮始终在当前标签页和同一对话 UUID 间往返：确认无回复、
  排队/等待任务或运行中的 Web 进程时直接切换，否则用应用内模态框要求用户“中断并切换”或取消；
  未发送草稿自动走加密恢复通道，不为安全保存本身再弹第二次确认。消息、工具、计划、权限、提问、附件、恢复卡与交互坞共用
  同一四行网格，各行显式指定位置，隐藏空行不得改变消息或输入框的位置。
- 用户提示词继续靠右使用强调色气泡并保留原始段落；助手回复靠左占用稳定的宽输出列，以当前主题的
  `--surface-terminal`、等宽 `>_` 提示符和强调色左边线复刻 PowerShell 输出层次。这个外壳只是结构化
  对话的视觉语言，不得把回复重新送进 PTY，也不得把每个流式 token 渲染成独立卡片。流式阶段只在同一
  DOM 节点内追加纯文本并显示光标，完成后原位切换安全 Markdown；减少动态效果时光标保持静态。
- 对话主列始终使用 `minmax(0, 1fr)`；当有效视口不宽于 720px（包括浏览器缩放）时项目栏收起为
  活动栏入口，点击后以现有 rail preview 动画临时展开。1024px medium 档隐藏技术性“终端”徽标，200%
  缩放再收起主题选择和工作台文字，只保留可读标题与可聚焦图标操作。
- 历史行采用 `minmax(0, 1fr)` 主列和独立尾槽。时间使用不换行、等宽数字；删除按钮与时间在同一
  尾槽交叉淡入，键盘 focus 与 200% 缩放都不得被滚动条裁掉。项目列表外层没有滚动条时不得永久
  预留 gutter，项目文件夹边框必须与同栏其他组件右边缘齐平；历史内层继续保留稳定滚动条安全区，
  但不得再叠加第二份右内距。历史网格固定从顶部排布，行高由内容自然撑开且不低于 27px；超过可见
  高度后只滚动容器，绝不把全部历史压缩进固定高度。尾槽在窄栏中使用 42–58px 响应式宽度，不能把
  标题主列压到不可读。
- 权限、问题与计划不是瞬间插入：卡片使用主题 `--dur-enter` / `--ease-enter`；全屏计划和终端诊断
  使用 `opening → open → closing → closed` 状态，退出动画结束后才关闭或隐藏 DOM。减少动态效果时
  保留焦点、遮罩和完整状态变化，只移除位移、脉冲与装饰性动画。
- 当前对话摘要参考 Codex 的信息检查器，但保持 ClaudeDock 的主题 token、信息密度和本机边界：
  不可拖动、不持久，点击外部或 `Esc` 退出；无任务只显示图标，有活动时短暂展开计数标签。检查器
  固定使用“标题与总览 → 分组 → 条目 → 辅助信息”四级层次；运行中、等待中、已完成作为紧邻标题的
  标签呈现，分别使用强调、警告与中性语义色，条目操作在独立尾列垂直居中。
- 原生对话的权限、提问、计划与 MCP 请求共用一个先进先出交互坞；界面一次只呈现队首请求，完成后
  以同一进退场动效切换到下一项，不把多张操作卡同时堆在输入区，也不重复渲染模型/档位说明。
- 原生权限选择必须包含受项目开关保护的“完全允许”；默认开启预置只让该选项可见并为 SDK 预置切换
  能力，不得自动激活。`dontAsk` 显示为“仅预批准”而非含糊的“不主动询问”：普通未预批准工具直接
  拒绝，但若本轮用户提示词明确要求选择题、选项或“我来选”，允许一次 `AskUserQuestion` 进入同一交互坞，
  最多承载上游支持的四个问题；模型主动发起或同轮重复发起仍拒绝。
- 文字操作只使用既有 `.button` 基类及 `--compact` / `--primary` / `--danger` 语义变体；标题栏与
  浮层 chrome 只使用 `.icon-button`。同类按钮不得重新声明字号、圆角、边框或主题颜色；“原生对话”/
  “返回终端”与信息、清空复用图标按钮，“Claude 工作台”和“主题”复用同一个 `.toolbar-menu-button` 菜单按钮。
  主题不再嵌套第二层框体：单一按钮打开单一选择卡片，两种菜单按钮的字号、字重、高度、圆角、描边、
  chevron、hover、press 与窄宽折叠行为必须完全一致。
- 左栏不再重复显示“当前对话状态”卡片：运行状态已经在标题栏、对话行和底栏可见，终端失败按
  `(session, generation)` 只弹一次主题化诊断框。开发引擎使用同一响应式图形卡组件，通过官方标记、
  主标题、能力说明、选择勾和左侧强调线建立层级；卡组始终使用单列横向卡，保证 270px 侧栏仍维持
  正常文字行。Claude Code 使用本地官方 Anthropic Clay Spark 和固定 Anthropic 表面；Codex 按当前
  appearance 使用本地官方 OpenAI 黑/白 Blossom 与固定白/黑表面。geometry、fill、surface 和 border
  不随主题重着色，不得用 inline 近似图形或 CSS filter 替代。标记是装饰图片，邻接文字已命名 runtime，
  因此使用空 `alt`。切换使用主题时长和缓动，不能退回“两个方框加文字”。
- 原生输入坞使用共享 DOM、提交状态机、附件生命周期、键盘语义和可访问性接口，但允许主题拥有不同
  外壳。Claude 明亮采用宽松的柔和卡片、圆形上箭头和轻微抬升确认；Telegram 明亮采用接近官方桌面
  客户端的紧凑扁平输入条、纸飞机、指针原点涟漪和短距离飞行动效。主题差异只能由 `data-theme` 与
  现有 token 驱动，不能复制业务逻辑，也不能下载来历或授权不清的图标；纸飞机使用应用内联矢量并
  继承主题色。Telegram 的输入行与元信息行必须保持同一白色表面，只允许用发丝线和文字层级分隔，
  不得在底部再嵌一块灰色状态栏。减少动态效果时取消位移、涟漪和脉冲，但发送完成状态、焦点和禁用
  反馈必须保留。
- 提交动作必须先捕获对话 UUID、提交 UUID、文本和附件集合，再进入异步 IPC；确认只允许清理仍属于
  同一提交的内容，不能用全局活动 UUID 处理迟到结果。SDK 接受输入后立即显示用户消息；单轮失败在
  对话中显示并允许修正重试，流级致命失败则结束 owner，不能留下外观可点但没有消费者的发送按钮。
- 原生输入坞只有一个动作按钮（`#native-send`），发送与中断是它的两副面孔，不再并排摆两个按钮。
  三个属性各司其职：`data-action` 决定这次点击是 `send` 还是 `stop`，`data-sending` 标记发送确认
  动效，`data-stopping` 标记只有真正按下中断后才生长的光环。只有「正在运行/正在中断」且输入框与
  附件队列都为空时才呈现 `stop`；用户一开始打字，下一个意图就变回「发送」，按钮必须立刻退回主题
  发送面孔，不能留成一个会丢弃正在流式回复的陷阱。两副面孔的切换发生在发送动效的 `animationend`，
  绑定用户实际看到的画面，而不是主进程回调的快慢——否则一次快速确认会让按钮在动效中途跳变。
  `stop` 面孔在静止时是透明底上的方形停止标记，不预先画出按下态；`stop` 期间按钮不禁用，只有
  `stopping` 阶段才禁用，避免重复中断。
- 运行中输入的内容不打断回复，而是提升到发送行上方的排队条 `#native-queued`，分两级推进：第一级
  是「本轮结束后自动发送」，快照回到 `idle` 时自动释放；用户按 `Esc` 中断后降为第二级「本轮已中断 ·
  点击『立即发送』继续」，必须由人显式点按。每个对话至多一条排队项，再按一次 Enter 以空行追加到
  同一条，排队条不得增长成第二条对话记录。只有 `idle` 允许释放队列：`requires-action` 正开着权限
  卡片，放行会插队；`failed`/`stopped` 会朝着已死会话反复重发。切换回终端时排队内容先折回输入框，
  由既有的加密草稿通道统一携带，不另开一条保全路径。
- 原生回复的可见身份按前台或 `parent_tool_use_id` 流通道稳定到整条消息：token 增量与最终完整帧必须
  更新同一行。renderer 保存每条消息的渲染签名、复用已有节点，并把连续快照合并到下一绘制帧；滚动、
  文本选择和已完成 Markdown 不得因另一个 token 到达而整体重建。
- 新建与恢复按 session 隔离：项目 `+` 在其他对话准备时仍可继续点击；被操作的会话行、composer、
  网络预检、工作台、原生切换和关闭/重命名入口进入灰色 busy 状态。成功只由真实 CLI 启动结果提交；
  失败必须撤掉占位、关闭精确临时终端并恢复历史行。自动关闭也失败时，临时行固定显示红色“启动失败 /
  恢复失败 · 请关闭”，不能退回绿色运行态。所有 `finally` 都必须解除所属锁。
- 安全终端启动不得把内部 settings、环境清理和生命周期 marker 组成的长命令写进可见输入历史；
  PowerShell 在显示提示符前用一次性环境值捕获它，PTY 绑定完成后只提交固定短触发词。默认 Claude
  会话不附加系统提示词或子代理，不强制 `--no-chrome`，模型由会话专用 settings 决定；显式开启的
  中转站兼容开关仍可按其界面说明附加对应能力。
- 所有新增组件必须经过四主题、720×640 / 820×640 / 900×640 / 1024×640 / 1180×760 / 1280×760、270/320/560px 项目栏、
  125%/150%/200% 缩放及进入/退出中间帧截图。证据写入 `dist/visual-qa/<feature>/manifest.json`，
  生成物不提交 Git；最终仍须在真实 Windows Electron 窗口中逐项点击和截图。

## 设计系统：单一事实源（2026-08-16）

### 三条原则

1. **简洁优先**：一个语义只保留一个原语、一个状态源和一个视觉信号；不要用多层外框、重复说明或
   平行组件补偿层级不足。
2. **按角色命名**：业务样式表达 body、caption、raised surface、danger 或 popup，不表达某个偶然的
   像素值。主题改变人格，组件不复制主题分支。
3. **规范由测试强制**：本文是视觉与交互规范的唯一事实源；`tests/renderer/design-tokens.test.ts` 和视觉烟测
   负责阻止令牌、文件职责、主题对比度、控件外观与布局契约回退。

### 三层令牌模型与文件职责

| 层级                  | 事实来源                                                                       | 职责                                 | 业务样式能否直接使用字面值         |
| --------------------- | ------------------------------------------------------------------------------ | ------------------------------------ | ---------------------------------- |
| Primitive / Reference | `src/shared/ui/terminal-themes.ts`、`styles/01-tokens.css`                     | 主题授权值、默认值、间距与推导公式   | 否；CSS 字面值只在 `01-tokens.css` |
| Semantic / System     | `--surface-*`、`--type-*`、`--accent-*`、`--ok/warn/bad-*`、`--dur-*`、`--r-*` | 表达内容角色、状态、层级、运动与密度 | 是，且业务规则只使用这一层         |
| Component             | `styles/05-primitives.css` 与各 view 顶部的组件私有契约                        | 控件几何、状态、修饰符与局部布局     | 只能由语义令牌推导                 |

`src/renderer/styles.css` 只是 `@import` 清单。样式按所有权拆分如下：

| 文件                | 唯一职责                                                                                  |
| ------------------- | ----------------------------------------------------------------------------------------- |
| `01-tokens.css`     | 无条件 `:root`、默认主题值和全部推导阶梯                                                  |
| `02-reset.css`      | reset、选区、焦点基线、滚动条与仅屏幕阅读器内容                                           |
| `03-typography.css` | 基础元素与标题角色                                                                        |
| `04-motion.css`     | 全部 `@keyframes` 和唯一 `prefers-reduced-motion` 块                                      |
| `05-primitives.css` | button、input、select、checkbox/radio、card、status-chip、popover、tooltip 与 dialog 共性 |
| `06-layout.css`     | titlebar、activity rail、workspace、drawer、footer 骨架                                   |
| `07-responsive.css` | 全部宽高媒体查询与条件 `:root` 覆写                                                       |
| `views/*.css`       | terminal、chat、projects、settings、router、mcp、markdown 的业务外观                      |

入口当前把 layout 与各 view 先载入，再后置 primitives、responsive、motion，以保留公共原语、响应式
覆写和减少动态效果的最终优先级。新增规则必须进入拥有该 selector 的文件；跨视图的 selector-list
整体归 primitive 或 typography，不得拆成多份相同声明。

### 完整令牌目录

下表按命名空间列出 `01-tokens.css` 的公共契约；同一行的 `*` 表示列出的完整后缀集合，而不是允许
业务代码自行发明新名字。值以 `TerminalThemeShell` 和 `01-tokens.css` 为准，本文只固定用途与所有权。

| 类别               | 公共令牌                                                                                                                                                                                                                                                                  | 用途                                      |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| 字体桥             | `--font-ui/display/mono/tabular`、`--type-base/ratio`                                                                                                                                                                                                                     | 主题授权的字体族、基准字号与比例          |
| 字体角色           | `--type-{micro,caption,body,subtitle,title,display}-{size,lh,fw,ls}`                                                                                                                                                                                                      | 六级排版角色四元组                        |
| 行高/字重/字距修饰 | `--lh-{tight,compact,body,note,prose,relaxed,heading}`、`--fw-{regular,medium,semi,strong,heavy}`、`--ls-{tight,snug,body,normal,title,display,wide,caps,eyebrow,shout}`                                                                                                  | label、强调态、等宽数据等有语义的角色修饰 |
| 表面与边线         | `--surface-{canvas,terminal,1,2,3,4,inset}`、`--line/line-subtle/line-hover/line-strong`、`--layer-{hover,active}`                                                                                                                                                        | 页面层级、边界与交互层                    |
| 文字与强调色       | `--text-hi/text/text-lo/text-dim/text-mute`、`--accent-{solid,solid-hover,text,fg,tint,line,ring}`                                                                                                                                                                        | 内容层级与主操作                          |
| 状态与语法色       | `--{ok,warn,bad}-{text,tint,line,solid}`、`--syntax-{neutral,red,green,yellow,blue,magenta,cyan}`                                                                                                                                                                         | 状态、代码与终端语义色                    |
| 间距               | `--s-{0-5,1,1-5,2,2-5,3,3-5,4,5,6,8,10}`                                                                                                                                                                                                                                  | 2–40px 的唯一公共间距阶梯                 |
| 圆角与控件         | `--r-{xs,sm,md,lg,xl,2xl,pill}`、`--r-theme-{sm,md,lg,pill}`、`--r-bubble`、`--control-h-{sm,md}`                                                                                                                                                                         | 通用、主题与组件几何                      |
| 运动               | `--dur-{zero,instant,1,2,3,4,hover,micro,enter,exit,exit-theme,shape,stagger,progress,refresh,blink,busy-loop,status-pulse-fast,status-pulse,status-pulse-slow}`、`--ease-{standard,decel,accel,enter,exit,spring}`、`--press-{sm,lg,theme}`、`--pop-{scale-from,travel}` | 时长、曲线、按压与弹层人格                |
| 阴影与遮罩         | `--sheen`、`--shadow-{overlay,drawer}`、`--mask-{veil,blur}`、`--opacity-disabled`                                                                                                                                                                                        | 浮层、抽屉、遮罩与禁用态                  |
| 布局               | `--titlebar-h/titlebar-drag-safe`、`--activity-rail-w/rail-w/drawer-w`、`--toolbar-h/footer-h/composer-h/native-composer-h`、`--composer-{row-h,max,ring}`、`--terminal-min`、`--summary-{header-h,max-h}`                                                                | 应用骨架和可测量边界                      |

组件私有令牌只在拥有者规则附近定义。目前的私有契约是项目引擎卡的
`--runtime-option-h/icon-size`、历史尾槽的 `--history-tail`、卡片退场的 `--card-rest-opacity`、
上下文计量的 `--context-color/progress` 与下载环的 `--download-progress`。它们不得被其他 view 当作公共令牌。

### 颜色、表面与间距

- 表面从 `--surface-canvas`、`--surface-terminal` 到 `--surface-1..4`、`--surface-inset` 逐层表达；
  明亮主题依靠纸面差和克制阴影，深色主题依靠表面提亮与发丝描边。
- 文字颜色按 `--text-hi → --text → --text-lo → --text-dim → --text-mute` 递减；它们是颜色语义，
  不承担字号职责。
- 主操作使用 `--accent-solid/solid-hover/text/fg/tint/line/ring`；成功、警告、失败只使用
  `--ok-*`、`--warn-*`、`--bad-*`。状态永远同时给文字或图形，不只靠色相。
- 间距阶梯完整覆盖 2 / 4 / 6 / 8 / 10 / 12 / 14 / 16 / 20 / 24 / 32 / 40px，对应
  `--s-0-5` 至 `--s-10`。组内优先 4–8px，组间 12–16px，区间 24–40px。
- 主题相关半透明色使用 `color-mix()` 与语义色混合；代码高亮最终映射到 `--syntax-*`，切主题不重建 DOM。

### 六级字体角色

每个角色都提供 size / line-height / font-weight / letter-spacing 四元组，例如
`--type-body-size/-lh/-fw/-ls`。新组件先选择一组角色默认值；若祖先已经建立相同角色可以继承，label、
强调态和等宽数据可以用上表的语义修饰令牌覆盖某一轴。禁止用 `font` 简写覆盖
`font-feature-settings`，也不得为了单个状态另造字号。

| 角色     | 令牌前缀            | 用途                             |
| -------- | ------------------- | -------------------------------- |
| micro    | `--type-micro-*`    | 徽章、计数、时间戳、eyebrow      |
| caption  | `--type-caption-*`  | 次要说明、副标题、紧凑按钮       |
| body     | `--type-body-*`     | 默认界面文字、列表主行、输入内容 |
| subtitle | `--type-subtitle-*` | 分区标题、卡片组标题             |
| title    | `--type-title-*`    | 面板和对话框标题                 |
| display  | `--type-display-*`  | 空态、设置头图、关键数值         |

主题只授权 `typeBase` 与 `typeRatio`，浏览器用 `round()` 推导整数像素；micro 另有 10px 下限。

| 主题          | UI / Display 字体                                       | Base / Ratio | micro / caption / body / subtitle / title / display | 正文行高 | 标题字距 | 强调字重 |
| ------------- | ------------------------------------------------------- | ------------ | --------------------------------------------------- | -------- | -------- | -------- |
| Claude 明亮   | Hanken Grotesk / Newsreader                             | 15 / 1.25    | 10 / 12 / 15 / 19 / 23 / 29                         | 1.70     | -0.012em | 600      |
| 石墨深色      | Inter / Inter                                           | 13 / 1.18    | 10 / 11 / 13 / 15 / 18 / 21                         | 1.50     | -0.008em | 650      |
| 深海蓝        | Inter / Inter                                           | 14 / 1.20    | 10 / 12 / 14 / 17 / 20 / 24                         | 1.62     | -0.004em | 600      |
| Telegram 明亮 | Segoe UI → Microsoft YaHei UI → Roboto fallback / 同 UI | 13 / 1.15    | 10 / 11 / 13 / 15 / 17 / 20                         | 1.45     | -0.006em | 600      |

等宽内容统一使用 `--font-mono`（Cascadia Mono → Cascadia Code → Consolas）。Anthropic 的商业字体
不随应用分发；Claude 使用可分发的近似字体。所有字体都本地或随系统提供，不发起第三方字体请求。

### 四主题动效与圆角人格

| 主题          | micro / enter / exit | enter / spring / exit 曲线                                              | Popup 起始缩放 / 位移 | 按压  | 发送交互             |
| ------------- | -------------------- | ----------------------------------------------------------------------- | --------------------- | ----- | -------------------- |
| Claude 明亮   | 120 / 240 / 180ms    | `(0.05,0.7,0.1,1)` / `(0.16,1,0.3,1)` / `(0.3,0,0.8,0.15)`              | 0.96 / 4px            | 0.985 | 圆形上箭头确认       |
| 石墨深色      | 95 / 150 / 150ms     | `(0.22,0.61,0.36,1)` / `(0.3,0.8,0.4,1)` / `(0.55,0.06,0.68,0.19)`      | 0.98 / 2px            | 0.985 | 通用强调色按压与涟漪 |
| 深海蓝        | 115 / 175 / 175ms    | `(0.16,0.84,0.24,1)` / `(0.18,0.96,0.28,1.02)` / `(0.4,0.05,0.72,0.15)` | 0.97 / 3px            | 0.985 | 通用强调色按压与涟漪 |
| Telegram 明亮 | 120 / 200 / 150ms    | `(0.075,0.82,0.165,1)` / `(0.23,1,0.32,1)` / `(0.4,0,1,1)`              | 0.94 / 6px            | 0.975 | 纸飞机飞出与触点涟漪 |

| 主题          | `--r-theme-sm` | `--r-theme-md` | `--r-theme-lg` | `--r-bubble` | 间距阶梯（主题共享）                                    |
| ------------- | -------------- | -------------- | -------------- | ------------ | ------------------------------------------------------- |
| Claude 明亮   | 8px            | 12px           | 16px           | 18px         | 2 / 4 / 6 / 8 / 10 / 12 / 14 / 16 / 20 / 24 / 32 / 40px |
| 石墨深色      | 6px            | 8px            | 10px           | 14px         | 同上                                                    |
| 深海蓝        | 6px            | 8px            | 10px           | 14px         | 同上                                                    |
| Telegram 明亮 | 4px            | 6px            | 10px           | 12px         | 同上                                                    |

组件使用 `--dur-1..4`、`--dur-exit`、`--ease-standard/decel/accel`、`--press-sm/lg` 与通用圆角，
不直接选择某一主题。所有 keyframes 只放在 `04-motion.css`；其中唯一的减少动态效果块取消位移、循环
和涟漪，但保留焦点、状态文字、遮罩和同步状态翻转。交互页面只做 opacity 进场；hover 不上浮。
表内授权值以 `src/shared/ui/terminal-themes.ts` 为实现来源；Claude 字体选择参考官方 MCP Apps 的排版与
透明主题原则，Telegram 的系统 UI 字体、纸飞机和涟漪取自其桌面端实现。外部依据链接集中在
`technical.md`，不得凭印象改写本表。

### 组件原语

| 原语             | 类名与状态                                                         | 必须做                                                          | 禁止做                                 |
| ---------------- | ------------------------------------------------------------------ | --------------------------------------------------------------- | -------------------------------------- |
| 文字按钮         | `.button` + `--compact/--primary/--secondary/--danger/--quiet`     | 类名自带完整外观与 disabled/focus/press                         | 在业务容器重写字体、圆角或主色         |
| 图标按钮         | `.icon-button`                                                     | 提供可读 `aria-label` 或 `.sr-only`；使用 `--control-h-sm`      | 用空白文字按钮冒充图标按钮             |
| 卡片             | `.card`                                                            | 使用 surface、line、radius；状态另用语义 tone                   | 叠加外环、渐变和无语义彩色             |
| 状态项           | `.status-chip`；按钮态或只读态                                     | 同高、同字体角色；状态同时有文字                                | 为底栏每个项目写一套高度和 hover       |
| 弹层             | `.popover`、`[hidden]` / dialog `[open]`、`data-placement="above"` | 状态同步翻转；关闭态 `pointer-events:none`；让 CSS 处理离散退场 | JS 计时器、`data-closing` 平行状态机   |
| 增强选择器       | `.select/.select-native/.select__trigger/.select__listbox`         | 原生 select 保持取值、校验、焦点与事件事实来源                  | 回退到系统原生外观或复制业务状态       |
| 文本输入         | 标准 input 类型；`.field` 只管标签与布局                           | surface、line、focus ring、disabled 全局一致                    | 依赖父容器碰巧上色                     |
| Checkbox / Radio | 原生 input + token CSS                                             | 保留原生 checked/disabled/focus 语义                            | 为视觉目标增加 JS 状态                 |
| Dialog           | 原生 `<dialog class="popover">` + 语义 header/body/footer          | `showModal()`、Esc、焦点恢复、`dialog::backdrop` 单一规范遮罩   | 用 `alert/confirm` 或普通 div 模拟模态 |
| Tooltip          | `.tooltip` + `.tooltip__content[role="tooltip"]`                   | 触发器可聚焦并用 `aria-describedby` 指向同一文字                | 只支持 hover、复制说明或承载交互控件   |

Tooltip 同时响应 hover 与 `:focus-within`，触摸设备仍应依赖触发器的可访问名称或常驻辅助文字；其内容只做
短说明，不承载按钮、链接或其他交互控件。

Popover 依赖 `@starting-style` 与 `allow-discrete`；`hidden` 和 dialog `open` 仍是同步可访问性契约。
普通弹层挂在 `body` 逃离 clipping；dialog 内的 select listbox 必须重挂到已打开 dialog 的 top-layer 子树，
因为仅提高 `z-index` 不能越过 top layer 的 inert 边界。

模态遮罩只有一条规范规则：`dialog::backdrop` 取 `var(--mask-veil)` 与 `var(--mask-blur)`。这两个令牌
按主题定义、由 `shell/theme.ts` 在换主题时行内改写，所以遮罩天然契合四种主题人格且能随时变色。
禁止为单个 dialog 再写 `::backdrop` 颜色——硬编码的 `rgb(0 0 0 / 58%)` 在浅色主题下是一圈黑边，
而 `color-mix(… var(--text-hi) …)` 在深色主题里会变成一层白雾。唯一允许的按 dialog 覆写是
`opacity`/`transition`（例如原生方案弹窗不要遮罩）。`tests/renderer/design-tokens.test.ts` 强制此约束。

### 布局骨架与分组

- **Titlebar**：品牌/当前项目在左，窗口状态与原生窗口按钮在右；拖拽区和 no-drag 控件边界明确。
- **Activity rail**：只负责一级业务导航；当前项、预览态和折叠态分别表达，不能把业务表单塞进 rail。
- **Workspace**：活动栏、项目栏、分隔条、主区四列；所有可收缩主列使用 `minmax(0, 1fr)`，拖拽期间
  关闭宽度过渡，释放后再 fit xterm。
- **Terminal toolbar**：`toolbar-group--labeled` 先放“工作台/主题”，`toolbar-group--icons` 后放图标操作；
  组内 gap 4px、组间 gap 12px，中间一条 `--line-subtle` 分隔，控件统一 `--control-h-sm`。
- **Footer**：primary（连接/资源）、secondary（模型/速度/模式/思考）、status 三组；所有可操作项复用
  `.status-chip`。宽屏原位行内显示 secondary；中窄宽度由 CSS 把同一 DOM 收进“会话设置”弹层，
  不得复制第二份控件或状态源。
- **Drawer / Dialog**：标题、可滚动正文、固定操作区三段；抽屉不得覆盖 composer，dialog 内容不得逃出
  top layer 或被背景 inert 吞掉。

### 响应式断点

CSS 媒体条件不能读取自定义属性，因此常量表写在 `07-responsive.css` 文件头，所有媒体查询集中于该文件。

| 断点    | 条件                | 主要职责                                                     |
| ------- | ------------------- | ------------------------------------------------------------ |
| compact | `max-width: 720px`  | 项目栏收起、设置导航横排、双栏对话框改单栏、文字菜单收成图标 |
| medium  | `max-width: 1024px` | 抽屉夹紧、底栏 secondary 收纳、工具栏与插件操作压缩          |
| wide    | `min-width: 1280px` | 允许多栏和更宽信息布局；不能作为核心操作可见性的前提         |

### 如何新建一个 UI

1. **先定语义与所有权**：确认它是已有 primitive、layout 还是某个 view；同名 selector 已存在就扩展原文件，
   不在另一个文件追加第二份。
2. **选文字角色**：正文用 body，辅助说明用 caption，徽章用 micro，分区/面板/空态依次用
   subtitle/title/display；新独立组件写入该角色需要的四轴，已有组件可继承相同角色，并只用语义修饰
   令牌表达 label、强调态或等宽数据差异。
3. **选层级**：页面底、面板、卡片、凹陷区分别取 semantic surface；交互只用 hover/active layer，
   状态只用 ok/warn/bad。
4. **新面板**：

   ```html
   <section class="card" aria-labelledby="example-title">
     <h2 id="example-title">分区标题</h2>
     <p>一句必要说明。</p>
     <button class="button button--primary" type="button">继续</button>
   </section>
   ```

5. **新弹窗**：

   ```html
   <button aria-controls="example-dialog">打开</button>
   <dialog id="example-dialog" class="popover" aria-labelledby="example-dialog-title">
     <header><h2 id="example-dialog-title">确认操作</h2></header>
     <section>必要说明与内容。</section>
     <footer><button class="button button--primary" type="button">确认</button></footer>
   </dialog>
   ```

   通过 `showModal()/close()` 切换；非模态元素使用 `.popover[hidden]`，触发器维护
   `aria-expanded/aria-controls`，向上展开再加 `data-placement="above"`，不要新增关闭计时器。

6. **新表单**：

   ```html
   <label class="field" for="example-name">
     <span>名称</span>
     <input id="example-name" type="text" aria-describedby="example-name-help" />
   </label>
   <p id="example-name-help">用途或错误只保留一个事实来源。</p>
   ```

   Select 保留原生元素并交给 `enhanceSelect()`；错误文字和 `aria-describedby` 必须指向同一来源。

7. **新列表项**：

   ```html
   <article class="card example-row">
     <div><strong>主标题</strong><small>辅助信息</small></div>
     <button class="icon-button" type="button" aria-label="删除主标题">…</button>
   </article>
   ```

   主列使用 `minmax(0, 1fr)`，尾操作使用独立槽；可增长列表自身滚动，hover 不触发布局位移。

8. **新徽章/状态**：

   ```html
   <span class="status-chip" data-tone="warning"><span aria-hidden="true">●</span>等待确认</span>
   ```

   优先 `.status-chip`，同时给文字与图形信号，不把颜色当唯一含义。

9. **新工具栏/底栏项**：放入已有 group，复用统一高度与 gap；窄屏移动原控件，不复制状态或监听器。
10. **验证**：依次运行 `npm test`、`npm run test:control-theme`、`npm run test:select-theme`、
    `npm run test:select`、`npm run test:dialog-select`、`npm run test:layout`、`npm run test:visual`；发布阶段再跑
    `npm run dist`。四主题、键盘、200% 缩放与减少动态效果都必须人工核对。

### 禁止事项

- 不在 `01-tokens.css` 外写颜色、px 字号、数字字重、毫秒或曲线字面值；不写 px `line-height`
  （`tests/renderer/design-tokens.test.ts`）。
- 不新增按尺寸命名的字号别名；业务样式只能引用六个语义文字角色（`tests/renderer/design-tokens.test.ts`）。
- 不在 `04-motion.css` 外写 keyframes 或 reduced-motion；不在 `07-responsive.css` 外写 viewport media
  （`tests/renderer/design-tokens.test.ts`）。
- 不引用未定义变量，不复制 selector 到另一个文件，不用更高 specificity 掩盖所有权错误
  （`tests/renderer/design-tokens.test.ts`）。
- 不写逐主题业务选择器来补字体、圆角或动效；主题差异必须由 `TerminalThemeShell` 与令牌桥驱动
  （`tests/renderer/design-tokens.test.ts` + `npm run test:control-theme`）。
- 不让弹层硬切、留下可点击的退场残影，或在按钮内部挂 fixed popup
  （`npm run test:select` + `npm run test:dialog-select` + `npm run test:visual`）。
- 不使用原生控件外观、原生 JavaScript 对话框、只有颜色的状态、hover 位移或装饰性渐变
  （`npm run test:control-theme` + `npm run test:layout` + 人工四主题复核）。
- 不为新 UI 复制状态、业务逻辑、IPC 或键盘语义；视觉层只能消费已有事实来源
  （`npm test` + 对应交互烟测）。

## 设计目标

界面支持四套完整人格：Claude 温和编辑感、Telegram 紧凑通讯感，以及石墨/深海蓝的中性工程
气质。差异覆盖字体、行高、圆角、消息结构和动效，不能只换色。终端或独立对话始终是视觉
主体，左侧控制区呈现配置、项目与历史；所选开发引擎的工作台从终端右侧按需滑入。避免拟物化、
过度渐变和大面积高饱和色。

## 视觉语言

- 背景：由所选主题决定的分层表面。深色终端是最暗层；明亮终端使用接近白色的阅读面，
  外围画布略深，卡片再抬升，形成真正的亮色层级。
- 终端不允许出现一圈黑边：`@xterm/xterm/css/xterm.css` 把 `.xterm .xterm-viewport` 写死成
  不透明黑，必须覆写为 `var(--surface-terminal)`，同时让每套主题的调色板底色与它的终端
  表面一致，否则画布内外会差出一个色阶，看上去就是一个黑框。
- 活动终端内的 xterm 隐藏输入区获得焦点时，外层用 `--accent-line` 细描边和
  `--accent-tint` 微光反馈“原始 PowerShell 已接管输入”。效果只响应 `:focus-within`，不改变
  布局、不持续呼吸，也不能压过错误/警告状态。
- 主色和状态色都由主题提供，但“正常 / 警告 / 错误”的色相与含义保持不变。
- 字体：正文使用 `--font-ui`，品牌标题使用 `--font-display`，终端与路径、命令等技术标识
  使用 `--font-mono`。Claude / Telegram 的正文、标题和行高必须在并排截图中明显不同。
- 圆角：卡片、嵌套内容与按钮分别消费 `--r-theme-lg`、`--r-theme-md`、`--r-theme-sm` 或对应公共
  语义令牌；具体像素由四主题人格表授权。状态胶囊使用 `--r-pill`，业务视图不得写死固定半径。
- 阴影：只用于窗口内浮层与拖放激活态，避免装饰性重阴影。
- 图标：使用一致的 1.8px 线性 SVG，禁止字形充当图标；应用图标以终端提示符和状态灯为核心。

## 布局与状态

- 最小窗口：820 × 640；建议窗口：1180 × 760。
- 顶部 48px (`--titlebar-h`) 为可拖拽标题栏，交互控件必须标记为不可拖拽。
- 标题栏刷新图标紧邻 Windows 原生窗口按钮左侧，是 ClaudeDock、Claude Code、Router、插件与
  MCP 的统一更新入口；检查中旋转并设置 `aria-busy`，发现更新后出现琥珀状态点。
  每个领域页面还必须保留自己的“检查更新”按钮。新增可更新领域时，必须同时接入全局聚合和
  领域入口；单个来源失败只显示局部/汇总错误，其他结果继续呈现。
  用户主动点击后必须打开主题化结果对话框，即使没有更新也显示明确空状态；结果按 ClaudeDock、
  Claude Code、Router 与插件分行，支持逐项执行和“全部更新”。某一来源不可用时只禁用对应行，
  不隐藏其他有效结果；开始下载、安装或卸载后可直接转到下载中心查看进度。
- 左侧控制栏默认 320px (`--rail-w`)，通过 5px 分隔条在 270–560px 之间调整并持久化；
  终端占据剩余空间，键盘也可聚焦分隔条后用方向键调整。拖拽必须在 `pointerup`、
  `pointercancel`、`lostpointercapture`、窗口失焦或进入后台时统一结束并释放捕获；从托盘恢复
  时再次清理残留状态，不能让一个中断的拖拽继续拦截整个窗口的点击。再次点击当前活动栏按钮
  时，控制栏和分隔条收起；工作区、独立对话、接入、扩展与底部设置入口始终可见。收起内容必须设为
  `inert` / `aria-hidden`，终端随布局变化重新 `fit()`，点击任一业务入口即可展开对应页面。
  控制栏收起时，工作区与独立对话入口的悬停或键盘聚焦会在活动栏右侧打开临时预览；接入与扩展
  直接进入完整画布，不使用侧栏预览。预览不切换聊天/终端主视图、
  不触发 xterm `fit()`，点击才固定页面。指针离开入口和面板约 120ms 后收起，`Esc`、窗口失焦、
  窗口缩放或打开其他对话框会立即清理预览；进入、退出使用主题动效，减少动态模式下取消位移。
- 开发工作台默认 468px (`--drawer-w`) 右侧覆盖式抽屉，可通过左缘分隔条在 360–760px
  之间调整并持久化；推荐宽度优先保证完整抽屉，不再为了保留终端最小宽度把信息压成窄条，
  只在容器不足时收缩到 `calc(100% - 24px)`。Claude 顶部固定“会话 / 命令 / 快捷键”
  三页签；Codex 使用单页准备流程，两者内容区都独立滚动。
  抽屉打开时以半透明遮罩压低终端，不销毁或重排 xterm.js 实例。
- 在 1024px medium 档及以下，控制栏最小 240px，终端工具栏改为两列，工作台允许
  覆盖除 80px 外的终端区域；扩展页工具栏改为纵向，三个插件页签始终等分可用宽度，按钮文字
  自然换行。插件卡的长名称、仓库地址、操作按钮和市场添加表单必须同时设置可收缩边界并
  自然换行，不得以内容最小宽度撑破侧栏。插件页不得出现横向滚动。底栏的连接与资源始终可见，
  模型、速度、权限模式和思考程度在宽度不足时进入同一 DOM 的“会话设置”浮层；状态摘要保持独立，
  可在受限宽度下由 CSS 收起。
  控件只能自然换行、收起次要文本或滚动，不允许通过负边距、绝对定位挤压造成重叠。
- 终端工具栏只有「标题 + 操作」两列，没有装饰性窗口灯之类的假 chrome。真实终端已经在
  应用的窗口里，再画一组红黄绿点只是在模仿另一个软件的外观。
- 在高度 640–700px 的窗口中收紧卡片和控制栏纵向间距，全部长内容继续由已有滚动容器承接。
- 左侧项目列表使用较大的文件夹标题，并按文件夹、运行中对话、历史对话形成可折叠层级；
  列表溢出时内部滚动。折叠开关只作用于历史对话区：运行中对话始终可见，因此正在使用的
  项目也能收起——收起后只保留文件夹标题和进行中的对话行，不允许出现“点击收起没有任何
  反应”的死开关。
- 从“添加项目”到快捷操作和托盘说明作为一个整体，紧随项目列表的自然高度向下移动，抵达窗口
  底部后保持可见，由项目列表内部滚动；列表减少后整体重新上移。不得用固定 340px 上限代替剩余空间，
  也不得压扁对话行。列表高度变化使用非线性减速，连续状态刷新不重置相同目标的动画进度。
- 点击运行会话的“关闭并归档”后，目标行立即显示处理中状态并禁用选择、重命名和关闭按钮；确认后
  目标终端显示关闭遮罩。全项目关闭同时禁用该项目的新建与恢复入口；单会话关闭不阻塞其他会话。
  确认取消或关闭失败恢复按钮，成功后才从运行列表移走，不允许出现按钮无反馈、用户可以反复点击的空档。
- 运行中对话创建时允许先显示“对话 N”临时名称；Claude Code 的自动标题可用后，原位置以
  “擦除旧名 → 打字机逐字输入新名”的动画替换为主题标题，动画期间显示闪烁光标，并同步
  终端工具栏与工作台范围，不新增提示条、不改变列表层级。`prefers-reduced-motion` 下直接
  替换文本。用户手动重命名立即生效、不播放动画，且旧的状态行刷新不得把名称改回去。
- 项目添加只保留一个虚线加号区域，同时支持点击选择和拖放，不再额外放置重复按钮。
- 目录选择器取消时不显示错误；原生对话框启动失败与选择完成后的项目打开失败必须使用不同
  文案，并保留系统返回的可读错误原因，不能统一吞成模糊提示。
- 会话状态分为：启动中、运行中、已停止、错误；文本和颜色同时表达，不只依赖颜色。
- 没有任何对话时（冷启动，或关掉最后一个对话之后）终端区显示引导态，而不是伪造一个会话。
  引导文案随「是否已有记住的项目」二选一：有就引导去点最近项目，没有就引导去添加文件夹。
  标题栏、状态胶囊、底栏和工作台范围同步显示“未打开项目”，输入框禁用。
  不允许为了避免空状态而在用户目录里起一个 PowerShell——那会造出一个用户从没打开过、
  以 Windows 用户名命名的假项目。
- 项目切换只替换当前可见终端，后台终端继续运行并保留各自滚动缓冲。
- “对话”切换右侧主工作区为 `.chat-shell`，不销毁后台 xterm；离开“对话”后终端在同一
  网格位置恢复。再次点击已选“对话”只收起配置侧栏，聊天主界面仍保留，避免误切回项目终端。
- “接入”表单和独立对话的模型设置默认使用极简模式：自定义只填网址、密钥，内置国内服务只填密钥。
  不要求用户选择协议、认证方式或填写可在线发现的模型。测试费用说明只保留“可能会消耗少量 token”；
  协议原理、阶段细节、密钥存储原理与排障解释进入高级设置或文档，不堆在默认表单。
- 表单内“高级设置”展开手动选项并变为“极简设置”；再次点击收起，不清空草稿。在高级设置修改官方
  网址后，返回极简模式保留为自定义接口。连接中禁用提交及设置切换；成功后保存，失败保留草稿。
  独立对话再次打开设置时重新读取 main 配置并回到极简模式。
- 窗口关闭默认隐藏到托盘；托盘图标按会话与忙碌租约的聚合状态显示，并提供项目切换菜单。
  首次关闭说明与“关闭窗口时的行为”设置必须如实区分隐藏和退出；选择直接退出后仍不能绕过
  忙碌确认。
- 从托盘菜单或其他显式入口真正退出应用之前，始终先用全屏灰色 backdrop 弹出居中的应用内确认框，不使用系统消息框；存在
  正在启动/运行的终端、回复流、发送、附件读取、下载、安装、卸载或配置写入时逐项列出。主按钮
  新建或恢复事务时还要逐项显示当前收尾阶段及强制退出后果。安全主按钮按状态显示“转到后台，继续收尾”
  或“最小化到托盘，继续运行”，“不退出，返回软件”立即恢复原界面，危险操作“仍要退出”退居次级；可续传下载与中断后可能留下
  不完整状态的 blocking 操作分组说明；没有活动项时使用简短的一般退出说明。系统关机/注销不问。
  任何情况下都不能出现「点了退出但应用关不掉」：渲染进程必须回答每一次询问，包括否定回答。
- 全局滚动条使用统一的透明轨道、低对比度圆角滑块与青色悬停反馈；xterm、控制栏、抽屉和
  项目列表遵循同一规则。
- 移动动画、滚轮滚动及对应滑块移动均采用非线性曲线。滚轮与终端使用约 180ms 三次减速，连续输入
  追加到逻辑终点，反向输入立即转向；滚动到边缘后把余量交给外层。滑块拖动、窗口拖动和输出位置
  同步必须直接跟手，不能人为叠加缓动延迟。开启“减少动态效果”时，CSS、JS 和终端滚动均立即完成。
- **悬停才出现的行内操作按钮不能贴着竖向滚动条。** 项目历史与对话历史的删除按钮与它交叉淡入的
  时间戳右边缘对齐（`right: 7px`），列表容器另留 `--s-2` 级的右内距，保证按钮与滚动条之间始终有
  可见空隙。滚动条只在列表溢出时出现，一贴上去就等于“按钮的位置取决于内容多少”，点击目标会被
  滑块吃掉一角。
- NSIS 安装器在目录选择后显示”安装选项”页，桌面快捷方式复选框默认勾选。
- NSIS 安装器只提供简体中文界面，与应用内中文文案保持一致。

## 终端输入框（对话式输入）

终端区自上而下是 `工具栏 / 输出区 / 输入框 / 底栏`
（`.terminal-shell` 的 `grid-template-rows: var(--toolbar-h) minmax(0, 1fr) auto var(--footer-h)`）。
输入提示词的主入口是输入框，不是输出区。

- 输入框是真正的 `<textarea>`。这是选它的全部理由：`Ctrl+A` 全选、`Shift+←/→` 逐字选择、
  鼠标拖选、`Ctrl+Z` 撤销、中文 IME 组合全部是浏览器原生行为，不由按键处理器在终端画布里
  模拟编辑器。**不要为这些行为写代码**——写了就会与原生行为冲突并重新引入终端的弊端。
- 只有三件事需要实现：`Enter` 发送 / `Shift+Enter` 换行、`↑/↓` 翻本地历史、随内容自动增高
  （上限 `--composer-max`，168px）。历史只在光标位于首/末且无选区时才翻，否则方向键属于
  文本编辑；`event.isComposing` 或 `keyCode === 229` 期间一律不拦截，避免打断 IME 候选。
- 实测高度写回 `--composer-h`，`.workbench-scrim` 与 `.claude-workbench` 的 `bottom` 由
  `calc(var(--footer-h) + var(--composer-h))` 得出，抽屉永远不会盖住输入框。
  `npm run test:layout` 断言在 820×640 下输入框不被底栏或抽屉覆盖。
- 多行提示词必须作为**一条**命令进入 PowerShell：行间用 `\x0a`（PSReadLine 的
  `Ctrl+j`/AddLine 绑定所插入的字符）连接，末尾的回车单独写一次。构造逻辑在
  `src/shared/conversation/composer-input.ts`，与 `terminal-session.ts` 的键位绑定成对存在，改一处必须改另一处。
- 会话未在运行时输入框禁用并换 placeholder；启动会话、切项目等操作之后记录“待聚焦”意图，
  只有同一个活动会话真正进入 `running` 后才把焦点给输入框，不使用固定毫秒延时。
  例外：`resume` 模式打开的是 Claude 自己的方向键选择器，需要原始按键，焦点留在输出区。
- 所有删除、清空、卸载和重启确认都使用带标题、说明、取消与确认按钮的应用内 `<dialog>`；
  禁止使用 `window.alert` / `window.confirm`。关闭对话框后优先恢复打开前的控件焦点；窗口
  重新获得焦点或从后台变为可见时，必须同步最新工作区状态后再执行输入框待聚焦逻辑，避免
  Windows 原生 JavaScript 对话框的焦点恢复缺陷同时影响 xterm 隐藏 IME textarea 和主输入框。
- 输出区仍然可以点进去直接打字（Claude Code 的 TUI 需要原始按键）。输出区内 `Ctrl+A`
  的含义是「全选输出」而不是 PSReadLine 的「移到行首」。
- 提示文字是输入框自己的 placeholder，开始输入即消失，不再占一行独立小字；「快捷键」页
  保持同一说法，且不得再出现与输入框冲突的旧条目（例如把 `Ctrl+A` 说成「移动到行首」）。
  屏幕阅读器另有一个 `sr-only` 的 `aria-describedby` 文本，因为 placeholder 在有值时会消失。
- 终端与独立对话的输入框是 `minmax(0, 1fr) auto` 两列：按钮 `align-self: stretch`，与输入框等高，
  不是浮在角落里的小方块。输入框最小高度 `--composer-row-h`（52px），仍按内容增高到 `--composer-max`。
  这套几何是**这两个输入框共用的契约**，不允许各写一套。原生输入坞是第三种排布，因为它多一个附件
  入口：`.native-composer__row` 用 `auto minmax(0, 1fr) auto` 三列，把附件、文本域和唯一的动作按钮
  放进同一张带描边的圆角卡里，附件与动作按钮是 36px 的等宽圆形按钮而非等高竖条。三者共享的是提交
  状态机、附件生命周期、键位语义和焦点/禁用反馈，不是列模板；改动任何一个都要确认另外两个的契约
  仍然成立。
- 终端和原生输入框上方都不放常驻的模式、思考或模型控制条，保持提示词编辑区单一、安静；Claude 的模型、
  权限模式、思考程度和请求值/实际值全部由底部状态栏及其浮层呈现，切换不能清空输入草稿。原生对话
  不再拥有自己的 `.native-control-bar`：同一条底栏在原生可见时改读 Agent SDK 快照，两种模式共用一套
  控件与浮层，用户不必学第二套位置。Codex 保留原生 TUI 审批，`/plan`、`/permissions`、`/model` 只通过
  底栏或命令页生成命令骨架。
- Claude 权限请求使用主题化对话框逐个排队，提供“本次允许”“拒绝并填写原因”，只有上游返回
  `permission_suggestions` 时才显示持久允许范围。高风险选择继续有明确确认；`Esc`、界面关闭、
  Hook 超时或会话代次失效都回退原生交互，绝不能表现为已允许。
- 发送时把当前提示词复制成一个固定定位的气泡，从输入框位置上浮淡出（`--ease-accel`，
  `--dur-progress`）后自行移除。它只是视觉反馈：文字照常进入 PowerShell，动画不参与任何
  发送逻辑，失败也不会影响发送。`prefers-reduced-motion: reduce` 时直接不显示气泡。
- 聚焦时输入框播放一次收敛式 `composerFocusIn` 微光：描边收到 `--accent-ring`、浅底用
  `--accent-tint`、光环有一次过冲后落定。该规则由 `.terminal-composer textarea:focus` 与
  `.chat-composer textarea:focus` 共用，改动必须同时对两者成立。

## 独立对话

- “独立对话”位于“工作区”之后；工作区、独立对话、接入、扩展构成固定顶层次序。它是与 Claude Code 项目终端
  并列的主工作区，左侧整栏只显示对话历史，右侧按 `工具栏 / 消息滚动区 / 输入区` 三行布局。
  独立模型接入统一从工具栏右上角齿轮打开模态窗，说明必须明确“不复用、不修改项目接入”。
  左栏不再保留“模型接入已移动”之类的迁移提示，也不给历史列表设置固定像素高度上限。
- 点击活动栏“对话”后，在输入框可用且没有模态窗或详情抽屉占用焦点时，于下一绘制帧自动聚焦
  消息输入框；再次点击“对话”收起历史侧栏时也一样。焦点请求不得抢过生成中的禁用状态或
  已打开的模态界面。
- 配置表单依次呈现协议、接口地址、模型、认证方式和凭据；已保存密钥只显示状态，密码框永远
  为空。Anthropic 与 OpenAI 兼容协议用显式选择，不根据 URL 暗猜。“测试连接”与“保存配置”
  并列，使用当前未保存草稿发出 1-token 最小请求，结果明确显示成功/失败、延迟和可用 usage。
- 工具栏持续显示当前模型、“实时上下文”和输入/输出 Token。输入框草稿立即计入估算值；
  流式过程中逐段刷新；供应商返回 usage 后显示精确值。估算值必须带“约”，不能伪装成精确计费。
- 用户消息靠右并使用强调色浅底，模型消息靠左使用普通卡片底；标签同时显示“你 / 模型”，
  不仅依赖左右位置区分角色。Claude 的模型标题使用衬线且卡片宽松；Telegram 把角色标签
  变成紧凑圆形头像并使用更小气泡圆角。结构差异只用 `data-theme`，颜色仍只走 token。
- 模型正文按正常文档流排版 `h1-h6 / p / ul / ol / blockquote / table / pre / code / a /
img`；表格横向滚动，代码块显示语言与复制按钮，公式居中且保留 MathML。流式尾部每帧重建，
  已稳定前缀不闪烁，未闭合围栏保持不稳定直到闭合。
- HTML 代码块下方可出现“运行此可视化”，但不得自动执行。运行区域有固定工具栏、加载/
  错误/停止状态与受限高度 iframe；“详细信息”从右侧可中断滑入，按顶部说明、紧凑网络策略、
  运行实例和请求审计分区。抽屉内容使用 `align-content: start` 与最大内容行，短说明和开关
  不得被空余高度拉伸；每个分区都要说明它控制或记录的边界。打开时消息区和输入区设为
  `inert`，`Esc` 与遮罩点击都可关闭。
- 输入区的回形针位于 textarea 左侧；待发送附件在上方横向卡片队列显示缩略图、文件名、
  大小和移除按钮。已发送图片显示主进程生成的小预览，PDF/CSV/文本显示文件卡，缺失附件要
  明确标成不可用而不是留下空白。
- 输入区是真正的多行 `<textarea>`，`Enter` 发送、`Shift+Enter` 换行，IME 组合期间不拦截。
  生成时禁用重复发送并显示独立“停止”按钮；工具栏“新对话”开启空白记录，不删除旧历史，
  也不影响项目或 Claude Code session。输入框必须去掉浏览器默认焦点环，并与项目终端输入框
  共用同一套聚焦规则、同一个 `composerFocusIn` 动画和同一套发送按钮几何——发送按钮与输入框
  齐平并随其高度伸缩。聚焦描边、浅底和微光均使用当前主题的 `--accent-ring` /
  `--accent-solid` / `--accent-tint` 与人格动效令牌，不允许写死 Claude 的陶土橙色。
- 每次发送与生成结束后原子写入本机历史；左栏按最近更新排序，显示标题、时间、消息数和
  Token。历史区从标题下方延伸到侧栏底部并独立滚动；空列表说明仍靠近标题，不被弹性空白推到
  底部。最多 50 个对话、每个 100 条消息。删除按钮具有包含标题的 `aria-label`，并复用
  应用内“永久删除、无法恢复”确认框。最多 50 个对话、每个 100 条消息。
- 独立对话的助手消息角色显示“Claude”。
- 空状态居中解释独立边界；首次模型输出前发生瞬时网络、限流或上游故障时，模型消息卡显示重试
  原因、等待时间与当前次数，收到正文后自然替换这段占位。已有部分文本后断流不自动重放：卡片
  保留正文并追加醒目的中断说明，同时用 toast 补充；该说明是视图状态，不得写入历史正文或
  下一轮模型上下文。
  Markdown 只从 lexer token 创建白名单 DOM，原始 HTML 降级为文本；Artifact 是唯一显式的
  HTML 执行入口。`https:` 与 `data:` 图片直接内联渲染，`<img>` 带
  `referrerpolicy="no-referrer"`、`loading="lazy"`、`decoding="async"`；其他协议的图片降级为
  替代文本。

## 全局设置

- 设置入口固定在活动栏底部，与业务页分组。依据 Microsoft 的 Windows 应用设置建议，设置应
  统一入口、放在导航末尾并把相关内容分组；现有分类为
  “总设置 / 高级设置 / Claude 执行 / 接入 / 代理 / 路由”。
- 全局设置使用主题化原生 `<dialog>`：固定标题与底部操作区，左侧分类导航，右侧内容独立滚动。
  720px compact 档分类导航改为顶部横排；820×640 最小窗口内所有控件仍可聚焦和滚动到。
- “总设置”采用单列设置卡：左侧“名称 + 说明”，右侧放开关、选择器或只读值。首期包含开机
  启动、主题、语言和当前版本；只有简体中文可用时，语言选择器禁用并解释原因，不伪造选项。
- 设置分为“即时控制”和“草稿选项”。应用代理保存/测试、安装/卸载、Provider 保存等即时控制
  点击后直接执行；开机启动、关闭行为、主题与高级选项先保留在对话框草稿中。任一草稿字段偏离
  打开时的持久值后，在完成按钮下显示 `*N 项未保存`；恢复原值必须立即扣减，计数归零就隐藏。
  “完成”按字段提交，“取消/关闭/Esc”恢复草稿和主题预览，已经执行的即时控制不回滚。
- “高级设置”以中转站兼容开关为主，并在下方放两个 CLI 后台的只读高级入口。每个开关默认关闭，说明写清它修复的是哪一类
  中转站缺陷、以及开启后会给会话附加什么；行为正常的中转站不该为一个用不上的修复付出代价。
  首个开关是“联网检索隔离”：开启后新会话才注入 `claudedock-web-research` 子代理、对应提示词与
  `WebSearch/WebFetch` 守栏，用于修复模型调到 high 以上无法联网检索的中转站；改动在下次启动
  会话时生效，界面据实说明，不假装立即改变已经运行的会话。
- “Claude 执行”只管理未来启动的 Claude Code 进程，不即时改写运行中或已经准备完成的会话。
  五档滑杆从“最省 Token”到“最佳性能”，但并发只表示吞吐策略，绝不宣称更高档会让模型更聪明。
  每档旁必须显示并发子代理、派生深度、工具调用并发和动态工具搜索的请求值；下方表格把请求草稿、
  Claude 默认、当前有效值、支持状态、来源和原因分列，不能把“已保存请求”伪装成“运行期已生效”。
- “使用推荐”只依据本机逻辑处理器与当前可用内存选择保守基线；没有稳定性基准和额度余量证据时，
  不得自动推到高吞吐或最佳性能。“恢复 Claude 默认”删除 ClaudeDock 管理的覆盖；自定义模式允许输入
  宽松但有界的整数与 `true / false / inherit / auto / auto:N`，版本或路由能力未知时保留请求但不注入。
- CCR CLI 与 ChatGPT 网关后台入口各用一张“名称 + 实时状态 + 按钮”的设置卡；后台未运行时按钮
  必须保持灰色，点击入口本身不能启动服务。ChatGPT 卡说明管理密钥会复制到剪贴板，正常用户不必
  打开该面板。
- “接入”完整承接原高级设置的唯一 DOM 节点，包括认证来源、自动发现、Router、cURL 与诊断。
  原“接入”业务页不再保留第二个高级入口。取消仍恢复未保存草稿，已经执行的安装、启停和
  Provider 保存仍立即生效。
- ChatGPT 托管网关的安装和 OpenAI 授权是应用级即时操作，不因冷启动尚无项目而显示一个看似可用、
  点击后只给瞬时提示的按钮；项目级模型实测与保存只在项目打开后执行。操作锁按发起范围记账，项目
  切换或关闭不能遗留永久禁用状态，重复点击仍合并到同一进行中操作。
- “代理”只呈现外部应用代理的地址、凭据和作用域，不出现远程网络服务或系统网络修改控件。
  面板顶部常驻两条边界说明：软件只传递用户提供的连接参数；设置不修改系统网络。
- “启用外部应用代理”属于设置对话框草稿，并随底部“完成”统一保存。关闭时地址、凭据、测试和
  作用域区域整体降级且设为 `inert`，已填写草稿仍保留；重新开启后原值可继续编辑。取消、关闭或
  Esc 必须恢复打开对话框时的启用状态与字段基线，不能只回滚文本字段。
- 表单顺序固定为“启用 → 协议/地址/端口 → 可选账号密码 → 操作 → 作用域”。HTTP 与
  SOCKS5 是显式协议选择；地址输入不接收 URL，端口使用 1–65535 数字控件，避免一个字段同时
  隐藏协议、凭据和路径。
- 已保存密码永不回显。状态行只显示账号与“密码已由 Windows DPAPI 加密保存”；密码框留空
  表示保留，清空账号表示删除凭据。错误必须落在 toast 和可读状态中，不能把无效地址静默纠正。
- “检测本机代理”只呈现系统代理与环境变量中的无凭据候选按钮；点击候选只填入草稿并提示用户
  确认作用域，不自动保存或启用。“保存设置”是唯一提交动作，“测试 GitHub 连接”只测试已保存
  配置并在结果行显示成功/失败、HTTP 状态、延迟和检查时间。
- 作用域是 CLI、ClaudeDock 自身网络、独立对话三个复选项，下面用动态摘要复述实际路径。
  SOCKS5 被选中时 CLI 复选框立即取消并禁用，因为 Claude Code CLI 只支持 HTTP/HTTPS 代理；
  该限制同时用控件状态和文字表达，不能只依赖禁用色。
- 旧版网络配置不读取也不迁移，页面用通用表述说明升级边界，不显示已经移除的功能。
- 代理表单沿用 `.field`、`.settings-row`、`.proxy-runtime-actions` 与现有按钮令牌；窄窗口下
  地址、端口和操作自然换行，候选按钮可键盘聚焦，测试结果使用文字与语义色双重表达。
- 接入测试的补救卡使用整体忙态。任一补救动作执行时，服务商选择、配置表单和同卡全部按钮都禁用，
  容器设置 `aria-busy`；安装 Router 不得提前清空当前草稿或把服务商静默切成 Gateway。
- “路由”把自动接入向导、供应商能力矩阵、阶段进度与 CC Switch 适配器收在同一面板。路由方式由
  供应商协议能力自动决定，普通流程不显示“是否使用 CCR”开关；决策卡只解释软件已选择直连还是
  CCR 及其理由。进度条显示检查、下载、安装、启动、配置与
  校验阶段。CCR 一键安装的主状态卡位于按钮上方，显示“阶段名 · 第 N/M 步”和当前动作；按钮禁用
  只是防重复提交，不能代替进度说明。完成/错误至少保留 6 秒再恢复静态状态。CC Switch 卡明确只做
  官方 MSI 生命周期与单向深链导出，不暗示读取其内部数据库。
- 下载中心按“正在进行”和“下载历史”分区；进行区聚合下载以及安装/卸载等忙碌操作，下载任务行
  包含来源、状态、真实百分比或不确定态、速度、已用/剩余时间与暂停/继续/取消。任务完成前进入
  单独“正在校验”状态；空状态必须说明只有相关任务出现时才会占用此处，不显示纯空白面板。
  历史按完成时间倒序、最多保留 100 条，只展示终态元数据并支持逐条删除或全部清空，不显示 URL、
  本机输出路径或凭据。**不确定态只属于仍在推进的任务**：服务器没给长度时百分比
  全程是 -1，因此已失败、已取消、已完成的任务必须显式停在确定态，否则进度条会在「下载失败」
  的文字旁边继续转圈，等于用动效否认自己刚给出的结论。
- MCP 是“扩展”完整画布内的第二个分段，与“插件”共用顶层入口。MCP 分段顶部固定作用域筛选、搜索与刷新；已安装卡显示客户端、作用域、
  配置来源路径、传输方式和健康徽章，目录卡只提供定向安装。项目共享启停先打开 diff 确认框，
  还原备份使用独立确认框；无项目、无配置、无筛选结果分别使用有下一步的空状态。
- MCP 目录以官方 Registry 为在线来源，并在网络不可用时保留应用内精选；推荐标签只表达可追溯的
  官方/精选来源，不把未经验证的星数或远端营销文案当作信任等级。Claude 插件同样先展示 Anthropic
  官方市场的精确来源，再展示受控社区目录；添加市场本身不等于安装插件，安装始终是单独动作。
- **整体重建的列表只让真正新增的条目播放入场动画**。两个 MCP 列表每次渲染都清空重建，卡片
  入场动画因此在所有行上同时重放，安装一个服务器看起来像整块面板闪了一下——动效指向了没有
  变化的地方。渲染时比对上一次的服务器集合（同一筛选条件下才可比），只有新出现的卡片播放
  入场并叠加一段强调色落定过渡，其余卡片保持静止。
- 主题选择与终端工具栏是同一事实来源，任一入口修改都同步另一个选择器、CSS shell tokens 和
  Windows 原生标题栏。开机启动失败时恢复开关并显示错误。
- 设置分类调研的后续候选为“对话与模型、终端与项目、通知、隐私与数据、辅助功能与外观、
  高级与诊断”；只有功能密度足够时再拆分。Windows 官方依据：
  <https://learn.microsoft.com/en-us/windows/apps/design/app-settings/guidelines-for-app-settings>。

## 性能约束

- xterm 使用 WebGL 渲染器（`@xterm/addon-webgl`），并监听 `onContextLoss` → dispose
  回退 DOM 渲染器，不让丢失上下文变成白屏。
- 新建活动终端必须先把容器设为可见再调用 `terminal.open()`；冷启动和会话首次可见时最多
  连续四帧重试 `fit()`。窗口、侧栏和抽屉的连续尺寸变化只走 100ms 尾沿防抖；拖拽期间只
  标脏，释放后恰好 fit 一次，`ResizeObserver` 对相同整数宽高短路。
- 输出写入在两侧都做合并：主进程按 session 和精确 `ptyGeneration` 攒 8ms 或 64KiB UTF-8 字节发
  一次 IPC；每个 timer 固定拥有当时的 generation 与缓冲对象，较旧 generation 不能替换较新的待发
  缓冲，flush 还要复核当前 workspace generation。自然退出先同步发送末尾缓冲，显式替换仍只丢弃旧
  generation。渲染层用 `TerminalOutputPump` 按 `requestAnimationFrame` 调度，同一 view 只允许一个
  `terminal.write` 在途，每次最多 64Ki 个 UTF-16 code unit 且不切开代理对；实时输出不设截断上限，
  也不丢弃旧分块。只有 xterm callback 确认后才消费队列并推进完整 revision。排队、RAF 和完成回调
  都必须同时匹配当前 view 对象与 generation。逐块 `send`/`write` 是输入卡顿的真实来源——每块都会
  触发一次 xterm 重排。
- 主进程的合并只影响发给渲染层的消息；`consumeTerminalOutput` 仍然逐块接收，因为它跨块
  跟踪退出标记，喂给它合并后的缓冲会漏判。generation-scoped discard 也只能清理目标缓冲，不能
  删除已经替换它的新 generation。
- 终端操作遮罩复制当前 xterm canvas 作为冻结快照并只模糊快照，真实 xterm 在下层继续消费
  输出和完成权限探测；遮罩引用计数且必须在 `finally` 释放。禁止暂停 `terminal.write()`，
  也不用 `backdrop-filter`，否则既会让探测超时，也会持续强制 GPU 合成。

## 会话生命周期与目录所有权

- `ptyGeneration` 是每次真实 PTY spawn 的唯一生命周期身份，由 `TerminalSession` 在 spawn 尝试前
  恰好递增一次；失败的 spawn 也消费该 generation，stop 不递增，restart 只因一次新 spawn 递增
  一次。所有 data/exit 回调还要匹配创建它们的 PTY 对象，不能只看 session ID。
- 直接 start/restart/stop 的顺序是不可拆分的设计不变式：先预检 renderer 提交的 expected
  generation，再失效旧操作并取得它的 unwind Promise；立即解除该 generation 的 permission probe，
  然后等待旧 lease 真正 unwind；随后复检 generation，只清理该 generation 的输出和 runtime，最后在
  禁止递归失效的同步区间内执行 PTY 动作。probe 必须先解除，因为被取消的旧操作本身可能正在等它。
- 每次 Claude launch 使用一个独立的
  `launch-<runtime-token>-<launch-generation>` 目录，同时拥有 settings、statusLine metrics、
  PostCompact signal 与顶层 Stop turn-stop 文件。任何异步 artifact 读取在 I/O 后都要再次匹配 launch
  generation、精确 `ptyGeneration` 和路径，旧 launch 不能回写新状态。
- 开发引擎切换以解析并规范化后的项目目录为所有权边界：目录在第一个 `await` 前同步保留，同目录
  已有和新开的开发操作都必须避让；每个异步边界后重新枚举 session/generation，持久化是最后一个
  无后续 `await` 的同步提交。不同目录互不阻塞。
- 配置事务在同一目录内严格 FIFO，并固定按 session lease → directory reservation 的顺序取得所有权。
  取得目录后先取消并等待已经开始、且尚未排入同一配置队列的兄弟会话开发操作；随后先保存原始快照，
  在 profile 仍未变化时完成 OpenAI 转换、历史解析或 Router Provider 等异步准备，复核快照后才执行无
  `await` 的同步 profile commit。后续路由验证、状态回读和可选会话恢复仍留在同一屏障里。rollback 只有
  在事务仍拥有目录与精确已提交快照时才允许写回，不能覆盖后来成功或外部更新；已排队事务不会互相
  等待造成死锁，不同目录继续并行。Router Provider 是先完成的独立持久化，项目 profile 失败只回滚
  项目快照，不冒充能够恢复 CCR 内已经保存的 Provider。
- renderer 对 Claude/Codex/开发引擎状态请求和启动续体使用 per-session generation；成功、拒绝、
  `catch` 与 `finally` 都必须持有当前 token。删除再创建同一 session ID 也不能让旧 token 重新生效，
  workspace 更新会裁剪缺失 session 的 token、terminal baseline 与 launch-result tombstone。

## Claude 工作台

- “会话”页先显示 Claude Code 版本门禁与接入路由，再显示上下文进度、token、费用、持续
  时间、运行模型和会话 ID；数据为空时使用“等待首个响应”，不伪造 0 token。
- 当前路由卡下方增加健康卡：1-token 测试通过使用绿色，部分兼容使用琥珀色，Router
  未就绪或真实 Claude Code 请求失败使用红色。错误卡必须写明来源是“连接测试”“Router
  状态”还是“真实会话”，并提供“前往接入排查”操作。
- 主会话需要在线资料时，界面不把底栏思考档位永久改成搜索兼容档；WebSearch/WebFetch 由
  会话内专用子代理以“均衡（high）”执行，结果返回后主会话继续显示并使用原档位。若旧网关
  仍触发 thinking/effort 400，底栏只在一次兼容重试期间显示临时“均衡”及“完成后自动恢复”
  文案；不能再写成本会话永久锁档。
- 当前项目指向 `3456` 且 Router 无 Provider/网关未运行时，健康卡必须给出明确修复入口；会话
  主按钮仍保持原主操作外观和可点击性，由启动预检尝试恢复应用可管理的服务并在失败时引导到
  “接入”。远程直连的无关 CCR 错误不得阻止会话。运行中错误通过 toast 与健康卡同时反馈，但同一
  错误不重复播报。
- 上下文进度 0–65% 使用青蓝到薄荷绿；65–85% 转为琥珀色；85% 以上转为珊瑚红。百分比、
  数字和颜色同时表达，不只依赖色彩。
- 新建入口只保留在项目行的 `+`；Claude 工作台不再提供第二个“新建”按钮，只保留当前终端的继续最近 /
  选择历史等恢复入口。任一恢复入口点击后必须在第一个 `await` 前建立精确 launch attempt owner、同步
  禁用入口并设置 `aria-busy`：先显示“正在进行网络预检…”，精确终端 lifecycle 进入启动阶段后显示
  “正在启动…”，暂停确认时显示“等待网络确认…”。冷状态也不能留下可重复点击的空窗；只有当前 owner
  的失败或真实 session/generation lifecycle 证据可以恢复入口。
- 启动锁按 session 与 generation 隔离，IPC 成功返回不直接解锁，也不设 renderer 超时。只有新的
  conversation UUID、新的运行中 `ptyGeneration`（即使 Windows 复用了同一 PID）、新的运行中
  PowerShell/ConPTY PID、Claude 已活动后退出且原 PowerShell 仍运行，或明确 IPC 失败、终端
  `stopped/error`、会话关闭/删除时才释放；`starting` 快照本身不解锁。旧确认、IPC 拒绝和
  `catch/finally` 都不能释放后来一次启动，删除 session 时还必须裁剪只用于接收精确迟到结果的
  tombstone。Codex 使用独立启动状态，不与 Claude 锁互相污染。
- 官方网络预检只有在结构化 Claude 状态明确标记官方接入时才参与启动可用性计算；状态缺失时不能
  猜测 provider，更不能在独立渲染路径直接改写启动按钮。按钮的 enabled/disabled/hover 统一由组件
  状态和主题 token 派生，悬停不得改变透明度或呈现成灰色禁用态。
- 原生启动的成功、返回失败和 IPC 异常都必须在 `finally` 恢复输入坞状态；没有 snapshot 的首次
  失败也不得停留在“正在安全启动 Claude”。缺凭据显示“尚未接入模型”，接入测试阻塞显示“接入配置
  暂不可用”，安装环境未就绪则显示 Claude Code 环境问题。
- 会话入口下方是每项目持久化的“允许「完全允许」模式”复选框，默认勾选。说明必须写清它只是
  启动时预置、不会自动激活，以及关闭后需要重新启动会话才生效。它是一个逃生开关：
  Claude Code 首次以该标志启动时会显示自己的一次性确认框，`root`/`sudo` 下还会直接拒绝，
  用户必须能关掉它。
- 历史对话只在左侧项目文件夹层级中出现，工作台不重复列出。历史项只显示名称与最近活动
  时间，完整列表放在文件夹内约六行高的独立滚动区中：运行中对话固定在上方不随滚动移动，
  滚动位置在列表重建后保留。左键在新的当前对话中恢复；悬停/聚焦显示红色 `×`，右键菜单
  同时提供重命名与删除。删除必须进入“永久删除、无法恢复”的应用内确认框；运行中对话与
  历史对话共用应用内重命名弹窗，不依赖浏览器原生 prompt，也不为了补齐内容伪造 token
  或费用。
- 运行中对话的主题标题来自 Claude Code 官方 `statusLine.session_name`：有用户自定义名称时
  使用自定义名称，否则使用 Claude Code 根据首条提示词生成的标题。自动标题到达时在原位置
  播放打字机动画（先擦除、后逐字输入、带光标），保留新旧标题的公共前缀以减少闪动；
  动画状态与 DOM 解耦，列表随工作区刷新重建时从当前帧继续，不会重播或跳变。
- “命令”页使用双列命令卡片呈现命令名和用途；参数输入只对 `/compact`、`/rename` 等支持
  参数的命令生效，`/theme` 打开 Claude Code 自身主题选择，`/clear` 必须二次确认。
- “快捷键”页集中说明 `Ctrl+C`、`Ctrl+V`、`Shift+Enter`、`Ctrl+L` 和右键菜单；映射只
  对应用内终端生效，不暗示已修改外部 PowerShell 或系统键位。
- “接入”页固定为带顶部进度条的两步向导：“选择模型 → 配置与验证”。第一步只显示 Claude 官方订阅、
  ChatGPT 官方订阅、国产模型、API / 中转站四个入口，采用二列自适应卡组；国产模型的完整目录进入
  卡片下方的小选择框，不再用六组折叠面板或三栏服务商墙要求用户先理解目录结构。
- 第一页选择只建立显式状态，点击“下一步”才沿前进方向进入对应流程。第二页必须同时提供“上一步”和
  “下一步”；无后台事务时返回沿相反方向回到选择来源。普通 API、国产模型与 Claude 登录继续复用原有
  配置、真实测试、保存、历史、Router 与 cURL 节点，不复制第二份表单。
- 向导底部操作区是悬浮在内容上的长胶囊：背景取当前主题表面色并通过 `--mask-blur` 对胶囊后方的实时
  内容执行高斯模糊，边框、阴影、字体和按钮圆角全部使用语义 token。上一步与下一步贴合胶囊两端，
  中间状态允许换行；窄窗口缩短按钮宽度但保留统一字号和触控高度，胶囊四周始终留下阴影与焦点环的
  绘制余量。各尺寸都保持 bottom-sticky，内容 viewport 预留底部安全区，不覆盖最后一排卡片与焦点环。
  跨步骤重排先测量胶囊的屏幕首末位置，再只用 `transform` 和主题弹簧曲线做 FLIP 平移；文字、按钮和
  透明度不随步骤淡变。减少动态效果时直接落位。步骤 viewport 不裁切卡片、阴影、焦点环和进退场帧。
- ChatGPT 授权等待是可打断阶段：“上一步”先取消主进程拥有的 OAuth 子进程，收到完成结果后再返回。
  Proxy API 安装/配置、模型发现、真实测试、全局选择保存和修复属于不可打断阶段，此时“上一步”禁用并用
  状态文字说明原因。可打断性来自类型化 progress 事件和主进程二次门禁，不从按钮文字推断；其他项目
  的忙态不能取消当前项目以外的事务。
- 四入口卡片同时显示名称和一句用途；当前选择使用强调色描边与勾选，已保存项另显示“下个对话”文字。
  “选择模型来源”标题说明与卡组之间使用响应式语义间距，窄屏适度收紧但不与卡片粘连。
  同一按钮族始终使用同一 caption 字号与控件高度；四主题只通过语义 token 改变字体、表面、圆角、
  阴影和非线性运动，不维护主题专用 DOM。
- “ChatGPT 官方订阅”与其他三种模型来源并列；“官方”只描述用户使用的 ChatGPT 账号订阅，不暗示
  ClaudeDock 的转换层属于 OpenAI。进入第二步后显示来源、第三方边界和受管网关状态卡，不给普通用户
  展示 CLIProxyAPI 命令、端口、配置文件或 CC Switch 操作步骤。来源准确写为“OpenAI Codex 负责人
  Tibo 公开分享的方案”，同时明确 CLIProxyAPI 仍是独立第三方项目。
- 已授权状态在操作区以“当前账号：[邮箱]”标签显示；旧授权无法解析邮箱时只显示明确的降级文案。
  退出操作使用危险色和“退出当前账号”，只停止 ClaudeDock 拥有的本地网关并清除其独立授权目录，
  不打开或控制浏览器，不退出 Google 或其他网站账号。退出完成后，重新登录必须由用户另行点击主操作；
  Token、刷新令牌和管理密钥仍不得进入 renderer、日志或账号标签。
- 受管状态至少区分“尚未安装 / 等待 OpenAI 授权 / 已停止 / 已就绪 / 操作失败”，主操作按状态显示
  “一键安装并登录”“登录 OpenAI 并自动配置”“启动并设为下个对话接入”或“重试”；已授权后提供“退出当前账号”
  次操作，退出后再由主操作重新登录。环境检测、Claude Code 补齐、安装、校验、生成本地密钥、启动、模型探测、真实连接测试和
  下个对话选择保存都在应用内完成，不要求先打开项目或终端；唯一离开应用的步骤是用户在 OpenAI 官方浏览器页面确认授权。
- 从点击开始到下载、校验、授权和全局选择保存结束，状态卡固定显示“安装进行中”，主按钮和退出账号按钮
  必须禁用。忙碌真值同时来自 renderer 当前操作与主进程状态，服务商列表刷新、项目状态重绘或重新
  构建指南都不能把按钮提前恢复；重复 IPC 只能等待同一个任务，不能创建第二份下载或授权流程。
- 普通接入页的 ChatGPT 流程只更新“下个对话接入”，不得停止、重启或改写任何已经打开的 Claude Code
  会话。连接成功明确说明下个新对话使用的模型；失败恢复原模型、原 profile 与可交互状态。
- 未完成授权时隐藏普通地址、认证和凭据表单；就绪后把网关实时返回的模型呈现为单一选择框，不提供
  自由输入模型标识或额外“测试并接入”按钮。切换选项自动复测并保存，失败恢复原选择和原配置。
  不把受管回环地址或随机客户端密钥暴露成用户任务；界面不提供密码、Cookie 或 OAuth Token 输入
  路径。动态模型框必须复用统一主题选择器、键盘交互和弹层进入/退出动效；进度卡以现有卡片进入动效
  出现，状态色与进度宽度使用设计 token 过渡，并由全局 `prefers-reduced-motion` 规则降到近乎静态。
  四套主题都要保留该流程的视觉冒烟截图。窄侧栏中状态文本与操作按钮允许换行，按钮保持键盘焦点、
  忙碌禁用和可读的失败信息。
- “自定义中转站接口”在第二步显式提供 Anthropic / OpenAI 协议选择，不根据域名暗猜协议。
  Anthropic 说明为直连；OpenAI 说明必须明确“由本地 Router 自动转换”，不能把转换路径描述成
  Claude Code 原生支持 OpenAI。切换协议时认证选项随之收敛：OpenAI 只显示 Bearer 或无认证。
- 自定义地址框接受域名、前导/尾随斜杠、`/v1` 和完整接口地址；失焦、切换协议、测试与保存时
  都使用同一补全规则，并把规范化后的完整端点回写。校验失败使用输入框原生错误状态，不静默
  修改成另一个主机；远程 HTTP、URL 用户信息、查询参数和片段仍须拒绝。
- 活动栏底部“设置 → 接入”完整容纳自动发现、认证来源、手动配置、Router 管理、cURL 识别、
  转换器说明和术语解释；认证来源首先说明
  ClaudeDock 显式凭据与 Claude Code `apiKeyHelper` 的组合，并提供“ClaudeDock 单一凭据”
  与“保留 helper”两种明确选择。选择 Router 或 cURL 时把对应
  完整工具临时提升到第二步，不复制功能或维护两套 DOM。全局设置层使用三行网格
  `标题 / 分类与独立滚动内容 / 固定操作区`，随全局主题换色，不使用
  `backdrop-filter`。完成保留输入；取消、关闭按钮与 `Esc` 恢复打开前的未保存输入，但明确
  告知安装、卸载、启停和 Router Provider 保存等已经执行的操作不会回滚。
- “设置 → 通用”按“启动与恢复 / 对话与模型 / 窗口与外观 / 关于”分组，不把启动、关闭、主题、
  对话恢复与版本信息平铺成一列。分组卡片使用短距离错峰入场，窄窗口把选择器与说明改为上下布局，
  并统一服从 reduced-motion。
- “自动加载上次对话”和“启动时自动接入模型”是两个分别保存、默认开启的开关。前者决定在允许自动
  接入时是否打开最后使用项目中最近的对话；后者是冷启动自动恢复的总开关，在窗口出现前真实验证并
  恢复该对话绑定的平台、账户/API、接口、详细模型和简单模型，再把结果保存为应用级“下个对话接入”。两者
  都开时必须先启动候选模型所需的后台路由、完成真实测试与可回滚事务，再启动 CLI；保存配置相同不
  代表服务已经连接。只开模型时使用启动阶段的短生命周期事务恢复接入，不创建可见对话；关闭模型
  开关时不自动打开或启动历史对话，并清空全局“下个对话接入”，冷启动保持未接入。关闭任一项不删除
  项目、历史对话或它们各自保存的模型绑定。
- 自动恢复对话进入接入与 CLI 启动阶段后，终端中央显示带旋转反馈的“正在连接模型…”；遮罩存在期间
  composer、xterm stdin 与底层 raw write 三层同时拒绝输入，直到事务和启动结果完成后才在 `finally`
  释放。composer 在静态 HTML 首帧即为禁用态，模型识别也必须等遮罩出现后再执行，不能短暂暴露可输入
  窗口；失败不启动错误模型的 Claude 会话，也不会静默改用另一套模型。
- “选择历史配置”是独立的接入主流程组件，固定放在模型与凭据表单之前；不能
  移进高级诊断层。内联区域只显示与当前模型来源同类的记录：Claude 官方订阅、ChatGPT 官方订阅、
  国产模型、API / 中转站互不混排。接入页右上角另有“接入历史记录”入口，在带主题高斯遮罩的原生
  模态层中展示完整项目历史；四个横向分类保持在同一轨道，点击或方向键切换时按左右方向使用弹簧
  `transform` 平移，窄屏允许分类条自身横向滚动，卡片区改为单列且不得裁切。模态层的当前分类独立于
  接入表单选择，恢复记录导致表单来源变化时不能把用户正在查看的分类强制跳走。
- 历史弹窗打开时没有隐式默认项：底部是灰色“取消”。点击一张历史卡只建立当前选择，卡片以
  主题强调边框、收敛式焦点动画、勾选标记和 `aria-pressed` 同时反馈；标题旁同步显示
  “当前选择：名称”，底部才变成同尺寸的蓝色“完成”。切换分类清空选择，右键“恢复”也只能进入
  这条选择—确认路径，不得绕过确认直接改配置。
- 确认历史记录后，普通“选择模型 / 配置与验证”进度、表单和底部胶囊全部隐藏并设为 `inert`，页面只保留
  “当前正在接入 XXX”的专用状态。API / 中转站与国产模型必须在提交前完成真实端点、认证和模型响应
  探针，速度快也不能跳过结果页；Claude 官方现有登录因令牌归 Claude Code 所有，独立 Messages 探针可
  明确提示不能借用该令牌，但账号状态由安全的 CLI 状态投影另行展示。失败保留各阶段结果和“重新接入 /
  返回接入页”，成功停留 1.5 秒后回到普通接入页。“取消接入”先显示正在取消，只有主进程确认精确事务
  已经退出、原项目配置与本事务准备的 Router 状态已按所有权完成回滚后，才恢复普通页面；检测到较新保存
  时不得覆盖。
- 历史加载、恢复、删除、重命名和恢复页 settlement 都同时绑定单调 generation 与当前 session；切换项目
  会立即清空历史和专用状态并使旧 owner 失效。主进程还要确认该 session 仍指向发起时的项目目录，因而
  旧项目的迟到结果不能更新新项目、关闭新项目 busy 或触发 1.5 秒成功回落。
- 点击历史对话时先比较该 conversation 创建时绑定的完整接入与当前接入。差异弹窗使用原生模态层和
  背景模糊，左右卡片逐项列出详细模型、简单模型、订阅账户或 API 凭据指纹、认证方式、协议、脱敏
  地址与接入名；差异摘要不能只比较模型字符串。左下为“不再提示”，勾选后把本次点击的按钮保存为
  默认；“使用该对话原有模型”为白色次按钮，“使用当前模型”为语义蓝主按钮。原接入缺少可恢复凭据或
  订阅账户已变化时必须明确解释并禁用，不得静默替换。
- 比较前把空白简单模型规范化为详细模型，因此两栏都填 `DeepSeek V4 PRO` 与下栏留空完全等价；反之
  平台、协议、端点、账户、API 指纹、Router Provider、详细或简单模型任一不同都必须被识别。打开模型
  弹窗、双卡、方向箭头和启动按钮进度使用统一非线性入场/流动动效，并服从 reduced-motion。
- 选择原接入后，左侧启动按钮按阶段显示“正在切换模型并检查网络…”或“正在切换对话模型…”，然后
  显示“正在恢复历史对话…”，不额外出现“当前”二字。只有国外模型显示网络检查；国产与本机模型不把
  网络作为可见步骤。历史列表同时过滤终端和原生 owner，运行中对话不能在历史区重复占一行。
- 接入页标题下固定显示脱敏的“下个对话接入”，它不依赖当前是否打开项目或终端；API / 中转站优先显示用户保存的名称，缺失时显示去掉
  userinfo、query 和 fragment 的 HTTP(S) 地址；国产模型显示服务商、模型、凭据是否配置和脱敏接口；
  ChatGPT 官方订阅显示受管网关验证过的账号邮箱；Claude 官方订阅显示 `claude auth status --json` 经主
  进程白名单投影后的账号标识、登录状态或认证方式。正常异步读取期间必须写“正在读取账号与本机网关状态”，只有命令不可用、超时或格式异常等真实失败才显示“账号状态读取失败”；不得把原始 JSON、令牌、Windows 用户名或项目名带入界面。
- 勾选“启动时自动接入模型”后，冷启动恢复期间整个接入画布置灰、轻微模糊并设为 inert，中央固定显示“正在接入模型”、当前步骤、详情和倒计时；Claude 官方订阅或 ChatGPT 官方订阅还显示主进程投影出的登录账户。加载箭头按其逆时针字形方向匀速旋转，不能出现箭头方向与运动相反的错觉。离开后再返回只恢复同一主进程事务的快照，不闪现“暂不可用”，不重复开始接入。默认 2 分钟后出现“取消接入”，默认 5 分钟由主进程强制中止；两个整分钟阈值在设置中可改，且硬截止必须晚于可取消时间。取消、超时或失败都要等待可回滚事务真正退栈才恢复原界面，不得提前显示成功或提前解锁。
- 接入页每次从活动栏进入都先显示“选择模型”，不能因项目切换、终端状态广播或延迟初始化结果自动跳到
  “配置与验证”。用户已经点击新来源后，较早发出的全局 profile 读取结果只能刷新摘要，不能覆盖选择。
  保存或模型切换失败必须恢复原 profile、下拉值和按钮状态，并显示明确错误；不得留下看似成功的模型。
- 每条记录以可点击恢复卡显示自定义名称、上游协议、连接方式、接口/网关、
  主模型、小型/备用模型、认证、凭据状态、保存时间和网关状态。名称优先使用用户重命名值，其次是
  Router Provider 名或自定义地址主机名，不再把所有自定义连接都显示成“自定义”。列表最多
  显示 20 条并在组件内部滚动，长 URL 与模型标识必须在值列自然断行，不得撑破窄侧栏。记录变多
  时被压扁的必须是页面滚动条而不是这个列表：接入页及其直接子块不参与弹性收缩，列表在自己的
  固定高度内滚动，页面整体滚动由控制面板负责。
- 协议与连接方式是两枚相邻文本标签：协议明确写 `Anthropic`、`OpenAI` 或“协议待确认”，
  连接方式明确写“官方直连 / 中转直连 / 本地直连 / 本机转换器 / Router 转换”。颜色只辅助
  区分，不能替代文字；从旧存储无法判断的 Router 上游必须显示待确认，不能用默认色伪装成
  已识别。
- 环境准备卡同时显示 ClaudeDock/Claude Code 已安装版本、可检测到的最新版本和检查时间。
  ClaudeDock 首次加载和手动刷新只检查打包配置中的 COS 当前通道，不自动下载；只有确认存在新版才显示
  “下载并更新”，下载中在原按钮显示进度，SHA-512 校验完成后显示“正在安装…”并自动退出、静默安装，
  安装完成后重新启动，不再要求第二次点击或确认。只有这次显式操作授予下载和自动安装权限；后台检查仍只检查。
  清理或启动安装器失败时显示可重试错误并留在应用内。开发版隐藏安装操作。Claude Code 不展示手动
  来源下拉：原生安装沿用官方更新，npm 安装用不超过 128 KiB 的样本自动择优，文案明确“不创建重复
  安装”。
- ClaudeDock 行必须保留完整预发布版本，例如 `v5.0.0-rc.17 → 5.0.0-rc.18`；可用、下载中、已下载
  和正在安装四个阶段都继续计入更新总数。当前 feed 是打包时固定的单一 generic HTTPS 地址，不显示
  不存在的 GitHub/镜像选择、测速或签名清单状态。
- “已下载”表示 electron-updater 已按所读取通道清单完成传输和 SHA-512 一致性检查，并不表示清单或
  安装包具有发布者签名。摘要不符、降级、网络或安装器错误必须显示可操作失败状态并保留当前版本；
  不能把“TLS 可用”“SHA-512 通过”或“下载完成”表现成“发布者身份已验证”或“供应链已验证”。
- 自动检测卡片同时展示服务名、真实 API 基址、运行/待处理/离线状态、检测依据和管理入口。
  Claude Code Router 必须明确区分 `3456` 模型接口与 `3458` 管理页。
- 对用户自行启动的 CLIProxyAPI 仍可显示高级自动检测卡，但它只作为外部通用 Gateway，不得与
  ClaudeDock 受管的 ChatGPT 订阅流程混为一谈，也不得把检测到的外部进程或配置接管进受管目录。
- cURL 结果使用绿色 Anthropic、琥珀色 OpenAI 和中性未知三种状态；只显示“已识别密钥”，
  不显示完整值。OpenAI 结果必须说明上游密钥和 Router 访问密钥可能是两把不同的钥匙。
- Router 状态卡同时显示安装状态、版本、管理服务和模型网关状态；未安装时显示“一键安装”，
  已安装且确认有新版时才显示“更新”。普通流程不显示安装源选择；官方 npm 源失败后的镜像重试由
  状态卡明确反馈。“启动”“卸载 CLI 路由”“停止后台”“打开完整管理页”在不可用或操作中必须
  禁用。桌面版只作为受保护的检测状态出现，不能提供启动、卸载或接管动作。
- Provider 为空导致 `No available models` 时，在状态卡下显示黄色“解决办法”卡：先说明
  缺少 Provider 及自动导入所需条件，再提供可用时的“一键修复”和始终可用的手动配置入口。
  Router 管理区始终保持可操作，不使用只适合某位用户当前配置的个性化判断文案；
  空 Provider 时禁用“启动 Router”，避免用户重复触发同一个错误。
- `better-sqlite3` 的 `NODE_MODULE_VERSION` 不匹配属于 Router 运行环境错误，而非
  Provider 错误。状态卡必须显示 ABI 原因和“不会改数据库/Provider/Codex”的边界，将主
  操作改为“修复运行环境并重启”；此状态下即使暂时读不到 Provider 也允许执行该修复。
- Provider 以紧凑卡片显示名称、协议、净化后的上游地址、模型列表、首选状态和“是否已保存
  密钥”；编辑表单支持新增/修改、保留旧密钥、设为首选及接入当前项目。删除需二次确认，
  一键 cURL 导入成功后立即清空包含密钥的输入与分析卡。
- 最终配置由服务商目录动态收敛字段：官方登录隐藏凭据与 API 基址；固定兼容服务预填基址、
  认证、主模型与小型/备用模型；本机转换器解释应填模型接口而非管理页。OpenAI 自动转换连接保存后
  表单与历史继续显示原始上游地址和模型，3456 只作为额外的“本地转换”信息展示。
- 真实测试用三行状态分别呈现“接口地址 / 身份认证 / 模型响应”，成功、警告、失败和跳过
  均使用图标、文字与颜色共同表达，并显示请求延迟。
- 测试失败后紧跟“原因 / 建议 / 操作”修复卡，操作必须直接可执行，例如切换认证、打开密钥
  控制台或文档、采用小型/备用模型、安装/启动 Router、重试和返回选择服务商；不能只给错误文本。
- 真实测试开始后，原结果卡切换为中性强调色的“后台正在测试连接”状态并通过
  `aria-live` / `aria-busy` 通知；临时禁用服务商卡片与当前配置控件，导航和 PowerShell
  不加遮罩、不设 `inert`。测试成功并继续保存、测试失败或抛出异常都必须经过同一 `finally`
  恢复进度标记、控件与按钮文案，不能从嵌套保存动作继承旧的 disabled 状态。
- 当前 Claude 项目首次载入及窗口从托盘隐藏态恢复时，自动用已保存配置执行一次真实测试；
  同一轮显示期间只执行一次，普通窗口聚焦、Alt+Tab 或状态重绘不能重复触发。底栏继续保留
  手动重测入口，自动与手动测试共用“正在检测连接”的忙态和最终健康色。
- 密钥输入永远为空；保存后只显示“已加密保存”的状态，不提供明文回显或复制入口。
- 历史配置每条的主体是可点击恢复按钮，最右侧是独立的 `×` 删除按钮，右键另开「重命名连接 /
  恢复 / 删除」菜单。重命名复用应用内模态框，明确说明只改变历史显示名、不修改连接参数。
  删除入口同时给两种是有意的：右键是熟练用户的路径，`×` 是能被看见的路径。没有记录时
  显示解释性空状态，不显示一个空列表。历史模态打开时，右键菜单必须进入同一 dialog 顶层；关闭后
  再恢复到页面层，不能让菜单被模态遮罩盖住或遗留在已关闭的顶层中。
- 历史条目显示保存当时的网关状态，但网关状态不参与「这条和上一条是否相同」的判断：
  网关反复启停不应该把同一份配置刷成一堵墙。
- 恢复一条记录后再保存不产生第二条记录：判重比对全部记录而非只比最新一条，命中的记录移到
  列表最前面并更新保存时间，名称与 ID 保持不变。留空的小型/备用模型按“与主模型相同”参与判重，
  自动补全不能让同一份配置看起来变了。
- 接入历史卡片固定为 176px 高；卡片正文独立纵向滚动，删除操作保持可见，外层列表继续只负责
  卡片之间的滚动。长 URL、模型名和标签不得撑高卡片或挤压相邻记录。
- 安全版本通过、待检查和被阻止分别使用绿色、琥珀色和红色提示条，同时带明确文字。
- 工具内按钮、状态、提示、错误和插件能力说明统一使用中文；PowerShell 原始输出不改写。
  产品名、命令、网址、模型名、插件 ID 与市场 ID 等必须准确匹配外部系统的技术标识可保留
  原文。插件卡只展示本地生成的中文能力说明，不提供英文原文折叠区；没有可靠中文说明时
  明确标注为概括，不伪装成插件作者提供的官方译文。
- 插件卡的“更新”和工具栏“更新全部”只在刷新后的目录明确标记 `updateAvailable` 时出现；
  启用/停用、卸载和安装属于独立操作，不受更新入口显隐影响。
- 终端底栏持续显示连接状态、资源环形进度、模型、速度、权限模式和思考程度，不要求用户打开
  工作台。详细快捷键信息不占用底栏。
- 原生对话可见时，模型、速度、模式、思考四个 chip 由这条底栏接管，改从 Agent SDK 快照的
  `capabilities` 读数，点击经 `updateNativeControls` 下发；终端侧的 chip 渲染在此期间整体抑制，
  否则后台会话的每次状态行心跳都会把四个读数覆盖回去，用户会看到两个真值来源互相闪烁。快照还没
  上报 `capabilities` 时四个 chip 显示 `—`、禁用并给出“尚未上报可用能力”的说明，不允许先摆一个
  猜测值。控制项提交期间四个 chip 一起 `aria-busy`，成功、失败、异常都在同一个 `finally` 里按
  最新快照重绘。
- 原生 Fast 分五种状态陈述，因为“已请求”和“已确认”是两个不同的断言：`unavailable`（当前模型没有
  声明支持）、`off`（点击可请求，并披露费用/额度可能更高）、`requested`（已向上游请求，未确认前
  绝不显示为已开启）、`confirmed`（adapter 收到结构化确认才用）、以及资格或额度被拒的“未生效”。
  只有 `unavailable` 才禁用按钮；其余四态都要保持可点，让用户能退回默认档。
- 底栏的连接、资源、模型、速度、模式和思考都是真按钮，不是装饰性文本。点击连接使用当前项目已保存的配置
  原地开始真实测试，不关闭工作台、不切换左栏页面；按钮切到 `data-tone="pending"` 的脉冲圆点、
  显示“正在检测连接”、设置 `aria-busy` 并禁用，测试结束再恢复成正常/需确认/异常。忙态分支
  必须排在健康色分支之前，否则一次陈旧的路由健康会把刚点下去的进度盖掉；忙态也不能用整包
  替换按钮文本的写法实现，那会连圆点一起删掉。
- 资源、模型、速度、模式与思考菜单共用 `.footer-menu` 浮层：`role="menu"`、固定定位、向上展开、
  两轴都做视口夹取，条目是「主标题 + 一句说明」的 `role="menuitem"` 按钮，当前项用
  `data-selected`/`aria-checked` 而不是只靠颜色。五个菜单登记进已有的 `pointerdown` 与 `window blur`
  收拢逻辑，不另起一套关闭机制。
- 资源菜单内的 Claude 上下文窗口是四档 `role="menuitemradio"`：自动（推荐）/ 100 万 / 20 万 /
  自定义。默认“自动”不注入任何窗口变量，官方订阅保持该档；选“自定义”只展开草稿输入，合法值
  保存成功后才改变 radio 的提交态。范围 8000–2000000，超出范围以中文错误 toast 拒绝而不是静默
  落库。菜单顶部把“已请求”与 statusLine / Agent SDK 的“实际采用”分开显示；配置目标不能冒充端点
  探测结果。托管 ChatGPT 的 272K / 1.05M 菜单与通用 Claude 菜单互斥，不能让两组 profile 叠加。
  状态行原始计数与窗口钳制值不一致时，环形进度置 `data-level="danger"`，明细给出两项具体数字，
  但不由这一信号推断端点容量或自动建议降档；只有真实上下文拒绝错误才可提示切回 20 万。
- 速度菜单默认“标准”，偏好按开发引擎、接入、认证、去凭据端点身份与模型隔离。官方 Anthropic
  的已确认 Opus 5 / Opus 4.8 可请求 Claude Code 原生 Fast；只有 statusLine 上报
  `fast_mode: true` 才显示“Claude Fast 已开启”，资格或额度拒绝时显示“未生效”。受管 GPT 仅显示
  “已请求 GPT 1.5x”，不能把发送 `service_tier=fast` 写成上游已采用；菜单同时披露更高费用/额度
  消耗。原生 Codex 显示禁用的“速度 Codex 内管理”。“小型/备用模型标识”会换模型，不得再叫成
  服务速度。
- 资源按钮的首要读数可持久选择“自动 / 上下文优先 / 额度优先”；菜单详情按能力显示当前上下文、
  自动压缩线、官方订阅窗口或供应商余额，缺失项明确写“不可用”，不能把本地网关统计伪装成
  ChatGPT 订阅额度。只有受管 ChatGPT 的 `gpt-5.6-sol` 显示窗口选择：标准档标明约 25.84 万有效、
  约 20.67 万压缩；扩展实验档标明约 99.75 万有效、约 79.8 万压缩，并在同一可见说明中披露
  后端可能仍按 27.2 万拒绝、API 长上下文计价提高、订阅消耗未知，以及更大容量不保证更稳定利用
  早期信息。选择只影响下次新建或重启的会话，toast 必须据实说明。
- 选择模型后，底栏模型按钮以 `disabled` + `aria-busy` 表达短暂提交态；成功、失败和异常
  都必须在同一个 `finally` 中直接恢复按钮，不能把恢复责任交给随后可能失败或延迟的状态刷新。
  模型列表的当前项取本会话已提交的目标模型，其次取 statusLine 实测模型，不能只标记项目
  默认配置。斜杠命令正文与回车必须分两次写入并按会话排队，避免 Claude Code 输入框留下
  一条看似已点击、实际未提交的 `/model`。
- 运行中切换速度必须先确认会重启终端，并用精确 conversation UUID 的 `--resume` 恢复同一对话；
  不执行 `/compact`。按钮在操作期间使用同一启动 generation 锁，取消确认、明确失败或真实生命周期
  事件负责释放，旧请求不能覆盖新状态。未运行时只保存下次启动使用的偏好。
- 权限模式一次性列全 Claude Code 的六种模式：手动确认、自动接受编辑、计划模式、自动选择、
  完全允许、仅预批准。当前项目没有预置「完全允许」时，该条禁用并在说明里写清要去工作台
  开启并重启；「仅预批准」在说明里写清会重启会话。不可用的模式保留在列表里并解释原因，
  不隐藏——用户需要知道这个模式存在以及为什么现在用不了。
- 模式徽标在 PowerShell 里由 Claude Code 自己重绘，底栏显示以它为准。`Shift+Tab` 在
  终端和输入框里都生效（输入框拦下默认的焦点遍历，转发同一段 CBT 序列）。Claude Code
  可能只输出变化的终端单元格，所以主动取样必须用输出修订号等 xterm 应用完此前屏幕差量，
  再扫描完整活动缓冲区并把已知模式回报主进程；底栏不根据点击做乐观假设。列表切换在第一
  次按键前和每次按键后都要完成一次取样：当前画面没有可读徽标（例如仍在选择器或确认框）
  时不发送按键，模式回到已访问项时停止，避免快捷键落入错误的交互上下文或绕环切歪。
- 思考程度一次性列全 Claude Code 能调的七项：跟随模型默认（`auto`）、最低、较低、均衡、
  更深、最深，以及 Claude Code 自身设置「最深 + 工作流」（`ultracode`）。按推理深度升序排列，
  每条说明都要写清代价，只作用于本次会话的（`max`、`ultracode`）必须在说明里点明，
  否则用户会以为自己改了持久设置。
- 思考程度全部走运行中的 `/effort`，任何档位都不重启会话——这一点和权限模式的「仅预批准」
  相反，不要给它套确认框。高档位选择还要在本会话临时 settings 中显式开启 thinking；
  这属于让所选能力真正生效的运行时准备，不能删掉高档位来规避兼容问题。
- 底栏思考读数以状态行上报的 `effort.level` 为准，也就是实际生效值；模型会把不支持的档位
  静默降级，`ultracode` 也只回报 `xhigh`，所以不能显示用户点了什么就算完。状态行还没刷新时
  显示本次请求值，重启会话后请求值作废。模型完全不带思考参数时状态行没有这个字段，此时
  按钮悬停提示和菜单内提示都要说明“未上报档位、可能不支持”，不允许伪造一个默认档。
- 只有终端明确出现 `output_config.effort` 为 `xhigh` / `max` 且
  `thinking is disabled` 的 400 时，才进入兼容恢复态：按钮暂时显示处理中，客户端自动提交
  `/effort high`，成功后以成功提示要求用户重试刚才的 WebSearch，并立即把读数显示为“均衡”。
  当前模型会话中的 `auto`、更深、最深和“最深 + 工作流”保留在菜单里但禁用，说明这是避免
  重复失败的临时上限；换模型或重启会话后清除上限。其他 API Error 不得触发这条回退。
- 最深两档最烧令牌，底栏按钮用强调色让这个选择保持可见，不能和均衡档看起来一样。
- 需要重启会话的操作（跨端点换模型、仅预批准）先用确认框说明三件事：会重启、对话历史通过
  `--continue` 恢复、终端画面会重绘。文案不回避重绘，那是用户会亲眼看到的。
- 底栏根据真实可用宽度与各组件自然宽度决定是否收纳，而不是按固定窗口断点猜测：全部组件能行内显示时
  “会话设置”按钮保持折叠且不可聚焦；发生真实挤压时按钮以短过渡进入，模型、速度、模式、思考的同一 DOM
  收进浮层；空间恢复后按钮平滑退出并关闭浮层。`ResizeObserver` 与内容观察只负责更新溢出属性，CSS 负责
  进入/退出动效。连接与资源始终保留，状态摘要可在受限宽度下收起。disclosure 暴露
  `aria-controls`/`aria-expanded`，区域具备名称与 group 语义，支持每次点击切换、方向键/Home/End 导航、
  焦点恢复、`Esc`、点击外部和窗口失焦关闭。跨过 medium 档直至最小 820px 宽度都不得丢失、错位或横向溢出。
- 终端右键菜单只包含复制、粘贴、全选和清屏，并保持键盘可达；有选区时 `Ctrl+C` 复制，
  无选区时保留 PowerShell 中断语义。纯 `Ctrl+V` 和菜单粘贴必须汇合到 generation-owned paste helper，
  对精确 `sessionId`、`ptyGeneration` 与 `TerminalView` 在剪贴板读取前后复检，并恰好调用一次
  `Terminal.paste(text)`；xterm 独占换行规范化与 bracketed-paste 编码，已有 `onData` 是唯一
  `terminal:write` 路径，禁止并行 direct write。纯 `Ctrl+V` 排除 repeat、Shift、Alt、AltGraph 与 Meta；
  剪贴板 5 MiB 上限加 12 个 bracket wrapper code units 仍作为一个 PTY write 接收，不切块。
  右键 `mousedown` 和 `contextmenu` 在终端容器 capture 阶段先于 xterm target handler 拦截；菜单捕获
  同一精确目标与单调 revision。点击自身 Paste 后隐藏菜单不撤销该 attempt，但另一个菜单替换 target、
  view dispose 或 PTY generation 变化会使异步读取结果失效；dispose 同时移除 capture listeners。
- “Claude 工作台”旁提供紧凑的终端主题选择器，列出 Claude 明亮、Telegram 明亮、石墨深色、
  深海蓝四套主题；窗口变窄时只收起“主题”标签，不遮挡工作台、信息和清屏按钮。启动恢复、
  工具栏切换和设置回填必须通过增强选择器的显式同步接口同时更新原生值与视觉名称，不能让
  首项“Claude 明亮”成为缓存标签。
- 项目名旁只在存在未完成后台任务时显示持续动效的“后台任务 N · 状态”按钮。详情面板展示任务、
  子代理数量、token 消耗可能性、是否会恢复主对话以及受控 Web 进程；token 只使用“可能/无/未知”，
  不伪造精确计量。减少动态模式取消循环动画，但保留明确的状态文字与焦点环。

## 官方网络预检 UI

- Claude 与 Codex 工作台共用“网络预检”入口；手动综合检查可覆盖 ChatGPT/Codex、Claude 与 Grok/xAI，
  自动登录/会话守卫只检查当前动作的精确 Provider 与配置目标。中转站、自定义网关和本地转换器不复用
  官方 Anthropic/OpenAI 结论，而是实测自身端点。
- 详情只保留一条从权威到建议的层级：1）Provider 连接结论；2）精确端点的 DNS、TLS、HTTP、重定向、
  内容替换、application session、CLI 与必需 WebSocket 证据；3）application/CLI 可见代理决策及 TUN
  caveat；4）目标限定的公网地址观察；5）DNS、IPv6、STUN、接口、环境、兼容性、信誉和建议操作。
- Provider endpoint evidence 是 access authority。`allowed` 与 `allowed_with_notice` 都使用绿色“连接正常”；
  TUN、虚拟接口、显式代理、分流提示、信誉源不可用或高风险建议证据不得把一个成功的精确 Provider
  连接改成黄/红或阻止动作。反之，精确 Provider 的必需能力失败即使建议证据看似低风险也保持阻止。
- `providerConnectivity` 与 `advisoryEvidence` 不混为综合风险分。建议采集失败表示该建议未知，不表示
  Provider 失败；界面不再使用“综合风险”“模型出口”“Claude 出口”或其他把泛用目的地冒充 Provider
  路径的标签。
- 公网地址观察逐项显示 process/session、transport、address family、精确 observation endpoint、
  collection time、freshness、confidence 与 source agreement，并明确“不证明 Anthropic/OpenAI 端点的
  public source address”。完整地址不进入 renderer 或诊断历史，IPv4 与 IPv6 独立呈现；不可用观察不伪造
  address family。DNS、IPv6/STUN、接口、时区、语言和信誉 check 也携带 advisory authority、process、scope、
  target、transport、checked time、freshness 与 confidence；cache/history 投影必须明确标为“缓存”。
- 路径行只能陈述当前 target、process 与 network scope 的可见第一跳。`resolveProxy(url) === DIRECT`
  只表示 Electron 没有为该 URL 返回显式 HTTP/SOCKS/PAC 代理；它不证明物理公网直连，也不排除 TUN、
  透明代理、soft router 或按目标分流。CLI 环境变量同样不能证明端到端拓扑。
- 应用探测绑定实际 Electron Session，使用无凭据请求并限制正文；CLI 使用实际 transport。401/403/405
  等 Provider 典型状态表示端点已响应，不表示认证成功。只接受受信任 redirect，TLS、非白名单跳转、
  captive portal/content substitution 和必需 WebSocket 失败按精确动作处理。
- 检测操作在第一个 `await` 前建立 owner，立即把按钮设为 `disabled`/`aria-busy` 并通过 `aria-live` 显示
  “正在进行网络预检…”。已经打开的 dialog 也立即切到 pending 并保留当前 target/action；无关重绘和
  stale completion/`finally` 不能覆盖结果或恢复较新的 owner。减少动态模式只取消循环动画，不删除状态文字。
- 新建或重启 Claude 会话遇到阻止结论时提供“我再看看 / 重新检查 / 坚持连接”。“坚持连接”只授权被
  精确捕获的 session、Provider、target、configuration revision 与 launch generation，不能跨项目或复用；
  启动后的后台复查只更新健康呈现，不主动中断会话。
- 首次冷启动若只有 DNS、连接重置或超时等瞬态 Provider 失败，可在阻断前等待 150ms 并强制重测一次。
  离线、TLS、非白名单跳转、门户网络、CLI transport 不支持和内部预检异常不自动重试；取消后不再发请求。
- 时区覆盖只作用于之后由 ClaudeDock 启动的 CLI，不修改 Windows 时区、DNS、代理、路由或网卡；Windows
  系统语言始终是只读参考。

## Codex 工作台

- 项目页的“新建项目开发引擎”是应用级下一次新建偏好，由两张等宽单选卡组成：Claude Code 强调原生体验与现有路由，Codex 明确
  写“官方 ChatGPT 订阅登录”。整张卡都是命中区，透明 radio 覆盖完整卡片并保留
  `focus-visible` 外框；选择不能只靠颜色表达。它不属于当前项目，也不切换任何已打开会话；每个新 session
  在创建瞬间捕获当时选择，之后的选择只影响下一次新建。
- 选择 Codex 后，右上角和抽屉标题统一改为“Codex 工作台”，隐藏 Claude 专属三页签。页面
  只有一条主叙事：“官方订阅通道”说明卡、三步就绪列表、账号/额度卡、次操作和会话入口，
  不复制 Claude 的路由、命令或权限页面。
- 顶部主按钮只在 Codex 尚未安装或登录时作为“安装、登录并启动 / 使用 ChatGPT 登录并启动”的修复入口；
  Codex 就绪后该按钮隐藏，新对话统一由项目 `+` 创建。安装与登录仍各有独立次入口，失败后可以只
  重试当前步骤；设备码会以等宽高对比文本显示并提供复制按钮。
- 三步状态固定为“Codex CLI / ChatGPT 账号 / 当前项目”。每步同时使用编号、状态色和文字；
  就绪为绿色、等待为中性、失败为红色。安装版本、套餐和额度只展示官方结构化状态，不从
  终端画面猜测。
- 英雄卡使用当前主题强调色的低强度径向底纹，彩色仍只服务于“官方通道 / 主操作”语义。
  主按钮不得高到挤压步骤列表；信息卡按内容自然增高，页面独立纵向滚动。
- 页尾常驻边界说明：默认工作区写入沙箱、提权仍确认、OAuth 令牌由 Codex 管理。不能使用
  “ChatGPT 已接入 Claude Code”或“官方 Claude 模型”等误导性文案。

## 动效与可访问性

- 动效时长、曲线、popup 起始位移与按压尺度只取自本文件“四主题动效与圆角人格”表及其语义令牌；
  状态灯、拖放、按钮和消息气泡不得另写业务字面时长。
- 悬停只使用描边、表面层与微光，不上浮控件或文字；按压反馈不得改变按钮占位尺寸。
- 界面切换一律有过渡，不做硬切：rail 业务页、设置弹窗分类页、终端 ⇄ 聊天三处都用
  `var(--dur-3)` + `var(--ease-decel)` 的进入动画，侧边栏折叠给 `.workspace` 的
  `grid-template-columns` 加 `var(--dur-2)` 过渡（内容淡出而栏宽瞬间归零，等于没有过渡）。
  拖动分栏条时该过渡必须关闭，否则每帧改写同一属性会橡皮筋。
- **这三处的进入动画只允许改 opacity，不允许 `transform`。** 它们动的是当前可交互的那一层，
  位移会把里面每个控件的命中区连带挪走，动画期间点击会落空。仓库里唯一带位移的
  `.workbench-page` 之所以成立，是因为它只位移已经 `pointer-events: none` 的那份旧页面——
  那种写法要求新旧两页同时在布局里，而 `hidden` 切换恰恰不满足。
- 循环等待动画只旋转专用的伪元素圆环，绝不旋转承载图标或文字的元素本身。标题栏刷新、下载中心
  入口和下载进度环统一使用「透明描边 + 主题色顶边」的 `::before` / `::after` 环，图标和数字
  徽章保持静止；圆环颜色取自 `--accent-solid` / `--accent-tint`，因此四套主题自动跟随。
- 终端聚焦描边始终保留，微光的过渡遵守 `prefers-reduced-motion`；焦点位置不能只靠动画表达。
- 独立对话输入框的主题色描边始终保留；减少动态效果时只把焦点入场压缩到近乎瞬时，不能移除
  静态焦点边界。
- 所有按钮提供可见焦点、`aria-label` 或可读文本；关键文字对比度目标不低于 4.5:1。
- 工作台页签、表单标签、上下文 `progressbar` 和展开按钮维护明确的 ARIA 名称、值与
  `aria-expanded` / `aria-hidden` 状态。
- 服务商分组收起后，其卡片必须退出键盘导航；全局设置使用原生模态 `<dialog>`，标题、
  说明、关闭按钮及“完成/取消”都具有明确名称，`Esc` 与“取消”保持同一语义。
- 自动检测区和 cURL 输入使用具名 `region` / `label`；动态诊断与测试结果通过
  `aria-live` 或现有状态提示通知，但不反复播报 6 秒轮询中未变化的内容。
- 终端 start/stop/restart、Claude preflight/launch、runtime switching、Codex install/update/login/
  cancel/logout/launch 与 plugin refresh/update 的 busy 文案、disabled、`aria-busy` 和 live status 都属于
  精确 operation token。runtime switching 由 main 按规范化项目目录持有，同目录 session 与 reload 后的
  renderer 共用 pending attempt。Codex 安装与账号动作由 main 按应用全局持有，并用单调 revision 与精确
  operation kind 恢复原文案。插件 mutation/refresh 也由 main 应用级单例持有：相同逻辑请求加入当前 Promise，
  竞争请求不排队；catalog snapshot 恢复 attempt/kind/target/phase，完整 mutation surface（安装、更新、启停、
  卸载、市场增删和来源输入）同步锁定。目录、workspace、catalog 或状态快照重绘只能读取当前 owner；只有
  该 owner 的 settlement 可以恢复控件，stale result/`finally` 不得清除较新操作。
- 标题栏刷新图标有稳定的中文 `aria-label`；检查期间使用 `aria-busy`，发现更新后标签同时
  包含可更新项数量，不能只用琥珀状态点表达。
- Router 管理区使用具名 `region`，Provider 表单全部使用显式标签；运行中的安装、启动、
  停止和保存操作通过按钮禁用与 toast 文本共同反馈，不能只依赖颜色。
- 下载中心、代理、路由和 MCP 的状态都同时使用徽章文字与颜色；进度条提供 `aria-valuenow`，
  未知总量时使用 `aria-busy`。退出、代理体检、MCP diff/还原对话框打开后焦点落在安全主操作，
  `Esc` 关闭并把焦点还给触发控件。
- 所有新增面板复用主题拥有的 `--dur-micro` / `--dur-enter` / `--dur-exit-theme` 与
  `--ease-enter` / `--ease-exit` / `--ease-spring`；不得为某个新弹窗另写毫秒值或曲线。
  四主题视觉冒烟覆盖 MCP、代理和路由页面；减少动态效果时取消位移和循环动画，但保留可见焦点环。
- 渲染入口依赖的元素必须使用标准直引号 HTML 属性且保持 ID 唯一；智能提示区域必须留在
  接入页容器内，避免内容逃逸到终端或控制栏。
- 拖放不是唯一入口，虚线加号区域本身是键盘可达的按钮。
- 项目行使用可聚焦按钮切换，关闭按钮包含项目名的可读标签；关闭运行中的项目需二次确认。

## 素材维护

- 应用图标矢量源位于 `assets/source/`；`npm run generate:icons` 生成 `assets/generated/` 中的 PNG 和 ICO。
  标题栏 Logo 由 Vite 从生成图标导入，开发和打包环境使用同一资源源。不要直接手改生成文件。
- runtime 品牌标记位于 `src/renderer/assets/brands/`：Anthropic Clay Spark、OpenAI black Blossom 和
  OpenAI white Blossom。它们是带官方来源 URL、archive entry、检索日期与 SHA-256 的独立审计资产，
  不经过应用图标生成器，也不增加 icon dependency。
- runtime SVG 只允许官方 geometry/fill 和 provenance comment；禁止 `<image>`、`<script>`、event handler、
  `href`、CSS filter 或 `currentColor` 重着色。Vite 必须输出可检查的本地文件，不能折叠为 opaque data URL。
- 资产变更同时更新 source hash、LF-normalized body hash、declarative contract 和 light/dark visual proof；
  品牌表面变量是固定品牌常量，不由当前 ClaudeDock 主题覆写。
