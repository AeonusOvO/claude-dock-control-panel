# ClaudeDock 5.0.0-rc.10

发布日期：2026-08-09

## 变更

- 在仓库根目录新增 `technical-screening-checklist.md`，集中整理运行时安全门禁、更新与下载信任链、签名和证书校验、CI/发布/部署门禁。
- 汇总第一方运行时提示词、Agent 指令、可复制提示词模板、提示词行为说明和对应测试证据。
- 明确区分生产执行阻断、安全回退、发布门禁、测试证据、文档指令和历史材料，避免把普通 UI 状态或历史 diff 误判为当前安全实现。

## 安全边界

- 本轮不改变任何运行时代码、网络请求、凭据存储、权限策略、更新信任或发布配置。
- 清单记录 `claude-web-search-guard.ps1` 对 malformed Hook payload 的兼容性 fail-open 例外，供最终技术评审单独确认。
- 静态清单不能替代动态测试、Authenticode 验证、安装/卸载烟测或双渠道发布审计。

## 回归覆盖

- 运行仓库规定的 lint、格式、类型、测试、布局、主题和构建检查。
- 重新生成 Windows x64 覆盖安装包及 `latest.yml`、blockmap 和 `win-unpacked/`。
