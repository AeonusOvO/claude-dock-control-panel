# Replace @DEPLOY_USER@ during provisioning; never publish the live account name.
@DEPLOY_USER@ ALL=(root) NOPASSWD: /usr/local/sbin/claudedock-promote *
@DEPLOY_USER@ ALL=(root) NOPASSWD: /usr/local/sbin/claudedock-rollback *
