# ClaudeDock 设计规范

## 设计令牌系统（2026-07-29 更新）

所有样式使用 CSS 自定义属性管理，禁止硬编码颜色、间距、圆角或时长。

`:root` 是唯一允许出现字面值的地方；文件其余部分只能引用令牌。`tests/design-tokens.test.ts`
把这一点变成可检查的不变式：`:root` 之外不允许 `#rrggbb`、不允许带色相的 `rgb()`/`rgba()`
（只放行 `0 0 0` 与 `255 255 255` 的中性透明度）、`font-family` 只能是
`var(--font-ui)` / `var(--font-display)` / `var(--font-mono)` / `inherit`、不允许写死
`font-size: <n>px`。
这条不变式同时是「全局主题真的生效」的保证——主题只需覆写令牌，不需要逐条改样式。
批量替换的工具与感知色差（CIE76）报告见 `scripts/tokenize-colors.cjs`。

### 字体族

- **界面正文**：`--font-ui` 随主题人格变化。Claude 使用本地 `Inter Variable`，Telegram
  使用本地 `Open Sans Variable`；石墨 / 深海蓝以 `Segoe UI Variable` 为首选。中文依次
  回退 `Microsoft YaHei UI`、`Segoe UI`。
- **品牌标题**：`--font-display` 是第三种职责明确的字体槽。Claude 使用本地
  `Source Serif 4 Variable` 表达温和编辑感；Telegram 与两套工程主题让它等于各自正文
  字体。仅消息标题、空状态标题和 Artifact 抽屉标题可使用，不得把长正文改成衬线。
- **等宽**：`--font-mono`（`Cascadia Mono` → `Consolas`）
- 三个字体槽均由本地包或系统字体提供，离线时不访问第三方字体站点。其他字体写法视为回归；
  图标按钮继承上下文字体（`inherit`），不单独指定字族。

### 颜色令牌

- **表面阶梯**：`--surface-canvas` (body 背景) → `--surface-terminal` (终端背景) → `--surface-1` (chrome) → `--surface-2` (面板) → `--surface-3` (卡片) → `--surface-4` (嵌套卡片) → `--surface-inset` (凹陷)
- **文字层级**：`--text-hi` (标题) → `--text` (正文) → `--text-lo` (次级/必读辅助) → `--text-dim` (装饰性) → `--text-mute` (禁用)
- **发丝描边**：`--line-subtle` (6%) / `--line` (9%) / `--line-strong` (13%) / `--line-hover` (20%)
- **主色**：`--accent-solid` (实心填充) / `--accent-text` (文字图标)；Claude 明亮使用陶土色，
  Telegram 明亮使用克制蓝色，深色主题沿用青蓝。
- **状态三色**：`--ok-*` (绿) / `--warn-*` (琥珀) / `--bad-*` (红)，每色包含 solid/text/tint/line 四槽
- **代码语义色**：Shiki 只负责识别 token 的色相类别，最终颜色映射到
  `--syntax-red/blue/cyan/green/magenta/yellow/neutral`。因此切主题不需要重建已显示代码，
  语法色会随 xterm palette 即时重绘。
- **主题作用域是全局的**：字体、表面阶梯、文字层级、交互层、发丝描边、主色、状态色、
  阴影、高光、主题排版、圆角、按压、进退场曲线和终端遮罩由
  `src/shared/terminal-themes.ts` 的 `shell` 字段驱动，映射表是同文件的 `SHELL_CSS_VARIABLES`。
  切换主题时渲染层遍历该映射写到 `documentElement.style`，主进程同步
  `setBackgroundColor` / `setTitleBarOverlay`（原生窗口边框由 Windows 绘制，CSS 管不到），
  所选主题存进 `WorkspaceStore`，冷启动第一帧即为正确配色。绿/琥珀/红的语义不变，但实际
  明度随外观调整，保证浅底和深底都达到正文对比度要求。
- **跟随主题的半透明色**：不写 `rgba(r g b / n%)`，改用
  `color-mix(in srgb, var(--token) n%, transparent)`，这样色相跟着令牌走。纯黑投影
  （`rgb(0 0 0 / n%)`）与纯白提亮（`rgb(255 255 255 / n%)`）与主题无关，保留字面值。

### 间距与圆角

- **间距阶梯**：`--s-1` (4px) → `--s-2` (8px) → `--s-3` (12px) → `--s-4` (16px) → `--s-5` (20px) → `--s-6` (24px) → `--s-8` (32px) → `--s-10` (40px)
- **圆角**：`--r-xs` (4px) → `--r-sm` (6px) → `--r-md` (8px) → `--r-lg` (10px) → `--r-xl` (14px) → `--r-2xl` (18px) → `--r-pill` (999px)
- **节奏原则**：组内 8px、组间 16px、区间 24-40px

### 字号阶梯

- **微标 10px**：`--text-3xs` (仅用于极窄空间的图标附标)
- **最小 11px**：`--text-2xs` (mono 路径、微徽章)
- **次级 12px**：`--text-xs` (帮助文字、术语表)
- **默认 13px**：`--text-sm` (按钮、输入、列表)
- **标题 14px**：`--text-base` (卡片标题)
- **面板 16px**：`--text-md`
- **大数值 20px**：`--text-lg`
- **展示 24px**：`--text-display` (引导图标等大字形)
- **浮层 28px**：`--text-xl`
- **眉标 11px**：`--text-eyebrow` (大写、字距 0.08em)
- 窄屏媒体查询里的 `font-size: 0` 是把按钮收成纯图标的手段，不是阶梯上的一档。

### 动效令牌

- **曲线**：`--ease-standard` (微交互) / `--ease-decel` (进场) / `--ease-accel` (退场) /
  `--ease-telegram`（通用快速滑入）；人格覆盖层使用 `--ease-enter` / `--ease-exit` /
  `--ease-spring`。
- **时长**：`--dur-1` (90ms 必须配 standard) / `--dur-2` (140ms) / `--dur-3` (200ms) / `--dur-4` (280ms 抽屉进) / `--dur-exit` (180ms 抽屉出)
- **主题节奏**：Claude 消息以 240ms 舒缓减速进入，Telegram 以 340ms 的
  `easeOutCirc` 人格进入，两套深色主题为 200ms 中性节奏；按压缩放走
  `--press-theme`。抽屉和按钮使用可中断 `transition`，一次性消息入场可以使用 keyframes。
- **刷新反馈**：`--dur-refresh` (900ms) 仅用于标题栏刷新图标的匀速旋转；减少动态效果时
  由全局规则收敛为一次近乎静止的反馈。
- **规则**：90ms 级微交互禁用 emphasized 曲线（会感知为迟滞）
- **交互页切换**：包含按钮、输入框和下拉框的左栏页面只允许透明度进场，不对整页容器做
  `transform`；否则 Chromium 合成层切换期间可能短暂保留过期的命中测试区域。
- **悬停不位移**：按钮、卡片、服务商、会话与安装入口的 `:hover` 禁止 `translateY`。
  悬停只改变交互层、描边和微光，避免 Chromium 重新采样文字造成闪烁；按下时允许轻微缩放，
  消息气泡进场允许一次性的位移/缩放。

### 层级表达

- 深色层级通过**表面提亮 + 发丝描边**；明亮主题使用纸面/画布的结构差、低透明交互层和
  克制的两段式阴影，不是把深色主题的十六进制值简单反转。
- `--sheen` 由主题提供：深色是内侧高光，明亮是低强度纸面投影。仅用于卡片层，嵌套卡片
  不重复叠加，避免层级发灰。
- 遮罩层只模糊一次性 canvas 快照，不对持续刷新的真实终端使用 `backdrop-filter`

### 单一强调色原则

- 彩色预算：青蓝（识别色）+ 绿/琥珀/红（语义状态色）
- 禁止装饰性渐变、光斑、多余彩色
- 所有彩色使用必须有真实语义（状态点、胶囊、健康卡、测试阶段图标等）

## 设计目标

界面支持四套完整人格：Claude 温和编辑感、Telegram 紧凑通讯感，以及石墨/深海蓝的中性工程
气质。差异覆盖字体、行高、圆角、消息结构和动效，不能只换色。终端或独立对话始终是视觉
主体，左侧控制区呈现配置、项目与历史；Claude 工作台从终端右侧按需滑入。避免拟物化、
过度渐变和大面积高饱和色。

## 视觉语言

- 背景：由所选主题决定的分层表面。深色终端是最暗层；明亮终端使用接近白色的阅读面，
  外围画布略深，卡片再抬升，形成真正的亮色层级。
- 终端不允许出现一圈黑边：`@xterm/xterm/css/xterm.css` 把 `.xterm .xterm-viewport` 写死成
  不透明黑，必须覆写为 `var(--surface-terminal)`，同时让每套主题的调色板底色与它的终端
  表面一致，否则画布内外会差出一个色阶，看上去就是一个黑框。
- 活动终端内的 xterm 隐藏输入区获得焦点时，外层用 `--accent-line` 细描边和
  `--accent-tint` 微光反馈“原始 PowerShell 已接管输入”。效果只响应 `:focus-within`，不改变
  布局、不持续呼吸，也不能压过错误/警告状态。
- 主色和状态色都由主题提供，但“正常 / 警告 / 错误”的色相与含义保持不变。
- 字体：正文使用 `--font-ui`，品牌标题使用 `--font-display`，终端与路径、命令等技术标识
  使用 `--font-mono`。Claude / Telegram 的正文、标题和行高必须在并排截图中明显不同。
- 圆角：卡片 14px (`--r-xl`)，嵌套卡片 10px (`--r-lg`)，按钮 8px (`--r-md`)，状态胶囊为全圆角 (`--r-pill`)。
- 阴影：只用于窗口内浮层与拖放激活态，避免装饰性重阴影。
- 图标：使用一致的 1.8px 线性 SVG，禁止字形充当图标；应用图标以终端提示符和状态灯为核心。

## 布局与状态

- 最小窗口：820 × 640；建议窗口：1180 × 760。
- 顶部 48px (`--titlebar-h`) 为可拖拽标题栏，交互控件必须标记为不可拖拽。
- 标题栏刷新图标紧邻 Windows 原生窗口按钮左侧，是 Claude Code、Router 与插件的统一更新
  检查入口；检查中旋转并设置 `aria-busy`，发现更新后出现琥珀状态点。它只表示“可重新检查/
  已发现更新”，不直接执行更新。
- 左侧控制栏默认 320px (`--rail-w`)，通过 5px 分隔条在 270–560px 之间调整并持久化；
  终端占据剩余空间，键盘也可聚焦分隔条后用方向键调整。拖拽必须在 `pointerup`、
  `pointercancel`、`lostpointercapture`、窗口失焦或进入后台时统一结束并释放捕获；从托盘恢复
  时再次清理残留状态，不能让一个中断的拖拽继续拦截整个窗口的点击。再次点击当前活动栏按钮
  时，控制栏和分隔条收起；项目、对话、接入、插件与底部设置入口始终可见。收起内容必须设为
  `inert` / `aria-hidden`，终端随布局变化重新 `fit()`，点击任一业务入口即可展开对应页面。
- Claude 工作台默认 468px (`--drawer-w`) 右侧抽屉，可通过左缘分隔条在 360–760px
  之间调整并持久化；顶部固定“会话 / 命令 / 快捷键”三页签，内容区独立滚动。
  抽屉打开时以半透明遮罩压低终端，不销毁或重排 xterm.js 实例。
- 在 820–900px 窄窗口下，控制栏最小 240px，终端工具栏改为两列，工作台允许
  覆盖除 80px 外的终端区域；插件工具栏改为纵向，三个插件页签始终等分可用宽度，按钮文字
  自然换行。插件卡的长名称、仓库地址、操作按钮和市场添加表单必须同时设置可收缩边界并
  自然换行，不得以内容最小宽度撑破侧栏。插件页不得出现横向滚动，底栏隐藏模型、权限模式和
  后台状态等次要摘要。
  控件只能自然换行、收起次要文本或滚动，不允许通过负边距、绝对定位挤压造成重叠。
- 终端工具栏只有「标题 + 操作」两列，没有装饰性窗口灯之类的假 chrome。真实终端已经在
  应用的窗口里，再画一组红黄绿点只是在模仿另一个软件的外观。
- 在高度 640–700px 的窗口中收紧卡片和控制栏纵向间距，全部长内容继续由已有滚动容器承接。
- 左侧项目列表使用较大的文件夹标题，并按文件夹、运行中对话、历史对话形成可折叠层级；
  列表溢出时内部滚动。折叠开关只作用于历史对话区：运行中对话始终可见，因此正在使用的
  项目也能收起——收起后只保留文件夹标题和进行中的对话行，不允许出现“点击收起没有任何
  反应”的死开关。
- 运行中对话创建时允许先显示“对话 N”临时名称；Claude Code 的自动标题可用后，原位置以
  “擦除旧名 → 打字机逐字输入新名”的动画替换为主题标题，动画期间显示闪烁光标，并同步
  终端工具栏与工作台范围，不新增提示条、不改变列表层级。`prefers-reduced-motion` 下直接
  替换文本。用户手动重命名立即生效、不播放动画，且旧的状态行刷新不得把名称改回去。
- 项目添加只保留一个虚线加号区域，同时支持点击选择和拖放，不再额外放置重复按钮。
- 目录选择器取消时不显示错误；原生对话框启动失败与选择完成后的项目打开失败必须使用不同
  文案，并保留系统返回的可读错误原因，不能统一吞成模糊提示。
- 会话状态分为：启动中、运行中、已停止、错误；文本和颜色同时表达，不只依赖颜色。
- 没有任何对话时（冷启动，或关掉最后一个对话之后）终端区显示引导态，而不是伪造一个会话。
  引导文案随「是否已有记住的项目」二选一：有就引导去点最近项目，没有就引导去添加文件夹。
  标题栏、状态胶囊、底栏和工作台范围同步显示“未打开项目”，输入框禁用。
  不允许为了避免空状态而在用户目录里起一个 PowerShell——那会造出一个用户从没打开过、
  以 Windows 用户名命名的假项目。
- 项目切换只替换当前可见终端，后台终端继续运行并保留各自滚动缓冲。
- “对话”切换右侧主工作区为 `.chat-shell`，不销毁后台 xterm；离开“对话”后终端在同一
  网格位置恢复。再次点击已选“对话”只收起配置侧栏，聊天主界面仍保留，避免误切回项目终端。
- 窗口关闭时隐藏到托盘；托盘图标按全部会话的聚合状态显示，并提供项目切换菜单。
- 全局滚动条使用统一的透明轨道、低对比度圆角滑块与青色悬停反馈；xterm、控制栏、抽屉和
  项目列表遵循同一规则。
- NSIS 安装器在目录选择后显示”安装选项”页，桌面快捷方式复选框默认勾选。
- NSIS 安装器只提供简体中文界面，与应用内中文文案保持一致。

## 终端输入框（对话式输入）

终端区自上而下是 `工具栏 / 输出区 / 输入框 / 底栏`
（`.terminal-shell` 的 `grid-template-rows: var(--toolbar-h) minmax(0, 1fr) auto var(--footer-h)`）。
输入提示词的主入口是输入框，不是输出区。

- 输入框是真正的 `<textarea>`。这是选它的全部理由：`Ctrl+A` 全选、`Shift+←/→` 逐字选择、
  鼠标拖选、`Ctrl+Z` 撤销、中文 IME 组合全部是浏览器原生行为，不由按键处理器在终端画布里
  模拟编辑器。**不要为这些行为写代码**——写了就会与原生行为冲突并重新引入终端的弊端。
- 只有三件事需要实现：`Enter` 发送 / `Shift+Enter` 换行、`↑/↓` 翻本地历史、随内容自动增高
  （上限 `--composer-max`，168px）。历史只在光标位于首/末且无选区时才翻，否则方向键属于
  文本编辑；`event.isComposing` 或 `keyCode === 229` 期间一律不拦截，避免打断 IME 候选。
- 实测高度写回 `--composer-h`，`.workbench-scrim` 与 `.claude-workbench` 的 `bottom` 由
  `calc(var(--footer-h) + var(--composer-h))` 得出，抽屉永远不会盖住输入框。
  `npm run test:layout` 断言在 820×640 下输入框不被底栏或抽屉覆盖。
- 多行提示词必须作为**一条**命令进入 PowerShell：行间用 `\x0a`（PSReadLine 的
  `Ctrl+j`/AddLine 绑定所插入的字符）连接，末尾的回车单独写一次。构造逻辑在
  `src/shared/composer-input.ts`，与 `terminal-session.ts` 的键位绑定成对存在，改一处必须改另一处。
- 会话未在运行时输入框禁用并换 placeholder；启动会话、切项目等操作之后记录“待聚焦”意图，
  只有同一个活动会话真正进入 `running` 后才把焦点给输入框，不使用固定毫秒延时。
  例外：`resume` 模式打开的是 Claude 自己的方向键选择器，需要原始按键，焦点留在输出区。
- 所有删除、清空、卸载和重启确认都使用带标题、说明、取消与确认按钮的应用内 `<dialog>`；
  禁止使用 `window.alert` / `window.confirm`。关闭对话框后优先恢复打开前的控件焦点；窗口
  重新获得焦点或从后台变为可见时，必须同步最新工作区状态后再执行输入框待聚焦逻辑，避免
  Windows 原生 JavaScript 对话框的焦点恢复缺陷同时影响 xterm 隐藏 IME textarea 和主输入框。
- 输出区仍然可以点进去直接打字（Claude Code 的 TUI 需要原始按键）。输出区内 `Ctrl+A`
  的含义是「全选输出」而不是 PSReadLine 的「移到行首」。
- 提示文字是输入框自己的 placeholder，开始输入即消失，不再占一行独立小字；「快捷键」页
  保持同一说法，且不得再出现与输入框冲突的旧条目（例如把 `Ctrl+A` 说成「移动到行首」）。
  屏幕阅读器另有一个 `sr-only` 的 `aria-describedby` 文本，因为 placeholder 在有值时会消失。
- 输入框与发送按钮是 `minmax(0, 1fr) auto` 两列：按钮 `align-self: stretch`，与输入框等高，
  不是浮在角落里的小方块。输入框最小高度 52px，仍按内容增高到 `--composer-max`。
- 发送时把当前提示词复制成一个固定定位的气泡，从输入框位置上浮淡出（`--ease-accel`，420ms）
  后自行移除。它只是视觉反馈：文字照常进入 PowerShell，动画不参与任何发送逻辑，失败也
  不会影响发送。`prefers-reduced-motion: reduce` 时直接不显示气泡。

## 独立对话

- “对话”位于活动栏最上方；“项目 / 接入”相邻组成项目工作流。它是与 Claude Code 项目终端
  并列的主工作区，左侧按“独立接入 / 对话历史”排列，右侧按
  `工具栏 / 消息滚动区 / 输入区` 三行布局。接入说明必须明确“不复用、不修改项目接入”。
- 配置表单依次呈现协议、接口地址、模型、认证方式和凭据；已保存密钥只显示状态，密码框永远
  为空。Anthropic 与 OpenAI 兼容协议用显式选择，不根据 URL 暗猜。“测试连接”与“保存配置”
  并列，使用当前未保存草稿发出 1-token 最小请求，结果明确显示成功/失败、延迟和可用 usage。
- 工具栏持续显示当前模型、“实时上下文”和输入/输出 Token。输入框草稿立即计入估算值；
  流式过程中逐段刷新；供应商返回 usage 后显示精确值。估算值必须带“约”，不能伪装成精确计费。
- 用户消息靠右并使用强调色浅底，模型消息靠左使用普通卡片底；标签同时显示“你 / 模型”，
  不仅依赖左右位置区分角色。Claude 的模型标题使用衬线且卡片宽松；Telegram 把角色标签
  变成紧凑圆形头像并使用更小气泡圆角。结构差异只用 `data-theme`，颜色仍只走 token。
- 模型正文按正常文档流排版 `h1-h6 / p / ul / ol / blockquote / table / pre / code / a /
img`；表格横向滚动，代码块显示语言与复制按钮，公式居中且保留 MathML。流式尾部每帧重建，
  已稳定前缀不闪烁，未闭合围栏保持不稳定直到闭合。
- HTML 代码块下方可出现“运行此可视化”，但不得自动执行。运行区域有固定工具栏、加载/
  错误/停止状态与受限高度 iframe；“详细信息”从右侧可中断滑入，含联网总开关、运行实例和
  请求审计，打开时消息区和输入区设为 `inert`，`Esc` 与遮罩点击都可关闭。
- 输入区的回形针位于 textarea 左侧；待发送附件在上方横向卡片队列显示缩略图、文件名、
  大小和移除按钮。已发送图片显示主进程生成的小预览，PDF/CSV/文本显示文件卡，缺失附件要
  明确标成不可用而不是留下空白。
- 输入区是真正的多行 `<textarea>`，`Enter` 发送、`Shift+Enter` 换行，IME 组合期间不拦截。
  生成时禁用重复发送并显示独立“停止”按钮；工具栏“新对话”开启空白记录，不删除旧历史，
  也不影响项目或 Claude Code session。
- 每次发送与生成结束后原子写入本机历史；左栏按最近更新排序，显示标题、时间、消息数和
  Token。最多 50 个对话、每个 100 条消息。删除按钮具有包含标题的 `aria-label`，并复用
  应用内“永久删除、无法恢复”确认框；历史正文是明文本地数据，界面与文档必须披露。
- 空状态居中解释独立边界；错误直接写入对应模型消息并用 toast 补充，已收到的部分文本保留。
  Markdown 只从 lexer token 创建白名单 DOM，原始 HTML 降级为文本；Artifact 是唯一显式的
  HTML 执行入口。远程 Markdown 图片显示带隐私说明的占位卡，不生成会自动请求的 `<img>`；
  用户可用卡片按钮显式交给系统浏览器打开。`data:` 图片仍可在消息内显示。

## 全局设置

- 设置入口固定在活动栏底部，与业务页分组。依据 Microsoft 的 Windows 应用设置建议，设置应
  统一入口、放在导航末尾、把相关内容分组，并在修改后立即生效；首期因此只建立有真实内容的
  “总设置 / 接入”，不创建空的通知、隐私或高级页。
- 全局设置使用主题化原生 `<dialog>`：固定标题与底部操作区，左侧分类导航，右侧内容独立滚动。
  680px 以下分类导航改为顶部横排；820×640 最小窗口内所有控件仍可聚焦和滚动到。
- “总设置”采用单列设置卡：左侧“名称 + 说明”，右侧放开关、选择器或只读值。首期包含开机
  启动、主题、语言和当前版本；只有简体中文可用时，语言选择器禁用并解释原因，不伪造选项。
- “接入”完整承接原高级设置的唯一 DOM 节点，包括认证来源、自动发现、Router、cURL 与诊断。
  原“接入”业务页不再保留第二个高级入口。取消仍恢复未保存草稿，已经执行的安装、启停和
  Provider 保存仍立即生效。
- 主题选择与终端工具栏是同一事实来源，任一入口修改都同步另一个选择器、CSS shell tokens 和
  Windows 原生标题栏。开机启动失败时恢复开关并显示错误。
- 设置分类调研的后续候选为“对话与模型、终端与项目、通知、隐私与数据、辅助功能与外观、
  高级与诊断”；只有功能密度足够时再拆分。Windows 官方依据：
  <https://learn.microsoft.com/en-us/windows/apps/design/app-settings/guidelines-for-app-settings>。

## 性能约束

- xterm 使用 WebGL 渲染器（`@xterm/addon-webgl`），并监听 `onContextLoss` → dispose
  回退 DOM 渲染器，不让丢失上下文变成白屏。
- 新建活动终端必须先把容器设为可见再调用 `terminal.open()`；冷启动和会话首次可见时最多
  连续四帧重试 `fit()`。窗口、侧栏和抽屉的连续尺寸变化只走 100ms 尾沿防抖；拖拽期间只
  标脏，释放后恰好 fit 一次，`ResizeObserver` 对相同整数宽高短路。
- 输出写入在两侧都做合并：主进程按会话攒 8ms 或 64KB 发一次 IPC，渲染层按
  `requestAnimationFrame` 合并成一次 `terminal.write`（缓冲上限 512KB，超限丢弃最旧的
  分块）。逐块 `send`/`write` 是输入卡顿的真实来源——每块都会触发一次 xterm 重排。
- 主进程的合并只影响发给渲染层的消息；`consumeTerminalOutput` 仍然逐块接收，因为它跨块
  跟踪退出标记，喂给它合并后的缓冲会漏判。
- 终端操作遮罩复制当前 xterm canvas 作为冻结快照并只模糊快照，真实 xterm 在下层继续消费
  输出和完成权限探测；遮罩引用计数且必须在 `finally` 释放。禁止暂停 `terminal.write()`，
  也不用 `backdrop-filter`，否则既会让探测超时，也会持续强制 GPU 合成。

## Claude 工作台

- “会话”页先显示 Claude Code 版本门禁与接入路由，再显示上下文进度、token、费用、持续
  时间、运行模型和会话 ID；数据为空时使用“等待首个响应”，不伪造 0 token。
- 当前路由卡下方增加健康卡：1-token 测试通过使用绿色，部分兼容使用琥珀色，Router
  未就绪或真实 Claude Code 请求失败使用红色。错误卡必须写明来源是“连接测试”“Router
  状态”还是“真实会话”，并提供“前往接入排查”操作。
- 当前项目指向 `3456` 且 Router 无 Provider/网关未运行时禁用会话启动按钮；远程直连的
  无关 CCR 错误不得阻止会话。运行中错误通过 toast 与健康卡同时反馈，但同一错误不重复
  播报。
- 上下文进度 0–65% 使用青蓝到薄荷绿；65–85% 转为琥珀色；85% 以上转为珊瑚红。百分比、
  数字和颜色同时表达，不只依赖色彩。
- 会话入口分为一个主操作“新建安全会话”和两个次操作“继续最近 / 选择历史”，并在按钮下方
  解释三者的上下文差异。
- 会话入口下方是每项目持久化的“允许「完全允许」模式”复选框，默认勾选。说明必须写清它只是
  启动时预置、不会自动激活，以及关闭后需要重新启动会话才生效。它是一个逃生开关：
  Claude Code 首次以该标志启动时会弹自己的一次性免责框，`root`/`sudo` 下还会直接拒绝，
  用户必须能关掉它。
- 历史对话只在左侧项目文件夹层级中出现，工作台不重复列出。历史项只显示名称与最近活动
  时间，完整列表放在文件夹内约六行高的独立滚动区中：运行中对话固定在上方不随滚动移动，
  滚动位置在列表重建后保留。左键在新的当前对话中恢复；悬停/聚焦显示红色 `×`，右键菜单
  同时提供重命名与删除。删除必须进入“永久删除、无法恢复”的应用内确认框；运行中对话与
  历史对话共用应用内重命名弹窗，不依赖浏览器原生 prompt，也不为了补齐内容伪造 token
  或费用。
- 运行中对话的主题标题来自 Claude Code 官方 `statusLine.session_name`：有用户自定义名称时
  使用自定义名称，否则使用 Claude Code 根据首条提示词生成的标题。自动标题到达时在原位置
  播放打字机动画（先擦除、后逐字输入、带光标），保留新旧标题的公共前缀以减少闪动；
  动画状态与 DOM 解耦，列表随工作区刷新重建时从当前帧继续，不会重播或跳变。
- “命令”页使用双列命令卡片呈现命令名和用途；参数输入只对 `/compact`、`/rename` 等支持
  参数的命令生效，`/theme` 打开 Claude Code 自身主题选择，`/clear` 必须二次确认。
- “快捷键”页集中说明 `Ctrl+C`、`Ctrl+V`、`Shift+Enter`、`Ctrl+L` 和右键菜单；映射只
  对应用内终端生效，不暗示已修改外部 PowerShell 或系统键位。
- “接入”页的默认路径按“环境准备 → 选择服务商 → 填写凭据与模型 → 测试并接入”组织。
  环境已就绪时隐藏环境卡；未就绪时服务商卡片与后续表单禁用，但不遮挡 PowerShell。
- 服务商按“官方接入 / 国内服务 / 海外与聚合服务 / 本地服务 / 高级方式”分组。每个分组标题
  是带线性箭头的展开按钮，维护 `aria-expanded` / `aria-hidden` / `inert`，内容以
  `grid-template-rows + opacity` 过渡展开或收起；分组状态独立，不影响当前服务商选择。每次
  从其他页面进入“接入”时，以最后选择的服务商为唯一展开组：官方只开官方、自定义只开高级，
  其他服务同理；没有可解析选择时全部收起，等待配置载入后再展开正确组。
- 服务商卡片按选择器自身的容器宽度响应，而不是按整个窗口猜测：低于 290px 为一列，
  290–469px 为两列，470px 起为三列。卡片同时显示名称和一句用途；当前选择使用强调色描边，
  已保存项另显示“当前配置”文字徽标，不能只用颜色表达。
- 选中服务商后才显示第二、三步；再次点击已选卡片进入真正的“未选择”状态，同时隐藏服务商
  说明、凭据/模型表单、测试结果和修复卡；服务商选择与历史配置仍保持可见。
  固定基址与认证方式由目录收敛；自定义、Router、cURL 才暴露对应高级字段。服务商控制台、
  官方文档和密钥不可混用等风险提示紧邻选择结果。
- 活动栏底部“设置 → 接入”完整容纳自动发现、认证来源、手动配置、Router 管理、cURL 识别、
  转换器说明和术语解释；认证来源首先说明
  ClaudeDock 显式凭据与 Claude Code `apiKeyHelper` 的组合，并提供“ClaudeDock 单一凭据”
  与“保留 helper”两种明确选择。选择 Router 或 cURL 时把对应
  完整工具临时提升到第二步，不复制功能或维护两套 DOM。全局设置层使用三行网格
  `标题 / 分类与独立滚动内容 / 固定操作区`，随全局主题换色，不使用
  `backdrop-filter`。完成保留输入；取消、关闭按钮与 `Esc` 恢复打开前的未保存输入，但明确
  告知安装、卸载、启停和 Router Provider 保存等已经执行的操作不会回滚。
- “选择历史配置”是独立的接入主流程组件，固定放在模型与凭据表单之前；不能
  移进高级模态层。每条记录以可点击恢复卡显示服务商、接口/网关、主模型、快速模型、认证、
  凭据状态、保存时间和网关状态。列表最多显示 20 条并在组件内部滚动，长 URL 与模型标识必须
  在值列自然断行，不得撑破窄侧栏。
- 环境准备卡同时显示已安装版本、可检测到的最新版本和检查时间。未安装时显示安装源与安装
  按钮；已安装时只有确认存在新版才显示来源与更新按钮，最新状态不提供“重新安装 / 检查”
  伪更新入口。官方原生、npm 与 npmmirror 使用面向公开用户的中性产品文案。
- 自动检测卡片同时展示服务名、真实 API 基址、运行/待处理/离线状态、检测依据和管理入口。
  Claude Code Router 必须明确区分 `3456` 模型接口与 `3458` 管理页。
- cURL 结果使用绿色 Anthropic、琥珀色 OpenAI 和中性未知三种状态；只显示“已识别密钥”，
  不显示完整值。OpenAI 结果必须说明上游密钥和 Router 访问密钥可能是两把不同的钥匙。
- Router 状态卡同时显示安装状态、版本、管理服务和模型网关状态；未安装时显示“安装”，
  已安装且确认有新版时才显示“更新”。“启动”“卸载 / 更换”“停止网关”“打开完整管理页”
  在不可用或操作中必须禁用，操作文案明确区分安装来源、管理服务与 `3456` 模型网关。
- Provider 为空导致 `No available models` 时，在状态卡下显示黄色“解决办法”卡：先说明
  缺少 Provider 及自动导入所需条件，再提供可用时的“一键修复”和始终可用的手动配置入口。
  Router 管理区始终保持可操作，不使用只适合某位用户当前配置的个性化判断文案；
  空 Provider 时禁用“启动 Router”，避免用户重复触发同一个错误。
- `better-sqlite3` 的 `NODE_MODULE_VERSION` 不匹配属于 Router 运行环境错误，而非
  Provider 错误。状态卡必须显示 ABI 原因和“不会改数据库/Provider/Codex”的边界，将主
  操作改为“修复运行环境并重启”；此状态下即使暂时读不到 Provider 也允许执行该修复。
- Provider 以紧凑卡片显示名称、协议、净化后的上游地址、模型列表、首选状态和“是否已保存
  密钥”；编辑表单支持新增/修改、保留旧密钥、设为首选及接入当前项目。删除需二次确认，
  一键 cURL 导入成功后立即清空包含密钥的输入与分析卡。
- 最终配置由服务商目录动态收敛字段：官方登录隐藏凭据与 API 基址；固定兼容服务预填基址、
  认证、主模型与快速模型；本机转换器解释应填模型接口而非管理页。
- 真实测试用三行状态分别呈现“接口地址 / 身份认证 / 模型响应”，成功、警告、失败和跳过
  均使用图标、文字与颜色共同表达，并显示请求延迟。
- 测试失败后紧跟“原因 / 建议 / 操作”修复卡，操作必须直接可执行，例如切换认证、打开密钥
  控制台或文档、采用快速模型、安装/启动 Router、重试和返回选择服务商；不能只给错误文本。
- 真实测试开始后，原结果卡切换为中性强调色的“后台正在测试连接”状态并通过
  `aria-live` / `aria-busy` 通知；临时禁用服务商卡片与当前配置控件，导航和 PowerShell
  不加遮罩、不设 `inert`。测试成功并继续保存、测试失败或抛出异常都必须经过同一 `finally`
  恢复进度标记、控件与按钮文案，不能从嵌套保存动作继承旧的 disabled 状态。
- 密钥输入永远为空；保存后只显示“已加密保存”的状态，不提供明文回显或复制入口。
- 历史配置每条的主体是可点击恢复按钮，最右侧是独立的 `×` 删除按钮，右键另开「恢复 /
  删除」菜单。删除入口同时给两种是有意的：右键是熟练用户的路径，`×` 是能被看见的路径。
  没有记录时显示解释性空状态，不显示一个空列表。
- 历史条目显示保存当时的网关状态，但网关状态不参与「这条和上一条是否相同」的判断：
  网关反复启停不应该把同一份配置刷成一堵墙。
- 安全版本通过、待检查和被阻止分别使用绿色、琥珀色和红色提示条，同时带明确文字。
- 工具内按钮、状态、提示、错误和插件能力说明统一使用中文；PowerShell 原始输出不改写。
  产品名、命令、网址、模型名、插件 ID 与市场 ID 等必须准确匹配外部系统的技术标识可保留
  原文。插件卡只展示本地生成的中文能力说明，不提供英文原文折叠区；没有可靠中文说明时
  明确标注为概括，不伪装成插件作者提供的官方译文。
- 插件卡的“更新”和工具栏“更新全部”只在刷新后的目录明确标记 `updateAvailable` 时出现；
  启用/停用、卸载和安装属于独立操作，不受更新入口显隐影响。
- 终端底栏持续显示连接状态、上下文环形进度、模型和权限模式，不要求用户打开工作台。详细
  快捷键信息不占用底栏。
- 底栏的连接、模型和模式是三个真按钮，不是装饰性文本。点击连接使用当前项目已保存的配置
  原地开始真实测试，不关闭工作台、不切换左栏页面；按钮切到 `data-tone="pending"` 的脉冲圆点、
  显示“正在检测连接”、设置 `aria-busy` 并禁用，测试结束再恢复成正常/需确认/异常。忙态分支
  必须排在健康色分支之前，否则一次陈旧的路由健康会把刚点下去的进度盖掉；忙态也不能用整包
  替换按钮文本的写法实现，那会连圆点一起删掉。
- 模型与模式菜单共用 `.footer-menu` 浮层：`role="menu"`、固定定位、向上展开、两轴都做
  视口夹取，条目是「主标题 + 一句说明」的 `role="menuitem"` 按钮，当前项用
  `data-selected` 而不是只靠颜色。两个菜单登记进已有的 `pointerdown` 与 `window blur`
  收拢逻辑，不另起一套关闭机制。
- 选择模型后，底栏模型按钮以 `disabled` + `aria-busy` 表达短暂提交态；成功、失败和异常
  都必须在同一个 `finally` 中直接恢复按钮，不能把恢复责任交给随后可能失败或延迟的状态刷新。
  模型列表的当前项取本会话已提交的目标模型，其次取 statusLine 实测模型，不能只标记项目
  默认配置。斜杠命令正文与回车必须分两次写入并按会话排队，避免 Claude Code 输入框留下
  一条看似已点击、实际未提交的 `/model`。
- 权限模式一次性列全 Claude Code 的六种模式：手动确认、自动接受编辑、计划模式、自动选择、
  完全允许、仅预批准。当前项目没有预置「完全允许」时，该条禁用并在说明里写清要去工作台
  开启并重启；「仅预批准」在说明里写清会重启会话。不可用的模式保留在列表里并解释原因，
  不隐藏——用户需要知道这个模式存在以及为什么现在用不了。
- 模式徽标在 PowerShell 里由 Claude Code 自己重绘，底栏显示以它为准。`Shift+Tab` 在
  终端和输入框里都生效（输入框拦下默认的焦点遍历，转发同一段 CBT 序列）。Claude Code
  可能只输出变化的终端单元格，所以主动取样必须用输出修订号等 xterm 应用完此前屏幕差量，
  再扫描完整活动缓冲区并把已知模式回报主进程；底栏不根据点击做乐观假设。列表切换在第一
  次按键前和每次按键后都要完成一次取样：当前画面没有可读徽标（例如仍在选择器或确认框）
  时不发送按键，模式回到已访问项时停止，避免快捷键落入错误的交互上下文或绕环切歪。
- 需要重启会话的操作（跨端点换模型、仅预批准）先用确认框说明三件事：会重启、对话历史通过
  `--continue` 恢复、终端画面会重绘。文案不回避重绘，那是用户会亲眼看到的。
- 底栏在 1040px 以下隐藏模型，900px 以下同时隐藏模型、模式和后台状态；两者随底栏一起
  收起，不允许挤压重叠。
- 终端右键菜单只包含复制、粘贴、全选和清屏，并保持键盘可达；有选区时 `Ctrl+C` 复制，
  无选区时保留 PowerShell 中断语义。
- “Claude 工作台”旁提供紧凑的终端主题选择器，列出 Claude 明亮、Telegram 明亮、石墨深色、
  深海蓝四套主题；窗口变窄时只收起“主题”标签，不遮挡工作台、信息和清屏按钮。

## 动效与可访问性

- 状态灯呼吸动画 1.8s；拖放与按钮反馈 140–180ms；消息气泡以 Telegram 风格快速减速曲线
  在 200ms 内进入。
- 悬停只使用描边、表面层与微光，不上浮控件或文字；按压反馈不得改变按钮占位尺寸。
- 遵守 `prefers-reduced-motion`，减少动效时禁用循环动画和位移。
- 终端聚焦描边始终保留，微光的过渡遵守 `prefers-reduced-motion`；焦点位置不能只靠动画表达。
- 所有按钮提供可见焦点、`aria-label` 或可读文本；关键文字对比度目标不低于 4.5:1。
- 工作台页签、表单标签、上下文 `progressbar` 和展开按钮维护明确的 ARIA 名称、值与
  `aria-expanded` / `aria-hidden` 状态。
- 服务商分组收起后，其卡片必须退出键盘导航；全局设置使用原生模态 `<dialog>`，标题、
  说明、关闭按钮及“完成/取消”都具有明确名称，`Esc` 与“取消”保持同一语义。
- 自动检测区和 cURL 输入使用具名 `region` / `label`；动态诊断与测试结果通过
  `aria-live` 或现有状态提示通知，但不反复播报 6 秒轮询中未变化的内容。
- 标题栏刷新图标有稳定的中文 `aria-label`；检查期间使用 `aria-busy`，发现更新后标签同时
  包含可更新项数量，不能只用琥珀状态点表达。
- Router 管理区使用具名 `region`，Provider 表单全部使用显式标签；运行中的安装、启动、
  停止和保存操作通过按钮禁用与 toast 文本共同反馈，不能只依赖颜色。
- 渲染入口依赖的元素必须使用标准直引号 HTML 属性且保持 ID 唯一；智能提示区域必须留在
  接入页容器内，避免内容逃逸到终端或控制栏。
- 拖放不是唯一入口，虚线加号区域本身是键盘可达的按钮。
- 项目行使用可聚焦按钮切换，关闭按钮包含项目名的可读标签；关闭运行中的项目需二次确认。

## 素材维护

- 矢量源位于 `assets/source/`。
- `npm run generate:icons` 生成 `assets/generated/` 中的 PNG 和 ICO。
- 标题栏 Logo 由 Vite 从生成图标导入，开发和打包环境均使用同一资源源。
- 不直接手改生成文件；更改图标时修改 SVG 源并重新生成。
