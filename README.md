# ClaudeDock 控制面板

ClaudeDock 是一个面向 Windows 的桌面控制面板，用于在图形界面中同时管理多个项目的
真实 PowerShell 伪终端、项目级 Claude Code / Codex 开发会话、模型/API 接入与常用操作，
并通过系统托盘查看后台状态。

## 当前版本重点（3.6.0 · 2026-08-03）

- 新增 ClaudeDock 安装版应用内更新：环境卡可以直接从 GitHub Releases 下载正式版，显示进度，
  `electron-updater` 按 `latest.yml`/blockmap 校验完成后才开放“重启并安装”。`v*` 标签会触发
  Windows Release 工作流，自动上传安装包、blockmap 和更新元数据；开发版不会误触安装器。
- Claude Code 不再把原生、npm 官方源和 npmmirror 当作可随意互换的三种来源。原生安装沿用
  Anthropic 官方更新器，未安装时使用官方 WinGet 包；只有检测为 npm 安装时，才分别拉取不超过
  128 KiB 的真实包样本，在 npm 与 npmmirror 中自动选择当前更快的一条，避免重复安装和 PATH 冲突。
- 代理“节点与更新源测速”取消 10 MB 下载，改为通过专属 Electron session 和当前内置 Xray 做
  v2rayN real-ping 同类的轻量 HTTP 真实延迟，并同时检查 GitHub/npm/npmmirror。实现未复制或链接
  v2rayN 的 GPL 源码。
- 新安装默认开启增强隐私模式；独立对话的模型回复标记为“AI 生成”。全局设置新增“隐私与合规”页，
  持续披露本地保存、第三方端点、跨境处理与代理风险；详细隐私说明和中国大陆公开发行评估位于
  `docs/PRIVACY.md` 与 `docs/LEGAL_COMPLIANCE.md`。
- 当前 GitHub 仓库仍是私有仓库，普通用户无法读取 Release；对外发布前必须公开仓库或改用公开的
  自建 HTTPS 更新源。当前安装包也尚无受信任 Windows 代码签名，不应忽略 SmartScreen 风险。

## 3.5.0 版本重点（2026-08-03）

- 修复独立对话的 DeepSeek 连接测试仍报“接口响应格式与所选协议不一致”。最小的 1-token
  Anthropic 请求可能只返回 thinking 块而没有可见正文；测试现在按协议 envelope 判定兼容，真正
  发送消息时仍要求存在可见文本，避免把空回复写进对话。
- 代理页会在启动前检查 V2RayN、Clash、sing-box 等常见进程、Windows 显式代理与 TUN/VPN 接口。
  普通系统代理模式可与 ClaudeDock 并行：内置 Xray 使用随机回环端口且不写系统代理；发现 TUN 时
  则先解释链式代理风险并由用户确认，ClaudeDock 不会擅自结束外部软件或修改其配置。
- “IPv6 开关”拆成清楚的两层：日常使用的是内置隧道 IP 策略（仅 IPv4、双栈自动择优、双栈并
  优先 IPv6），后两者使用 Happy Eyeballs 并发尝试；可能短暂断网的 Windows 网卡 IPv6 开关移入
  高级折叠区，不再把“当前是开还是关”藏在含混按钮里。
- 新增“节点与更新源测速”：所有探测都强制经过当前内置 Xray，分别检查 GitHub API、npm 官方源和
  npmmirror，再从 Cloudflare/CacheFly 下载 10 MB 测真实吞吐，便于区分节点慢、更新源慢和连接失败。
- 修复软件更新检查没有继承 ClaudeDock 应用代理的问题：版本请求改用 Electron 默认 session；
  npm 官方源与 npmmirror 同时发起并接受首个有效结果，单条慢源不再串行拖满两次超时。

## 3.4.0 版本重点（2026-08-02）

- 代理泄露体检改为完整的双路径检查：出口 IP 继续由 Cloudflare 与 IPinfo 交叉验证，DNS 则通过
  `dnsleaktest.com` 的唯一权威测试域名分别观察直连和代理解析器，失败时回退到 `bash.ws`；Xray
  路由固定为 `AsIs`，目标
  域名保持到代理节点再解析。在线证据不可用时明确降级为警告，不再把本地 DNS 列表当成完整检测。
- 代理设置新增可逆的“禁用 IPv6（建议开启）”。它通过 Windows 管理员授权修改网卡的
  `ms_tcpip6` 绑定，只记录并恢复本次由 ClaudeDock 禁用的网卡；操作可能短暂断网，界面同时保留
  Windows 通常建议启用 IPv6 的风险提示。体检只把真正的全局 IPv6 地址当作旁路证据。
- 代理作用域新增“对话工作台”。独立对话使用专属 Electron session，可与 CLI、ClaudeDock 自身
  网络分别决定是否接入内置代理；切换作用域会关闭该会话的旧连接，使后续请求立即走新路径。
- HTTPS 代理订阅来源会连同节点一起保存，URL 由 Windows DPAPI 加密；代理页可单独更新全部已保存
  订阅。体检报告支持逐条删除；风险报告点击“返回调整”会先停止隧道，不能带着已知风险继续连接。
- 设置里的非即时选项改为草稿提交。开机启动、关闭行为、主题和高级选项修改后，完成按钮下方显示
  `*N 项未保存`；恢复原值会逐项扣减，点击“完成”才写入，取消会恢复。冷启动主题以主进程保存值
  为准，不再被渲染器的默认“Claude 明亮”覆盖。
- 按钮字体、普通文本输入框、聚焦光环、悬停和禁用状态统一到组件令牌；代理订阅地址等散落在
  `.field` 外的输入框不再使用 Chromium 原生样式。设置取消/完成和工作台操作也遵循同一字号层级。
- 右上角刷新入口现在聚合 ClaudeDock、Claude Code、Router、插件、代理订阅与当前项目 MCP；软件、
  插件、代理和 MCP 页面同时保留各自的检查入口。项目/对话关闭后的强制历史刷新可排队，旧请求
  不再覆盖刚归档的结果。

## 3.3.0 版本重点（2026-08-02）

- Xray-core 现在可以在代理面板直接点“测速并安装”：软件会对所有通过摘要校验的国内镜像与官方
  GitHub 线路实测归档吞吐，选择最快线路，在下载中心完成下载、固定 SHA-256 校验、解压和安装；
  不再要求先选择节点，也不需要复制地址到浏览器。手动拖入仍作为离线兜底。
- CCR、Codex 与 CC Switch 的 GitHub Release 资产也会用 Electron 默认会话并行测速镜像和官方
  线路。这一会话继承 Windows 系统代理/PAC；GitHub API 发布元数据不再走忽略系统代理的 Node
  `fetch`。崩溃恢复下载会等应用代理规则稳定后再重建，避免首轮 `setProxy` 把恢复连接掐在 0 字节。
- 内置 Xray 隧道改为 IPv4-only：DNS 查询、代理节点域名连接和自由出站都使用 IPv4，`::/0`
  交给 blackhole 明确拦截。该策略只存在于 ClaudeDock 启动的 Xray 临时配置，不修改 Windows
  全局 IPv6、DNS、系统代理或路由表；作用域卡会显示当前 CLI/应用接入和 IPv6 拦截状态。
- Anthropic 兼容响应不再强制消息 ID 必须以 `msg_` 开头；只要 ID 非空且 `content` 为数组即通过。
  这修复了 DeepSeek 等兼容端点已返回 200、认证已通过却被误判“响应格式不兼容”的问题。若正文
  明确含 OpenAI `choices` 才建议使用本地路由器，未知结构不再盲目转换。
- 接入补救操作改为单一忙态：点击任一操作后，服务商卡、配置表单与同卡其他操作会整体锁定并带
  `aria-busy`，结束后统一恢复。安装路由器也不再先清空当前服务商草稿或擅自切到“本地路由器”。

## 3.2.0 版本重点（2026-08-02）

- 内置代理终于能在没有任何现成代理的机器上把自己装起来。此前内核只有 GitHub Release 一个来源，
  于是形成死锁：要用代理必须先下内核，下内核又必须先有代理。现在内置 8 条国内可达的**前缀反代**
  镜像（完整 GitHub URL 拼在镜像域名后）加官方直连，下载前并发探测，用探得通的那条下。可信度不随
  镜像走：归档始终按代码里固定的 SHA-256 校验，镜像只改变字节走哪条路。
- 探测分两段，因为「响应快」和「下得动」是两回事。第一段拉同目录 299 字节的 `.dgst` 校验文件，
  一次请求同时回答「域名解析吗 / 连得上吗 / 跟到真实内容了吗 / 是不是我们钉住的那个版本」——
  摘要与常量不符的线路当场出局，无论是过期还是被投毒。第二段对第一段里延迟最好的 4 条各拉 256 KiB
  真实归档采样实际速率（3 秒上限，整轮探测不超过 1 MB）。首轮实测就证明了这一段的必要性：`.dgst`
  首字节最快的那条（789 ms）传 21 MB 的实际速率只有 ~13 KB/s，光看延迟会选出一个 25 分钟的下载。
  选路因此按实测速率排，没测到速率的才回落到延迟。
- 修复内核解压**从未成功过**。`powershell.exe -Command <字符串>` 会把后面的参数拼进命令文本，
  `$args` 始终是空的，于是 `Expand-Archive -LiteralPath $args[0]` 每次都以参数校验失败告终，
  对外只表现为「退出码 1」。路径改为经环境变量传入（`$env:` 取值是字面替换，路径里带引号或分号
  也不会变成第二条语句），并且子进程的 stderr 不再被丢弃——这个 bug 能藏这么久正是因为它被丢了。
- 修复本地入站健康检查抢跑。`spawn` 返回时 Xray 还没绑定端口，此前第一次 `connect` 拿到
  `ECONNREFUSED` 就直接判定启动失败，全过程不到 50 毫秒——比内核解析完配置还快。现在探测会持续
  敲门直到超时，只有「重试也不会变」的答案才提前结束：真实的 HTTP 状态码，或内核已经退出。
- 代理面板新增内核块。没有内核时直接显示状态、每条线路的通/不通/延迟/实测速率、可点击打开或复制的
  下载地址，而不是等用户点了「启动」才蹦出一行报错。已安装则折叠成一行。镜像域名 churn 极高（各类
  汇总里约一半两年内就死了），所以面板还能自己添加镜像域名，与内置源一起参与探测。
- 内核可以直接拖进面板安装。拖放只在内核区生效，与「拖文件夹建项目」互不冲突：分流发生在既有的
  document 级 handler 内部，不新增监听器、不用 `stopPropagation`，`dragDepth` 因此始终配平，
  全屏遮罩不会卡住。也可以用「选择文件…」，接受官方 zip 或裸 `xray.exe`，装完必须能跑出版本号
  才会替换现有内核。
- 「内核下载引导代理」由用户自己填，默认为空即直连，软件不替用户猜、也不写死任何端口。旁边的
  「检测本机代理」只**列出**环境变量与系统代理里的候选供点选，绝不自动启用。
- 删除历史对话的按钮不再贴着竖向滚动条：右边距与时间戳对齐（7px），滚动容器再补 2px，
  按钮离滚动条 9px。
- 界面切换补上过渡。rail 页、设置分页、终端 ⇄ 聊天都改为淡入，侧边栏折叠改为对
  `grid-template-columns` 做过渡并在动画结束后补一次终端 fit。这三处只做透明度、不做位移：
  仓库里唯一允许位移的 `.workbench-page` 位移的是 `pointer-events: none` 的那一份旧页面，
  对还在接收点击的图层做位移会在整个动画期间挪动它内部每个控件的命中区。
- 新增两个联网冒烟脚本：`scripts/xray-core-probe-smoke.cjs` 打印每条线路的实测通断/延迟/速率，
  `scripts/xray-core-install-smoke.cjs` 跑完整的探测 → 下载 → 解压 → 启动链路。

## 3.1.0 版本重点（2026-08-02）

- 修复关闭主窗口/最小化到托盘必定弹出「A JavaScript error occurred in the main process」。
  Chromium 在 `userData` 根目录下自己占用了一个名为 `Preferences` 的**文件**，而 Windows 路径大小写
  不敏感，应用偏好用的 `preferences` **目录**正好和它撞名，于是每次隐藏到托盘时 `mkdirSync` 都以
  EEXIST 抛出。存储目录改名为 `app-preferences`（旧目录里的设置首次启动时自动迁走），建目录前会清掉
  同名的非目录残留；托盘提示写入再失败也只降级为日志。主进程另加 `uncaughtException` /
  `unhandledRejection` 兜底，后台任务出错不再变成模态崩溃框。
- 修复 Xray 内核「总是下载失败」。两处原因：其一，内核下载的传输中安全上限被钉死成资产的精确字节数，
  上游哪怕重新发布时多出一个字节，`getTotalBytes()` 一超限就立刻以「下载内容超过安全上限」失败，连
  重试的机会都没有——而精确校验本来就由完成时的 `expectedBytes` + `expectedSha256` 负责，传输中的上限
  只需要够紧地兜住风险，现在放宽到 64 MB（其余下载一直用的就是这种常量上限）。其二，此前 60 秒无字节
  就直接判死并删掉半成品，可被限速的线路本来就会中途断好几次，每次都从 0 重来自然永远下不完。现在所有
  可恢复的中断（停滞、连接重置、不可续传的中止）都汇入同一条自动续传通道：最多 12 次、1→15 秒指数
  退避，每次都从磁盘上已有的字节继续。取消会抹掉 Chromium 的暂存文件，因此续传前先做一次前缀快照——
  下载只会追加写入，任何前缀都是合法的续传点，即使快照与写入并发也不会损坏。续传期间按钮显示
  「第 N/12 次」，用户点「继续」则立刻执行待定的那次尝试；12 次仍未完成才失败，且保留日志供下次启动继续。
- 每个对话独立记住自己的模型、权限模式与思考深度。新增 `conversation-preferences-store`，以对话
  UUID 为键把运行时观测到的配置落盘（仅接受 UUID 形式的 id 与白名单化的取值，避免任何字符串进入
  文件名或启动参数），从历史列表打开旧对话时底部状态栏直接还原成它上次的配置，而不是回落到全局
  默认值。部分观测只做合并，不会用「未知」抹掉本次会话早先捕获到的值。
- 侧边栏列表不再横向滚动。`overflow-y: auto` 会把 `overflow-x` 一并算成 `auto`，一条长标题就能给
  整个项目列表拉出一条横向滚动条；两个滚动容器现在显式禁止横向滚动，标题以省略号收尾。悬停某一行
  时「时间戳 → 删除」在同一个位置交叉淡入淡出：删除按钮绝对定位覆盖在时间戳上，时间戳只淡出、保留
  布局宽度，所以悬停既不重排，长标题也不会滑到按钮底下。
- 关闭正在运行的对话是归档而不是删除。确认文案改为「关闭并归档」，关闭后自动展开该项目文件夹并重读
  历史，让这一行看得见地落到「历史对话」里，而不是凭空消失；运行中的对话只提供关闭与重命名。
- 接入页移除「路由器已迁入全局设置」的迁移提示与入口。界面只呈现当前生效的配置，用户不需要知道后端
  的改动历史。
- MCP 页与插件页统一为同一套组件。MCP 改用插件页的搜索优先工具栏与「已安装 / 可安装 / 备份」标签
  布局，两页共用 `.plugin-toolbar` 与 `.plugin-form-panel`；插件页则补上与 MCP 作用域筛选完全一致的
  分类下拉（分类取自插件本地化词表），并复用 MCP 的 `data-fresh` 守卫——此前每敲一个搜索字符所有
  插件卡片都会重放入场动画，整块面板像在闪。
- 悬停显隐动效统一到 150–200 毫秒。新增 `--dur-hover: clamp(150ms, calc(var(--dur-micro) * 1.35), 200ms)`：
  主题原本 95–150ms 的微动节奏放在这么大面积的交叉淡出上会读成闪烁，clamp 既保留各主题的相对性格，
  又保证每个主题都落在这一档里。
- 刷新按钮只转图标。按钮本身是圆角方形并且必须保持圆角方形，画在边框上的动画会读成控件在形变——那是
  下载按钮专用的反馈语言。下载按钮则相反：静止时与刷新按钮同款圆角方形，一有任务在跑就缓动成正圆，
  由 `conic-gradient` 圆环承载进度（暂停时圆环静止并转为警示色），任务完成/取消/失败后再缓动回圆角方形。

## 3.0.2 版本重点（2026-08-02）

- 修复联网检索隔离始终报 `Agent type 'claudedock-web-research' not found`。2.5.4 只转义了引号，
  但 Windows PowerShell 5 见到含引号的原生参数就原样传下去、不再补外层引号，于是 MSVCRT 按
  空格把这段 JSON 拆成 75 个参数，子代理从未注册成功。现在把 JSON 里的空格一并编码成
  ` `——它本身就是合法的 JSON 转义，解析结果逐字节一致，参数里则不再有可拆分的空白。
- 修复 2.5.4 那条回归测试放过真实缺陷：它用的是自造载荷，恰好能通过旧转义。测试改为直接跑
  实际下发的 `claudedock-web-research` 定义，并断言 argv 中只有一个包含代理名的参数。这样验证
  不再依赖每次新开对话手动发一条联网请求。
- 修复未挂任何代理时启动内置代理必定下载失败（「60 秒内没有收到任何数据」）。`setProxy` +
  `closeAllConnections()` 的去重签名以空串起步，因此 sidecar 刚进入 `starting` 就被当成规则变化，
  真的执行了一次连接清空，正好掐断刚刚建立的 Xray-core 引导下载。现在在构造 sidecar 时先把当前
  规则登记为已应用；`starting` 与 `stopped` 解析出的规则本就相同，整个启动路径在隧道真正就绪前
  不会再碰 Chromium 的代理设置。下载核心不需要先有代理。
- 修复下载失败后进度条继续转圈。服务器没给长度时 `percent` 全程是 -1，只看这个数字的话
  已失败、已取消的任务会永远保持不确定态动画；现在终态一律停止动画并显示确定进度。
- 修复 MCP「全部刷新」与路由向导提交按钮仍是 Chromium 原生按钮。`.dialog-primary` 此前只由对话框
  作用域的选择器上色，类名本身没有外观；现在这两个控件分别并入主题的浅色按钮套件和
  `.dialog-primary` 自带外观，新增 `npm run test:control-theme` 遍历全部 160 个按钮，任何一个落回
  原生样式都会失败。
- 修复新增 MCP 没有契合主题的动效。两个 MCP 列表每次渲染都整体重建，卡片入场动画因此在所有行上
  同时重放，安装一个服务器看起来像整块面板闪一下。现在只有本次渲染中真正新出现的服务器才播放
  入场动画，并带一段主题强调色的落定过渡。

## 3.0.1 版本重点（2026-08-02）

- 修复内置代理对自建节点完全不可用的问题。此前解析器只认 `security=tls`，REALITY 分享链接的
  `pbk` / `sid` / `spx` / `fp` / `flow` / `alpn` 等参数全部被丢弃，节点被静默降级成明文 TCP，
  预览能显示、导入后却永远连不上。现在按 v2rayN 的字段语义完整保留这些参数，`realitySettings`
  与 `tlsSettings` 互斥输出，XTLS `flow` 只在握手层存在时才写入，缺少公钥的 REALITY 节点直接
  在保存时报错而不是留到运行时失败。
- 修复启动内置代理卡在 0%。应用作用域此前用 `direct` 覆盖了 `session.defaultSession`，正好也是
  下载 Xray-core 用的会话，用户配置的系统代理被顶掉后下载再也拿不到字节。未启用应用作用域时
  改为回落到系统代理设置；下载内核新增 60 秒无字节停滞检测，会给出可读原因而不是无限 0%。
- 修复代理启停语义。在下载界面取消 Xray-core 后再次点击启动不会再残留旧的启动尝试：停止会
  作废进行中的启动世代并连带取消它等待的下载；失败或取消后的状态是「已停止」，按钮回到可启动，
  而不是卡在既不能启动也不能停止的「错误」态。
- 代理运行中直接退出软件后，下次启动会自动恢复隧道。`autoStart` 只在隧道真正就绪时写入、
  一旦停止立即清除，因此能区分「意外退出」与「用户主动停止」。
- 修复图标跟着一起旋转的转圈动画。标题栏刷新、下载中心图标和下载进度环改为由独立的伪元素画
  一圈主题色圆环旋转，图标与文字保持静止；`prefers-reduced-motion` 下同步降级。
- 修复「打开路由设置」「恢复备份」「安装 / 导出 / 卸载 CC Switch」等按钮退化成无样式方框，
  现已并入主题的浅色按钮套件。
- 历史配置、代理导入预览和体检历史列表改为在自身区域内滚动并跟随视口高度收敛，配置变多时
  不再顶开或遮挡周围界面。

## 3.0.0 版本重点（2026-08-02）

- 新增统一下载中心：CCR、Codex、Xray 与 CC Switch 安装资源共用可暂停、继续、取消和崩溃恢复的
  下载内核；显示真实百分比、速度、已用/剩余时间，并在完成前核对来源、尺寸与 SHA-256。
- 退出与窗口关闭改为忙碌感知。可续传下载会提示可后台继续，安装、卸载和配置写入会明确标注
  中断风险；托盘与窗口 × 使用同一套状态真相，不会静默杀掉正在生成的对话。
- 独立对话取消固定总时长上限；默认静默只提示并旁路探活，不结束慢回复。可选的本地静默上限默认
  关闭，中途断流会保留正文并提供“继续生成”。
- 「设置 → 代理」可导入分享链接、Clash `proxies` 子集和 HTTPS 订阅，以 Windows DPAPI 保存
  凭据，并用受管 Xray sidecar 为选择的 CLI、独立对话或 ClaudeDock 应用网络提供代理；不会写
  Windows 系统代理。IP、DNS、WebRTC 与环境变量泄露体检会在有风险时阻断新接入；用户返回调整
  时会停止隧道，只有明确知情接受后才能继续。
- 「设置 → 路由」集中管理 CCR、CC Switch 和供应商能力矩阵。一键向导只问供应商、Key 与可选模型，
  原生 Anthropic 兼容供应商优先直连，需要 OpenAI 协议转换时才安装/启动 CCR；全过程展示阶段与
  下载进度。CC Switch 只使用官方 MSI 与 `ccswitch://` 单向导出，绝不读写其 SQLite。
- 左栏新增第 5 个「MCP」页：直接发现 Claude Code 的 user/project/local 三种作用域与 Codex CLI
  配置，显示真实来源文件和受限健康检查；目录离线提供精选条目，联网合并官方 MCP Registry。
  安装/卸载走 Claude CLI；项目共享 MCP 的启停必须先展示目标文件和 diff，再原子写入、备份，且
  可逐字节还原。没有“一键全装”。
- DeepSeek 与整张 Claude 供应商目录按当前官方集成文档复核；供应商“模型不存在”等原始错误不再
  被快速模型静默降级遮住。CCR 的所有写配置调用机器强制 `applyProfile: false`，只服务 CLI。
- 4 套主题继续驱动颜色、字体、几何、动效时长和缓动；新增下载、代理、路由、MCP、退出界面全部
  使用既有 token 与 `prefers-reduced-motion` 降级。

## 2.6.0 版本重点（2026-08-01）

- 修复部分中转站接不进去的问题。Claude Code 自己会在 `ANTHROPIC_BASE_URL` 后面追加
  `/v1/messages`，此前的“智能补全”却把用户填写的 `/v1`、`/relay/v1`、`/proxy/anthropic`
  等路径归一化掉，导致按 `/v1` 发布的中转站在别的软件能连、在这里连不上。Anthropic 协议现在
  原样保留中转站给出的基址路径，只有整段粘贴 `/v1/messages` 时才还原回它所属的基址；OpenAI
  协议仍然需要完整请求地址，补全逻辑不变。
- 接入页“历史”列表不再把整页挤扁：列表在自己的 360px 区域内局部滚动，控制面板负责整页滚动。
- 重放一条较早的历史记录不再新增一条重复记录。判重现在比对全部记录而不只是最新一条，命中的
  记录移到列表最前面；留空的快速模型按“与主模型相同”参与判重，不会因为自动补全而被当成新配置。
- 联网检索隔离改为“设置 → 高级设置”里的开关，**默认关闭**。关闭时新建的 Claude Code 会话不再
  附加 `claudedock-web-research` 子代理、`--append-system-prompt` 与 `WebSearch|WebFetch` 钩子，
  只有中转站确实存在“调到 high 以上无法联网检索”的协议缺陷时才需要打开。effort 400 的兼容
  回退与恢复保持常驻，与这个开关无关。

## 2.5.4 版本重点（2026-08-01）

- 修复 Windows PowerShell 5 启动 Claude Code 时剥掉 `--agents` JSON 双引号、导致
  `Agent type 'claudedock-web-research' not found` 的问题。启动命令现在按 Windows 原生参数
  规则转义 JSON 的结构引号、字符串内引号与相邻反斜杠，Claude Code 能真实注册会话级搜索
  子代理。
- 新增 PowerShell → 原生进程参数链路回归测试：启动一个 argv 探针并重新解析收到的
  `--agents` JSON，不再只检查生成命令中是否出现代理名字。

## 2.5.3 版本重点（2026-08-01）

- Claude Code 会话新增仅随本次进程存在的 `claudedock-web-research` 子代理：主会话遇到在线
  检索、WebSearch 或 WebFetch 时，把完整搜索子任务委派给该子代理；子代理继承当前模型但固定
  使用 `high`，只开放 WebSearch/WebFetch。搜索结果返回后，主会话继续用用户原先选择的
  `xhigh`、`max` 或其他档位分析，不再为了搜索永久降低整场对话的思考程度。
- 会话级系统提示负责主动委派，`PreToolUse` 本地钩子拦住遗漏的主线程直搜并要求改走专用
  子代理；定义通过 Claude Code 官方 `--agents` / `--append-system-prompt` 注入，不写用户或
  项目的 `.claude` 配置。若特定网关仍返回 thinking/effort 400，兼容回退只临时使用 `high`
  完成一次重试，顶层响应结束后自动恢复原档位。

## 2.5.2 版本重点（2026-07-31）

- 统一双协议中转、连接历史与端点补全相关文件的 Prettier 格式，修复 GitHub Actions 在
  `npm run format:check` 阶段退出的问题；本次维护不改变接口、存储格式或用户可见行为。
- 状态行 PowerShell 集成测试为子进程设置 30 秒硬上限，并把单项测试上限调整为 45 秒，容纳
  GitHub Windows runner 超过 10 秒的首次冷启动/安全扫描，同时仍能终止真正挂起的脚本。

## 2.5.1 版本重点（2026-07-31）

- 修复布局烟测把同一自绘下拉框中的透明原生 `select` 与可视 trigger 误判为两个互相遮挡控件的
  问题。同一 `.select` 复合控件内部的命中与重合现在按一个控件计算，不会放过不同下拉框或其他
  控件之间的真实遮挡。
- 布局烟测新增故意叠放两个独立按钮的校准探针；只有探针同时触发命中偏差与矩形重叠，且正式
  42 个场景全部无失败时才通过，避免为了消除误报而意外关闭真实遮挡检测。

## 2.5.0 版本重点（2026-07-31）

- “自定义 Anthropic 接口”已改为“自定义中转站接口”，模型与凭据表单新增 Anthropic / OpenAI
  协议切换。Anthropic 兼容接口由 Claude Code 直接访问；OpenAI Chat Completions / Responses
  会自动写入本地 Claude Code Router、启动 3456 网关并转换为 Claude Code 使用的 Messages 请求。
- 中转站地址支持智能识别和补全：可只填 `.com` 域名、带前导/尾随斜杠的地址、`/v1`、完整
  `/v1/messages`、`/v1/chat/completions` 或 `/v1/responses`。远程地址自动补 `https://`，本机
  回环地址自动使用 `http://`；切换协议时会替换已知接口后缀。（Anthropic 协议的路径归一化已在
  2.6.0 改为原样保留中转站给出的基址，见上文。）
- OpenAI 转换连接在项目配置与历史记录中仍展示原始中转站地址、模型、认证和凭据状态，同时单独
  标明本机 Router 转换路径；恢复历史时按 Router Provider ID 复用上游，不把 3456 误当成用户
  填写的接口。

## 2.4.0 版本重点（2026-07-31）

- 项目级 Claude 接入现在把 Anthropic Messages 直连与经 Claude Code Router 转换的 OpenAI
  Chat Completions / Responses 统一记入连接历史。每条记录显示真实上游协议标签，并另以
  “官方直连 / 中转直连 / 本地直连 / 本机转换器 / Router 转换”标签说明连接方式。
- 接入历史不再用笼统的“自定义”充当名称：Router Provider 自动沿用 Provider 名，自定义地址
  默认显示主机名；用户可以右键选择“重命名连接”，名称只用于当前项目的历史辨识，不修改接口、
  模型、凭据或协议。
- 历史存储升级为 version 2，协议参与判重且在一键恢复后继续保留。旧版明确可判断的直连记录
  迁移为 Anthropic，无法反推出上游协议的旧 Router 记录显示“协议待确认”，不做错误猜测。

## 2.3.5 版本重点（2026-07-31）

- 独立对话不再把中转站的瞬时故障直接当成最终失败：请求尚未产生模型输出时，会对网络断开和
  HTTP 408/409/425/429/500/502/503/504/529 最多自动重试 4 次，采用带抖动的指数退避，并
  遵守最长 60 秒的 `Retry-After`。界面会显示当前重试次数和原因。
- SSE 响应现在必须收到 Anthropic `message_stop` 或 OpenAI `[DONE]` 才算完整；首个有效输出前
  断流可安全重试，已有部分回答后断流则保留已收到的正文并明确标成中断，不盲目重放请求，避免
  重复扣费和重复文本。中断说明只用于界面反馈，不会混进下一轮模型上下文。
- 独立对话改为手动审查重定向：拒绝可能改写 POST 的 301/302/303 和可能泄露凭据的跨站跳转，
  只跟随最多 3 次同源 307/308。空闲超时从 120 秒放宽到 5 分钟，总时限从 15 分钟放宽到 60 分钟，
  更适合长思考和繁忙中转站。

## 2.3.4 版本重点（2026-07-30）

- 修复高档思考与 Claude Code `thinking` 状态冲突时，WebSearch 等工具直接返回
  `output_config.effort ... thinking is disabled` 400 的问题。七个思考档位全部保留；
  选择高档位时，ClaudeDock 只在本会话的临时 settings 中显式开启 thinking，不修改用户的
  Claude Code 设置。
- 启动会话时会清除继承来的 `CLAUDE_CODE_DISABLE_THINKING`、
  `CLAUDE_CODE_EFFORT_LEVEL` 与 `MAX_THINKING_TOKENS`，让底栏档位成为本次会话的真实控制源。
  如果上游仍命中这条已知兼容性错误，客户端只把当前模型会话自动降到“均衡”，提示重试刚才的
  WebSearch，并临时锁住会重复失败的高档位；换模型或重启会话后会重新开放，不影响其他模型。

- 底栏「模式」右侧新增「思考」，一次列全 Claude Code 能调的所有思考程度：跟随模型默认
  （`auto`）、最低、较低、均衡、更深、最深，以及 Claude Code 自身的「最深 + 工作流」
  （`ultracode`）。每项都写清代价与作用范围——`max` 与 `ultracode` 只作用于本次会话，
  其余会写入思考程度设置并跨会话保留。
- 思考程度走运行中的 `/effort`，所有档位都不需要重启会话，与权限模式里的“仅预批准”
  和跨端点换模型不同。命令正文与回车仍按会话串行提交，避免 Claude Code 把整块输入当粘贴。
- 底栏显示以 `statusLine` 上报的 `effort.level` 为准，也就是 Claude Code 实际生效的档位：
  模型不支持所选档位时会静默降到它支持的最高档，`ultracode` 也只会回报为 `xhigh`。状态行
  尚未刷新前先显示本次请求值；已执行兼容回退时也立即显示“均衡”，不等待旧状态行刷新。
  重启会话后请求值作废。模型完全没有思考参数时状态行不含该字段，此时菜单和悬停提示都会
  说明这一点，而不是显示一个假档位。

## 2.3.0 版本重点（2026-07-30）

- 四套主题各自拥有字体人格：Claude 使用 Hanken Grotesk 搭配 Newsreader 衬线标题（Anthropic
  自有的 Styrene/Tiempos 为商业授权字体，无法随应用分发，这两款是最接近的免费替代）；
  Telegram 使用其桌面客户端同款 Roboto；两套深色主题保留 Inter，但动效时长与缓动曲线不再
  互相复制，切换深色主题不再只是换配色。
- 建立标准组件套件，杜绝原生控件。原来由操作系统绘制的 `<select>`（含主题选择框）改为完全
  由主题令牌渲染的下拉列表，复选框与单选框同样重绘；原生元素保留在 DOM 中继续承担取值、
  校验与 `change` 事件，因此所有既有调用点行为不变。
- 统一对话与项目终端的输入框标准。两者现在共用同一套几何契约与聚焦动效：发送按钮与输入框
  齐平并随其高度伸缩，聚焦时的光环带一次过冲，发送时气泡上浮——此前只有项目终端有这些。
- 补齐交互反馈：所有按钮都有按压响应，此前约三十个只有悬停、甚至毫无反馈的按钮现已补全
  悬停与按压状态；主要操作按钮增加从指针位置扩散的涟漪。列表行按钮刻意不做缩放，避免文字
  相对相邻行错位。
- 退出前保护正在进行的对话：当回复正在生成或消息正在发送时，退出（托盘菜单、Alt+F4 等）会
  先弹出符合当前主题的确认框。系统关机/注销不询问，终端在托盘后台运行属于既有设计也不询问。

## 2.2.2 版本重点（2026-07-29）

- 恢复终端底栏“连接待测试”的真实连接检查：Claude 项目点击后会继续使用当前已保存的
  中转站、本地网关或官方配置发送最多 1 个输出 token 的 `/v1/messages` 请求，而不是只打开
  官方网络预检详情。
- 当前 Claude 项目首次载入以及应用从系统托盘恢复时，会自动执行一次相同的真实连接检查，
  不再要求用户先点一次按钮。普通 Alt+Tab、窗口聚焦或重复状态刷新不会反复请求；自动实测
  可能产生极少量模型费用。
- 官方网络预检继续作为额外防护：只有 Anthropic 官方连接在发出真实请求前需要通过官方访问
  守卫；中转站与本地网关不套用官方端点结论，直接验证其自身配置的真实接口。

## 2.2.1 版本重点（2026-07-29）

- 修复 ChatGPT 等官方端点返回正常跳转时，Electron 应用探测误报
  `Redirect was cancelled` 的问题。应用侧现在同步跟随最多 8 次 HTTPS 重定向，逐跳校验
  服务商受信任域名；旧探测器产生的同类取消错误只标记为“未确认”，不再当成网络断线阻止使用。
- 网络路径只描述当前进程能看到的代理第一跳。未发现系统代理或
  `HTTP(S)_PROXY / ALL_PROXY` 时，界面显示“未发现本机显式代理”，不会宣称公网直连；
  Mihomo/Clash、Xray/V2Ray、sing-box 内部链式转发以及 TUN、透明代理、软路由仍由真实官方
  端点结果验证。

## 2.2.0 版本重点（2026-07-29）

- 新增统一的官方 AI 网络预检。应用启动、项目开发引擎切换、网络接口/在线状态变化时自动检查
  Anthropic Claude Code 或 OpenAI Codex；探测只使用 DNS、无凭据 HTTP(S)、TLS、
  WebSocket 握手、CLI `curl` 路径和 `--version`，不会发送模型请求或消耗模型额度。
- 主进程分别解析 Electron/系统浏览器、Claude/Codex CLI、项目终端和受限渲染进程的网络路径。
  代理、VPN、虚拟网卡或应用与 CLI 路径不一致只产生可解释提示，不会单独封锁；Claude CLI
  命中官方不支持的 SOCKS 路径则阻止启动。
- 地区判断来自两家独立出口情报服务；只有两路结果一致且不在服务商最新官方支持列表时才阻止。
  单源、冲突或情报不可用会降级为警告。公网 IPv4/IPv6 只在内存中短暂出现，历史中仅保存
  `/24` 或 `/64` 掩码。
- 在官方登录、官方服务切换、CLI 启动、恢复官方历史接入和独立对话首次请求前统一执行访问守卫。
  关键 DNS/API/TLS/CLI 路径未知或失败时保持原状态；配置切换与 Claude 重启使用幂等回滚，
  后续步骤失败会恢复原开发引擎或原接入配置。
- 工作台顶部和终端底栏统一显示“正常 / 有提示 / 警告 / 部分可用 / 已阻止 / 正在检测”。
  详情弹窗列出风险理由、进程路径和逐项结果，可手动复检、清空 7 天/40 条脱敏历史，并可开启
  “增强隐私模式”停用 ipapi、ipwho.is 与 ipify 的公网出口检查。
- 服务商端点、支持地区、版本规则和依据集中在
  `src/shared/provider-profiles.ts`。Claude Code 的安全启动基线继续为 2.1.197，并与主预检共用
  同一份结构化版本规则；本轮未复制 CheckCC 或其他第三方项目代码、资源与依赖。

## 2.1.0 版本重点（2026-07-29）

- 每个项目新增“Claude Code / Codex”开发引擎选择。选择 Codex 后，主操作会自动完成官方
  CLI 检测、安装、ChatGPT 登录和当前项目启动；已安装用户不会重复安装，选择会按项目持久化。
- Codex 安装只接受 OpenAI GitHub Release 中与版本匹配的 `install.ps1`，执行前同时核对
  下载地址、文件大小和 SHA-256；优先使用官方 Windows 独立版，兼容现有 npm 安装。
- ChatGPT Plus、Pro、Business、Edu 或 Enterprise 的 Codex 权益通过官方 Codex App Server
  登录。ClaudeDock 只接收账号类型、套餐、额度和登录进度，不读取、保存或转交 OAuth 令牌；
  支持浏览器登录、设备码登录、取消和退出。
- Codex 会话仍运行在真实 PowerShell/ConPTY 中，默认固定 `workspace-write` 沙箱和
  `on-request` 审批；提供新建、继续最近和选择历史三种官方 TUI 入口。Codex 与 Claude Code
  不能在同一项目终端里同时运行，切换前会检查活动状态。
- 新增 Codex 工作台，以“官方 CLI → ChatGPT 账号 → 当前项目”三步展示准备状态、版本、
  订阅额度和安全边界。覆盖式工作台在 1180px 推荐宽度保持完整信息宽度，小窗口再按容器收缩。
- 本版不把 ChatGPT 订阅伪装成 Claude Code 官方模型。CC Switch 的多工具配置、MCP/Skills
  同步和可选本地路由已纳入后续路线，但 Codex OAuth → Claude 的反向代理属于非官方高风险
  兼容路径，不会默认安装或静默启用。

## 2.0.2 版本重点（2026-07-29）

- 点击活动栏“对话”后，只要输入框当前可用且没有详情抽屉或设置弹窗占用焦点，就会在下一绘制帧
  自动聚焦独立对话输入框；再次点击“对话”收起左栏时也保持这一行为。
- 独立对话输入框不再使用浏览器默认焦点环。聚焦时由当前主题的强调色令牌驱动描边、底色和
  一次收敛式微光动效；Claude、Telegram、石墨和深海蓝分别显示自己的颜色与节奏，并继续遵守
  `prefers-reduced-motion`。
- “详细信息”抽屉改为紧凑的“说明 / 网络策略 / 运行状态 / 请求审计”结构，内容从顶部自然排列，
  不再把短文案和开关拉伸成不成比例的大块区域；联网控制收敛为紧凑开关，运行与审计各自带
  边界说明。
- “对话”左栏删除模型接入迁移提示，只保留从标题一直延伸到底部的完整对话历史；列表不再受
  248px 高度上限约束，模型接入继续统一从右上角齿轮弹窗打开。

## 2.0.1 版本重点（2026-07-29）

- 主题切换现在贯穿字体、动效、几何与配色四条轴，而不只是换色。样式表里通用的时长、缓动、
  圆角、行高、字距和字重阶梯全部由主题自有令牌派生：`--ease-standard` 指向主题的
  `--ease-enter`，`--dur-2` 指向 `--dur-micro`，`--r-md` 指向 `--r-theme-md`，行高与字距
  按固定比例从 `--lh-body` / `--ls-title` 推导。切到 Telegram，整个外壳的过渡变慢（0.12s →
  0.15s）、缓动换成它自己的长回弹、圆角收紧（12px → 8px）、行高收紧（24.1px → 21.2px）；
  切回 Claude 则恢复宽松节奏。标题、对话框标题与项目卡也开始使用主题的 display 字体，
  Claude 的 Source Serif 4 衬线标题因此可见，Telegram 保持单一 Open Sans。
- 独立对话输入框支持 Ctrl+V 直接粘贴图片与文件。剪贴板里的图片没有磁盘路径，因此走新的
  字节导入通道 `chat:import-attachment-bytes`，由主进程按同样的数量、体积和媒体类型上限
  校验后写入附件目录；同一次粘贴里混有路径文件和内存字节时两条通道并行处理。
- 修复独立对话连不上中转网关的问题。Claude Code 注入 `ANTHROPIC_API_KEY`，而多数中转站读
  `Authorization: Bearer`，此前对话客户端只发 `x-api-key`，导致项目能接入、对话接不上；
  现在两个头同时发送。
- 修复回复被截断导致的“降智”观感：流式 Anthropic 请求的 `max_tokens` 此前硬编码为 4096，
  会连思考一起截断，现在提升到 64000，并在网关拒绝时按 8192 降级重试。
- 模型接入表单从左栏移到「对话」右上角的齿轮弹窗：背景模糊、面板清晰，左栏只保留历史。
- 每个独立对话都可以像项目对话那样重命名，并带同样的打字机擦除/重打效果；重命名后的名字
  会被持久化的 `titleCustom` 标记保护，不再被首条用户消息派生的标题覆盖。

## 2.0.0 版本重点（2026-07-29）

- 终端主题现在从创建实例开始就由 ClaudeDock 接管：当前 xterm 调色板、后续新建终端、
  PSReadLine 24-bit 语法色和 Claude Code 自身的明暗主题保持一致。亮色主题不再出现黑底、
  黑边或低对比度 dim 文本。
- 窗口连续缩放和左右分隔条拖动改为 100ms 尾沿防抖；只有冷启动继续执行四帧有界测量。
  主进程无条件确认最终行列数，xterm 获得 Windows ConPTY build hint，避免重复重排造成的
  输出重影和重复行。
- 切模型、切权限模式与压缩重启期间，终端显示冻结快照、主题化 veil 和居中状态标签；底层
  xterm 仍继续消费 PTY 输出和权限徽标探测，操作结束后一次恢复，不丢输出也不改变面板尺寸。
- 独立对话升级为安全富文本：Markdown 由 token 树逐节点构造 DOM，不使用 `innerHTML`；
  支持标题、列表、表格、引用、链接、图片、KaTeX 公式、Shiki 语法高亮和代码复制。流式回复
  按动画帧合并，并只追加稳定 token 前缀，未闭合代码围栏不会先渲染成错误段落。
- 模型给出的 HTML 代码默认保持高亮文本，只有用户点击“运行此可视化”后才创建
  `sandbox="allow-scripts"` 且无同源权限的 Artifact iframe。内置 d3、Plotly、Mermaid 和
  KaTeX 可离线加载；“详细信息”抽屉显示完整网络请求、状态与可可靠取得的响应字节数，并提供
  持久化总断网开关和逐个停止入口。
- 独立对话支持 PNG/JPEG/GIF/WebP、PDF、CSV、TSV 和纯文本附件。文件由主进程校验并复制到
  `userData/claude/chat-attachments/`，历史 JSON 只保存 UUID 与元数据；图片以缩略图、
  文档以文件卡片显示。Anthropic 与 OpenAI 线格式分别序列化，1.x 纯字符串历史可透明读取。
- 四套主题开始表达不同人格，而不只是换色：Claude 使用 Inter 正文、Source Serif 4 标题、
  宽松行高与柔和圆角；Telegram 使用 Open Sans、紧凑行高、较小气泡圆角和自己的缓动；
  石墨与深海蓝保持中性工程风格。主题还控制按压、进退场、遮罩和 Artifact CSS 变量。

## 1.7.0 版本重点（2026-07-28）

- “项目与对话”的每条历史对话新增可见删除按钮，右键菜单也提供同一操作。删除前使用应用内
  对话框明确提示永久且不可恢复；若该 Claude conversation 正在运行，会先关闭对应终端，
  再按规范化项目路径与 UUID 删除精确的会话记录并刷新列表。
- “对话”已移到活动栏最上方，让“项目 / 接入”保持相邻。独立对话现在实时显示把输入草稿、
  历史消息和流式回复计入的上下文与输入/输出 Token；接口提供真实 usage 时显示精确值，
  否则明确标成估算值。工具栏提供“新对话”，接入表单提供与项目页一致的 1-token 连接测试。
- 独立对话历史会自动保存到本机，最多保留 50 个对话、每个 100 条消息；左栏可恢复或经
  应用内二次确认永久删除。它不启动 PowerShell，也不复用项目级 Claude Code 配置；支持
  Anthropic Messages 与 OpenAI Chat Completions 兼容接口，凭据仍只以 Windows 安全存储
  密文保存。
- 主题系统从“只给深色外壳换色”升级为两套明亮主题和两套深色主题。默认的“Claude 明亮”
  使用暖白纸面、陶土强调色和 Open Sans，“Telegram 明亮”提供冷白表面与蓝色交互层；
  两套深色主题继续保留。主题同时切换排版、表面层级、阴影、交互层、语义状态色、xterm
  与 Windows 原生标题栏。
- 按钮悬停不再上浮，改为描边与微光反馈，避免文字重采样闪烁；消息进场和选择反馈采用
  Telegram 风格的 200–300ms 快速减速曲线，并完整遵守 `prefers-reduced-motion`。
- 活动栏左下角新增常驻“设置”入口。全局设置首期按“总设置 / 接入”分类：总设置包含实际
  开机启动状态、主题、当前可用语言与应用版本；原“接入”页里的高级设置入口已移除，认证来源、
  网关发现、Router、cURL 和诊断工具整体迁移到“全局设置 → 接入”，没有复制第二套状态。
- 打开“接入”页时只展开上一次所选服务商所在的分类：官方、自定义及其他分类都遵循同一规则，
  其余分类默认折叠；离开页面后手动展开的临时状态不会污染下一次默认状态。
- “高级设置”新增“认证来源与密钥冲突”：默认让 ClaudeDock 保存的显式凭据成为该项目会话的
  唯一凭据，避免与 Claude Code 用户/项目设置里的 `apiKeyHelper` 同时生效；高级用户也可选择
  保留 helper。推荐模式只写运行期临时设置，不修改原 Claude Code 配置文件。
- PowerShell / Claude Code 输出区获得鼠标或键盘焦点时显示克制的主题色描边与微光，明确反馈
  原始终端已接管输入；减少动态效果偏好仍会关闭过渡。
- 终端底栏的“连接待测试”是真按钮：当前 Claude 项目首次载入和应用从托盘恢复时会自动使用
  已保存配置实测一次，点击仍可随时手动重测；按钮进入转圈的等待态并禁止重复点击，测试结束后
  自动恢复成连接正常/需确认/异常。“测试并接入”也由同一个测试生命周期统一恢复，不会在
  成功保存后持续转圈。
- 底栏“模型”可点击，弹出与整体样式一致的列表，来源是当前接入配置和该项目的接入历史
  （中转、本地、国内、国外、官方 API 与官方账号都在其中）。同一端点的模型在运行中的对话里
  即时切换；换了接口地址或凭据的条目会先确认，再压缩上下文、重启会话并用 `--continue`
  接回原对话。
- 运行中切换模型不再把 `/model <模型>` 与回车一次性写进 Claude Code：正文与回车按安全间隔
  分两次提交，同一会话的应用内斜杠命令排队执行，避免命令停在输入框或快速操作时字节交错。
  切换成功、失败或异常都会立即恢复模型按钮，列表当前项以本次会话实际选择为准。
- 底栏新增“模式”，列出 Claude Code 的全部权限模式：手动确认、自动接受编辑、计划模式、
  自动选择、完全允许、仅预批准。终端和输入框里按 `Shift+Tab` 都能切换，PowerShell 里的
  模式徽标与底栏显示实时同步。
- 点击模式列表时先主动读取一次 xterm 中的真实徽标，确认当前模式后才逐次发送 `Shift+Tab`；
  每次按键都要等屏幕差量应用完成并再次回读，命中目标才停。若当前在选择器、确认框等画面而
  读不到徽标，ClaudeDock 不会发送按键；若循环回到已访问模式，也会立即停止并提示目标不可用，
  避免 `auto` 等可选模式缺席时继续盲按而切歪。
- 工作台新增“允许「完全允许」模式”开关，默认开启：启动时预置但不激活，之后可用 `Shift+Tab`
  或底栏切入。关掉后需要重新启动会话，启动命令也不再带
  `--allow-dangerously-skip-permissions`。
- “仅预批准”不在快捷键循环里，只能在启动时设定，选择后走与跨端点换模型完全相同的
  “压缩 → 重启 → `--continue`”流程。

## 更早版本重点（2026-07-27）

- “接入”页改为“环境准备 → 选择服务商 → 填写凭据与模型 → 测试并接入”三步主流程。
  Anthropic、DeepSeek、GLM、Kimi、通义千问、MiniMax、豆包、MiMo、StepFun、
  SiliconFlow、OpenRouter 和 Ollama 等入口由同一份服务商目录预填官方兼容基址、认证方式、
  主模型与快速模型；自定义中转站、Router 和 cURL 路径完整保留在高级方式中。
- 服务商类别都可点击标题展开或收起，并带箭头与过渡效果；服务商卡片根据侧栏实际宽度自动
  切换一、二、三列。再次点击已选服务商会取消选择，此时后续配置与测试步骤整体收起，页面只
  保留服务商选择、高级设置入口和可复用的历史配置。
- “高级设置”改为主题自适应的模态窗口，集中承载网关、cURL、Router 和诊断工具，并提供
  “完成”“取消”与 `Esc` 关闭；取消会恢复本次打开窗口前的未保存输入，已经执行的安装、卸载、
  启停或 Router Provider 保存仍立即生效。
- “选择历史配置”保留在接入主流程中，位置固定在高级设置入口与模型配置之间；每条直接展示
  曾填写的接口/网关、主模型、快速模型、认证和凭据状态，点击即可恢复全部保存参数。
- 连接测试通过后才自动保存；401/403、404、400/422、超时、网络错误和非标准 Anthropic
  响应会生成可点击的修复动作。Kimi 开放平台/Kimi Code 密钥不通用、SiliconFlow 使用
  `x-api-key` 等服务商差异会在选择时直接提示。
- 标题栏右上角新增统一刷新图标：首屏完成后会在后台检查 Claude Code、Router 与插件市场，
  平时不再常驻“更新”按钮；只有确认存在新版时才显示对应更新入口，未安装的软件仍显示安装
  入口。点击刷新图标会强制刷新软件元数据与插件市场。
- 修复窄侧栏下插件市场的最小宽度与遮挡：长插件名、仓库地址、市场表单和多按钮操作区都能
  在容器内换行/收缩，820px 最小窗口不再横向溢出。
- 修复窗口偶发进入“看得见但点不动”的状态：分隔条拖拽即使被失焦、托盘隐藏或系统取消打断，
  也会主动释放鼠标捕获；窗口恢复后会重新校准终端布局。新建/切换对话时，输入框会等到对应
  PowerShell 真正进入运行态再获得焦点，不再依赖固定毫秒延时。
- 修复点击“真实测试端点、密钥和模型”时接入页与 PowerShell 短暂无响应：安装检测、Router
  状态、网关扫描和软件更新现在共用两个受控后台任务槽，同类请求合并，用户发起的真实测试
  优先于尚未开始的自动刷新；测试卡会显示后台状态，其他界面仍可继续使用。
- 启动后不再凭空出现一个以 Windows 用户名命名的项目：没有打开项目时工作区就是空的，
  界面显示引导态，等用户点击最近项目或添加文件夹。
- “接入”页新增接入历史记录：每次保存都会留一条，与上一条完全相同则不新增；点击一键恢复
  当时的全部配置，右键菜单或记录右侧的 `×` 都可以删除。凭据仍只存在 Windows 安全存储里，
  历史文件本身不含明文。
- 输入框把快捷键说明改成占位文字（开始输入即消失），发送按钮撑满右侧整列；发送时提示词
  以气泡上浮的方式离开输入框。
- 终端不再有一圈黑边，输出区背景跟随所选主题；点击发送会真正提交给 Claude Code，而不是
  只把文字粘进它的输入框。
- 修复 `Ctrl+C` 等场景下两屏叠在一起的重绘错位：PTY 采纳的尺寸会回传给渲染层，xterm 与
  ConPTY 始终在同一网格上。
- 面向公开用户的环境准备与接入向导：检测 Claude Code/Router，并提供官方源、npm 官方源
  与 npmmirror 国内镜像的一键安装或更新。
- Router 可识别桌面版、npm 版和混合安装；用户可停止并彻底卸载旧安装（连同全部配置数据），
  再切换来源安装新版；npm prefix 之外的安装目录也能清除。
- 插件市场直接读取 Claude CLI 的官方目录，支持安装、启停、卸载、刷新市场与批量更新；单个
  插件与“更新全部”只在目录确认存在更新后出现。
- 工具内的按钮、状态、错误与插件能力说明统一使用中文；PowerShell 原始输出、产品名、命令、
  网址和插件 ID 等必须保持准确的技术标识不做翻译。
- Windows 安装向导固定使用简体中文。
- 插件 ID 保留官方名称，英文说明通过本地规则生成中文能力说明，界面不再显示英文原文。
- 统一全部滚动区域的样式；左侧控制栏与右侧工作台都可拖动调整宽度并记住设置。再次点击当前
  活动栏按钮会收起左侧控制栏，业务入口与底部设置仍保持可见，再次点击任一入口即可展开对应页。
- 820px 起的窄窗口和 640px 起的低高度都有独立响应式布局，控件空间不足时换行或收起次要
  信息；插件名、市场地址、表单和操作按钮不会再用隐含最小宽度撑破侧栏。
- 工作台状态下沉到终端底栏，直接显示连接健康、上下文占用与运行模型；快捷键说明独立成页。
- 终端区分为「输出区 + 输入框」：提示词写在输出区下方的独立多行输入框里，因此 `Ctrl+A`
  全选、`Shift+←/→` 逐字选择、鼠标拖选、`Ctrl+Z` 撤销和中文 IME 都是浏览器原生行为；
  `Enter` 发送、`Shift+Enter` 换行（多行提示词作为一条命令进入 PowerShell）、`↑/↓` 翻本地
  历史。输出区仍可直接点进去打字，供 Claude Code 自身的 TUI 使用。
- 快捷键说明是输入框的占位文字，开始输入即消失；发送按钮撑满输入框右侧整列。发送时提示词
  会以气泡上浮的方式离开输入框，开启「减少动态效果」时不播放该动画。
- PowerShell 终端提供 UTF-8 中文输入、标准复制/粘贴、跨行退格和右键菜单；输出经 WebGL
  渲染器绘制，并在主进程与渲染层两侧合并写入，大量输出时不影响输入手感。
- 删除、清空和重启等确认操作统一使用应用内模态框，不调用 Electron/Chromium 的原生
  `alert` / `confirm`；关闭模态框后恢复原焦点，窗口从托盘或其他应用切回时会重新同步会话
  状态，避免终端中文 IME 与独立输入框一起失去焦点或停留在错误的禁用状态。
- 下拉框、复选框和单选框同样不是操作系统控件，而是由当前主题令牌绘制，因此换主题时它们
  跟着换；键盘操作（`↑/↓`、`Enter`、`Esc`、`Tab`）与原生下拉一致。
- 退出应用时如果有回复正在生成或消息正在发送，会先弹出确认框——这类工作随进程一起消失。
  运行中的项目终端不会拦截退出，它们本来就设计为继续在托盘后台运行；系统关机或注销也不
  询问，因为无论如何都会结束进程。
- 工具栏可切换四套明暗主题，主题驱动整个外壳——标题栏、活动栏、侧栏、工具栏、底栏、卡片与
  原生窗口边框一起换色，终端底色外不会残留深色圈层；所选主题会被记住，冷启动即为正确配色。
  映射和 PSReadLine 调整仅在应用终端内生效。
  xterm 的 Unicode 11 宽字符组件按其要求启用提议 API，避免启动时出现
  `allowProposedApi` 错误。

视觉与交互约束详见 [design.md](design.md)。

## 路线图与后续演进

[roadmap.md](roadmap.md) 记录本轮需求的实施结论，以及历史保留周期/一键清空、更多语言包等
后续方向。当前已上线能力以本 README 和代码为准。

## 功能边界

- 每个项目拥有独立的 Windows PowerShell/ConPTY 会话，可同时在后台运行。
- 每个项目可独立选择 Claude Code 或 Codex。Codex 选择保存在
  `userData/claude/agent-runtimes.json`；默认仍是 Claude Code，删除项目记忆时同时删除该选择。
- Codex 使用 OpenAI 官方 CLI、官方 App Server 账号协议和官方 ChatGPT 登录，不把 ChatGPT
  凭据写入 Claude 配置，也不修改 `~/.codex/config.toml`、`~/.codex/auth.json` 或系统代理。
- 冷启动和关闭最后一个对话之后，工作区是真正的空状态：会话总是属于用户自己选的文件夹，
  不会为了填满界面而在用户目录里起一个 PowerShell。
- 项目列表支持添加、切换和关闭项目；终端输出与滚动缓冲在项目切换后仍会保留。
- 将文件夹拖入窗口或使用目录选择器即可添加项目；重复添加同一路径会切回已有会话。
- 系统目录选择器只使用仍然存在的活动目录作为初始位置；目录失效时回退到用户目录，Windows
  父窗口句柄异常时自动无父窗口重试，并区分“选择器启动失败”和“添加项目失败”。
- 关闭主窗口后驻留系统托盘，可从托盘恢复、切换/添加项目或控制当前终端。
- “对话”是独立于项目终端的通用模型聊天页，配置保存在
  `userData/claude/chat-profile.json`；支持 Anthropic Messages 与 OpenAI Chat Completions
  兼容接口。消息历史以 version 2 明文 JSON 保存在 `userData/claude/chat-history.json`，
  最多保留最近 50 个对话、每个 100 条消息；可逐条永久删除。附件正文独立存放，历史只记录
  引用。输入草稿会实时计入估算上下文，接口返回 usage 后改用供应商给出的精确 Token。
- 左下角“设置”统一承载应用级设置；代理等即时控制直接生效，开机启动、关闭行为、主题与高级
  选项先形成草稿，显示未保存项数并在“完成”时一次确认。终端工具栏的主题切换仍即时生效，
  当前版本只提供简体中文。高级接入工具位于“设置 → 接入”，内置代理与路由分别位于
  “设置 → 代理 / 路由”。
- 标题栏下载入口集中显示受管资源的真实进度、速度、已用/剩余时间和校验阶段；可暂停、继续、
  取消并在崩溃后恢复。退出时会区分可恢复下载与安装/卸载/配置写入风险，默认引导最小化到托盘。
- 左栏 MCP 页按 user/project/local 作用域显示 Claude Code 配置，并只读显示 Codex CLI MCP；
  安装/卸载为单项操作，项目共享启停必须先看目标文件与 diff，并可从完整备份还原。
- 每个项目可以独立选择 Anthropic 官方接入、Anthropic Messages API 兼容服务或本地
  模型转换器。OpenAI Chat Completions / Responses 上游由 Claude Code Router 转换后接入，
  Anthropic Messages 上游可以直连或经过 Router；两类协议都显示在项目连接历史中。带
  `/v1/chat/completions` 的 OpenAI 格式地址不能直接填给 Claude Code；服务商若另行提供
  `/v1/messages` 则可以直连。DeepSeek 官方现已提供
  `https://api.deepseek.com/anthropic` 直连接口。
- 服务商目录是预设、认证方式、主/快速模型、控制台/文档链接和外链白名单的单一事实来源。
  切换服务商会清空未保存凭据和旧测试结果，避免把上一家服务商的密钥误发给下一家；重新打开
  “接入”页只展开上次选择所在分类。
- “接入”页自动发现 Claude Code Router（默认模型接口 `3456`、管理页 `3458`）、
  LiteLLM（常用端口 `4000`）、当前项目保存的本机地址，以及现有 Claude Code
  用户/项目设置；检测只回传地址和“是否有凭据”，不回传凭据内容。
- 可粘贴服务商提供的 cURL，自动识别 OpenAI/Anthropic 协议、接口、模型和认证头，并给出
  “可直连”或“先配置本地转换器”的明确下一步。
- “接入”页在服务商选择与模型配置之间保留每个项目最近 20 条历史配置。保存时如果所填内容与
  最近一条完全一致就不新增记录；上游协议参与判重，网关运行状态只随记录一起显示、不参与判重，
  避免网关反复启停刷出一堆几乎一样的条目。卡片显示自定义名称、协议与连接方式标签、接口/网关、
  主模型和快速模型，点击一键恢复当时的接入配置（含安全存储中的密钥和协议元数据）；右键可以
  重命名、恢复或删除，记录右侧的 `×` 也可删除单条。
- 可在“设置 → 路由”一键获取 Claude Code Router 官方 Windows 安装程序、启动/停止模型网关、
  通过 npm 或 npmmirror 安装、卸载/替换旧版、打开完整管理页，并可视化新增、编辑、删除
  Provider。OpenAI cURL 可一键写入 Router，同时把 Router 路由接入当前 ClaudeDock 项目。
- 同一页提供供应商一键接入向导和 CC Switch 适配器：向导优先选择原生 Anthropic 直连，只在
  OpenAI 协议转换需要时使用 CCR；CC Switch 仅通过官方 MSI 安装/卸载和官方 `ccswitch://`
  导入协议单向导出，不读取或修改其 SQLite。
- CCR 管理服务已运行但 Provider 为空时，不再只显示 `No available models`：界面会解释
  当前项目是否受影响、禁止无意义的重复启动，并提供“用当前项目配置一键修复”或手动添加
  第一个 Provider。自动修复只在远程 Anthropic 直连和 API Key 已安全保存时可用。
- CCR 的 `better-sqlite3` 若被 Electron 内置 Node 误加载，界面会把
  `NODE_MODULE_VERSION` 报错解释为运行时不匹配，并提供“修复运行环境并重启”。修复只会
  用 CCR 安装时配套的系统 `node.exe` 重启管理服务，不重编译数据库、不修改 Provider，
  也不触碰 Codex。
- 可发送最多 1 个输出 token 的 `/v1/messages` 测试，分别显示端点、身份认证和模型响应结果；
  测试在受控后台队列运行并优先于待执行的自动发现刷新，响应正文最多读取 64 KiB。当前 Claude
  项目首次载入、应用从托盘恢复以及用户手动点击都会实测已保存连接，可能产生极少量费用；
  接入页每 6 秒执行的本机网关自动发现仍只读状态，不发起模型请求。成功保存、测试失败和异常
  都会恢复“测试并接入”按钮。
- “会话”页持续显示当前路由健康状态：使用本机 Router 时会在启动前确认 `3456` 网关与
  Provider 是否就绪；Claude Code 真实对话若返回 ConnectionRefused、认证、端点或模型
  错误，会立即显示红色提示并引导回“接入”页。
- Claude 工作台提供新建、继续最近和选择历史三种会话入口，并把 `/context`、`/usage`、
  `/model`、`/permissions`、`/compact`、`/theme` 等常用斜杠命令变成可点击操作。
- 新对话先显示“对话 1 / 对话 2”等临时名称；Claude Code 根据首条提示词生成标题后，
  ClaudeDock 会通过官方 `statusLine.session_name` 自动同步到左侧列表、终端标题和工作台范围，
  并以“擦除旧名、打字机输入新名”的动画原位更新（系统开启减少动态效果时直接替换）。
  这不会额外发送一条 ClaudeDock 提示词；手动重命名立即生效且仍会覆盖自动标题。
- 项目列表按“文件夹 → 当前对话 → 历史对话”折叠展示。历史对话完整列出并在文件夹内独立
  滚动，运行中对话固定在上方不随滚动移动；折叠开关只收起历史对话区，正在进行的对话
  始终可见，所以使用中的项目也可以收起。恢复历史仍通过 Claude Code 官方
  `--resume <session-id>`；运行中和历史对话均可重命名，历史项右键写入 Claude 的
  `custom-title` 元数据。历史项的 `×` 与右键“删除历史对话”都会先二次确认，再只删除该项目
  下经 UUID 校验的目标会话；工作台不再重复显示同一份历史列表。
- 通过 Claude Code 官方 `statusLine` 数据实时显示上下文窗口、输入/输出 token、会话估算
  费用、持续时间、模型和会话 ID；不解析易变的终端绘制文本。
- 终端底栏无需展开面板即可查看连接健康、上下文占用和模型；右键菜单提供复制、粘贴、
  全选和清屏，`Ctrl+C` 在有选区时复制、无选区时保持 PowerShell 中断语义。输出区聚焦时
  出现主题色微光，提示接下来的按键将直接进入 PowerShell / Claude Code。
- 底栏的连接、模型、模式和思考四处都可点击：连接使用已保存配置在当前界面直接开始真实测试并
  显示等待态，不切换左栏页面；模型、模式与思考弹出浮层列表，宽窗口显示、900px 以下随底栏
  一起收起。
- 模型切换分两条路径。同一接口地址、预设、认证方式和 helper 策略的模型走运行中的 `/model`，
  对话不中断；命令正文与提交回车分两次写入，并按会话串行化，避免 Claude Code 把整块输入
  识别为粘贴后吞掉回车。切换按钮在操作结束时直接解除禁用，不依赖后续状态刷新；
  其余条目需要更换环境变量，只能重启 PTY，因此会先确认再压缩上下文、重启并 `--continue`。
  被点击的条目会在主进程重新生成列表核对一遍，模型标识还要再过一次命名校验才写进终端。
- 权限模式的真值来自 Claude Code 自己重绘的模式徽标，`statusLine` 数据里没有这一项。
  Claude Code 会只输出变化的终端单元格，因此 ClaudeDock 用输出修订号确保 xterm 已合成完
  待处理差量，再扫描完整活动缓冲区并回报徽标。底栏切换在首次按键前和每次按键后都主动取样；
  读不到可靠徽标就不发键，检测到模式循环则停止，同一会话的并发点击也会被拒绝。
- `bypassPermissions`（完全允许）无法在未预置的会话里进入，这是 Claude Code 的限制。
  ClaudeDock 默认在启动时用 `--allow-dangerously-skip-permissions` 把它加进循环但不激活，
  可在工作台按项目关闭。`dontAsk`（仅预批准）永远不在循环内，只能在启动时设定。
- 插件页兼容原生 Claude 可执行文件与 npm 生成的 `.cmd/.ps1` 启动器；插件目录及市场刷新
  不依赖是否打开了项目。插件包 ID 不翻译，插件能力说明只显示中文版本；更新入口仅在后台
  或用户主动刷新确认有新版后显示。
- `claude plugin list --json` 的已装条目只带 `id`（没有 `name`/`description`，`version` 可能是
  `unknown`），并且已装插件会从 `available` 列表里剔除。ClaudeDock 用 `id` 反推包名与市场名、
  把 `unknown` 视为“版本未知”而不显示 `vunknown`，再用市场清单补齐中文说明，因此已安装的
  插件（如 frontend-design）在“已安装”页可见且不会误报可更新。
- 模型/API 路由只注入 ClaudeDock 为当前项目启动的进程，不修改 Codex、Claude Code
  全局配置或 Windows 系统级 API 路由；启动时会用更高优先级的临时设置覆盖用户配置中
  遗留的 CCR/Claude 路由别名，避免“测试直连成功、真实会话却回到已停止的 3456”。
- 不劫持任意已经打开的外部 PowerShell 窗口；应用创建并接管自己的 ConPTY 会话。

## 开发环境

- Windows 10 1809 或更高版本
- Node.js 24 或更高版本
- npm 11 或更高版本
- Claude Code 2.1.197 或更高版本（ClaudeDock 受保护启动的最低版本）

安装依赖并启动开发模式：

```powershell
npm install
npm run dev
```

常用命令：

```powershell
npm run lint
npm run format:check
npm run typecheck
npm test
npm run test:conpty
npm run test:control-theme
npm run test:layout
npm run test:visual
npm run build
npm run dist
```

`npm run test:layout` 会在隐藏的 Electron 窗口中检查 820×640、900×640 和 1180×760，
覆盖项目、对话、接入、插件四个分组、工作台三页、收起的左栏、全局设置两个分类，以及富文本
长内容、附件和 Artifact 抽屉压力态；共运行 42 个场景。若交互组件被透明层挡住、彼此重叠或
关键容器横向溢出则失败。自绘下拉框的透明原生层与可视 trigger 按同一个复合控件检查；测试会
先注入两个故意重叠的独立按钮校准检测器，确保跨控件遮挡仍能触发失败。
`npm run test:visual` 除保留插件页、接入向导、历史配置、全局设置、连接测试、终端聚焦态、
独立对话详情抽屉和重命名弹窗的回归图外，还生成 `claude`、`telegram`、`graphite`、
`midnight` 四主题乘以富文本对话、终端和终端遮罩三种状态的 12 张矩阵图；富文本对话矩阵会
聚焦输入框，以同时核对四主题的焦点颜色与动效终态。图片统一写入 `dist/visual-qa/`。
`npm run test:conpty` 使用一次性用户目录启动真实 PowerShell ConPTY，在 820→1400→900→
1280→1180px 间往返缩放并把最终终端截图写到
`dist/visual-qa/conpty-resize-live.png`；它只在 Windows 上运行，不会读取或修改日常用户数据。
`npm run test:control-theme` 在隐藏窗口里读取全部按钮的计算样式，任何一个仍是 Chromium
用户代理默认外观（`border-style: outset`）的控件都会让它失败——主题里没有任何规则会产生
`outset`，所以这等价于「这个按钮从未被项目选择器命中」。
`npm run test:xray-download` 需要联网，它复现内置代理引导下载被自身代理清空掐断的竞态；
对应的离线保证是 `tests/proxy-environment.test.ts` 中 `starting` 与 `stopped` 规则一致的断言。
`npm run test:xray-probe` 需要联网，打印内置镜像表里每条线路当前的通断、延迟与实测速率，
并指出这次会从哪条下载。镜像域名 churn 极高，发布前和收到「所有下载线路都不可用」时都该跑一次；
加一个参数（如 `http://127.0.0.1:10808`）可以改从引导代理探测。
`npm run test:xray-install` 需要联网，用一次性 userData 跑完整的探测 → 下载 → 解压 →
启动链路，失败时会连同内核状态与最近 25 行隧道日志一起打印。它有意每次从零开始；
只想反复调试启动那一段时用 `KEEP_CORE=1` 复用上次下好的内核。这两个脚本的离线保证分别是
`tests/xray-core-sources.test.ts` 与 `tests/xray-sidecar.test.ts`。

`npm run dist` 在 `outputs/` 完成 Windows x64 打包，最终安装程序固定为
`outputs/ClaudeDock-Setup-<version>-x64.exe`。后续安装包、校验元数据和解包产物均只放在该
本地输出目录，不再复制到项目根目录。

每轮完成并验证的项目修改都要更新版本：不兼容或架构级变更升主版本，有明确发布价值的成组/
重大新功能升次版本，小功能优化、修复、文档、构建或维护改动升修订版本。随后必须运行
`npm run dist`，将对应版本的安装程序、blockmap、`latest.yml` 与 `win-unpacked/` 生成到
`outputs/`。

正式发布使用 `v<package version>` 标签触发 `.github/workflows/release.yml`。工作流会核对标签与
`package.json`，运行完整验证，再用 `electron-builder --publish always` 把 NSIS 安装包、blockmap
和 `latest.yml` 发布到 GitHub Release。自动更新要求仓库/Release 对最终用户公开；私有仓库不能把
`GH_TOKEN` 嵌入客户端。发布前还应在 GitHub Secrets 配置 `WINDOWS_CERTIFICATE_BASE64` 与
`WINDOWS_CERTIFICATE_PASSWORD`，使用稳定的 Windows 代码签名主体。

## 日常使用

标题栏右上角的刷新图标是统一更新检查入口。页面首次完成加载后也会自动在后台执行同样的
检查；它聚合 ClaudeDock 发行版本、Claude Code、Router、插件市场、已保存代理订阅和当前项目
MCP 目录/健康状态，单个来源失败不会抹掉其他结果。每个可更新领域还必须在自己的页面提供独立
“检查更新”入口；新增可更新模块时必须同时注册全局聚合与领域入口。没有发现可安装软件更新时，
对应安装/批量更新按钮保持隐藏，检查按钮仍然可达。

使用内置代理时，在“设置 → 代理”导入并选择节点，再选择 IP 策略和作用域。若 V2RayN 只开启了
系统代理/PAC，可直接并行；若面板显示外部 TUN/VPN，则建议先关闭外部软件的 TUN，或明确确认接受
“外部 TUN → ClaudeDock Xray → 节点”的潜在链式路径。启动后点击“节点与更新源测速”可验证真实
HTTP 延迟及 GitHub/npm 来源连通性；测试到响应头后即停止，不再下载 10 MB 测速文件。中国大陆
公开发行时，通用节点/订阅/出口能力仍是高风险项；请先阅读
`docs/LEGAL_COMPLIANCE.md` 并决定移除模块，或改为依法取得的受控企业线路模式。

如果要使用 ChatGPT 订阅开发，添加项目后在“当前项目开发引擎”选择 **Codex**，再点击
“一键准备 Codex”。应用会在需要时下载并校验官方安装器、打开官方 ChatGPT 登录页，登录完成
后自动回到当前项目启动。浏览器登录不方便时可在 Codex 工作台选择设备码；已经登录的官方
Codex 会直接复用自己的登录态。若选择 **Claude Code**，继续按下面的服务商接入流程使用。

1. 启动 ClaudeDock 后不会自动打开任何项目：界面停在引导态，等你从左侧点击最近打开过的项目，
   或添加一个新的项目文件夹。
2. 把项目文件夹拖到窗口任意位置，或点击虚线“＋ 添加项目”区域；应用会为它新建独立会话，不会停止
   已经运行的其他项目。
3. 在左侧项目列表切换当前项目；点击项目行右侧的关闭按钮会终止并移除该项目会话。
4. 点击左侧“接入”。如果 Claude Code 未安装、版本过旧或位于受阻止版本范围，先在环境卡完成
   安装或更新；环境达到受保护启动要求后，该卡自动收起。
5. 在第一步选择密钥所属服务商。目录会自动填好真实 Anthropic 兼容基址、认证方式、主模型和
   快速模型；Kimi 开放平台与 Kimi Code 会员必须按密钥来源分别选择。需要自定义地址、
   本地 Router 或 cURL 时，选择“高级方式”中的对应入口，功能没有被移除。每个类别标题都能
   展开/收起；再次打开页面时只展开上一次选择所在类别。再次点击当前服务商会回到未选择状态，
   后续表单随之收起。
6. 点击活动栏左下角“设置”，切换到“接入”，在全局设置窗口中调整认证来源，或使用自动发现、
   手动配置、
   Router 管理、cURL 识别、转换器说明与术语解释。显式 API Key / Token 默认选择
   “只使用 ClaudeDock 保存的凭据”；只有确实需要 Claude Code 自己的 `apiKeyHelper` 时才改为
   “保留”。完成后点“完成”，需要放弃未保存输入时点
   “取消”或按 `Esc`。如果服务商给了一段 cURL，选择“粘贴 cURL”后直接粘入：
   - 识别为 **Anthropic `/v1/messages`**：点击“自动填入可直连配置”。
   - 识别为 **OpenAI `/v1/chat/completions`**：如果已安装 Router，可点击
     “一键写入 Router 并接入当前项目”；应用会新增或更新 Provider、设为首选，并把
     `provider/model` 路由保存为当前项目的 ClaudeDock 接入。
   - cURL 中的 Bearer 上游密钥交给 Router；如果 Router 自己启用了访问保护，
     ClaudeDock 需要填写的是另一把 Router 访问密钥，二者不要混用。
     “接入”页的“选择历史配置”会直接列出曾填写的接口/网关、主模型和快速模型；需要复用时
     先点击该记录恢复全部参数，再继续模型配置。
7. 选择模型并填写凭据，点击“测试并接入”。测试最多请求 1 个输出 token，可能产生极少量
   费用；只有端点、认证和模型三项全部通过才自动保存。失败卡会解释原因并提供切换认证、
   打开服务商控制台/文档、改用快速模型、安装/启动 Router、重试或返回选择服务商等操作。
   “跳过测试并保存”保留给官方登录、离线配置或已明确接受风险的高级用户。
8. 没有 Router 时，在“安装来源”选择官方 GitHub 安装包、npm 官方源或 npmmirror，点击
   “一键安装”。GitHub 安装包会核对 Release 声明的文件大小与 SHA-256；npm 镜像只对
   本次命令生效，不改全局 registry。已有 Router 可点击“彻底卸载并清除数据”，会删除路由器
   程序、`%APPDATA%\claude-code-router` 下的全部 Provider 配置与上游密钥，状态回到“尚未
   安装”，随后即可换一个安装来源重新安装。该操作不可恢复，也不会触碰 `~/.claude` 下
   Claude Code 与 Codex 自己的配置。
9. “Provider 配置”支持 OpenAI Chat Completions、OpenAI Responses 和 Anthropic
   Messages 三种上游。可逐项编辑地址、模型和密钥，也可只点“用于当前项目”复用已保存
   的上游凭据。
   - 如果显示“还没有配置 Provider 和模型”，先看黄色“解决办法”卡片。
   - 当前项目已有测试通过的远程 Anthropic + API Key 时，点击“一键修复”会在主进程复用
     Windows 安全存储中的密钥，创建 Provider、启动 `3456` 并切换当前项目。
   - Bearer、无认证或当前项目本来就指向 `3456` 时，点击“手动添加第一个 Provider”，
     依次填写协议、接口、模型和上游密钥，保存后再启动 Router。
   - 如果出现 `better_sqlite3.node`、`NODE_MODULE_VERSION` 或“compiled against a
     different Node.js version”，点击“修复运行环境并重启”。这不是数据库损坏，也不需要
     手工运行 `npm rebuild`。
10. 保存成功后配置会进入“选择历史配置”，下次点一下即可整套恢复；不需要的记录可以右键或用
    记录右侧的 `×` 删除。API 凭据通过 Windows 安全存储加密，界面不会回读明文。
11. 左侧快捷操作可直接“新建安全会话”；工作台“会话”页还可继续最近会话或打开 Claude
    的历史选择器。启动会重建当前项目
    的 PowerShell，以便只通过子进程环境注入路由和密钥，因此会终止该终端中原有的进程。
12. 提示词写在终端输出区下方的输入框里，`Enter` 发送、`Shift+Enter` 换行、`↑/↓` 翻历史；
    Claude 响应后，终端底栏与“会话”页实时显示当前上下文；“命令”页执行白名单斜杠命令，
    “快捷键”页说明终端映射。终端顶部的主题选择会改变整个应用外壳的配色，命令页的 `/theme`
    打开 Claude Code 自身主题选择。Claude Code 完成首条提示词的后台命名后，左侧临时名称会
    自动替换为主题标题；也可随时右键手动重命名。`/clear` 会开启空上下文的新会话，执行前会
    二次确认。
13. 当前 Claude 项目首次载入及面板从托盘恢复时，底栏会自动使用已保存配置执行一次真实连接
    测试；也可以随时点击连接按钮手动重跑。测试最多请求 1 个输出 token，可能产生极少量费用，
    进行时按钮留在当前界面显示等待态且不可重复点击。底栏中间的“模型”弹出可切换的模型列表，
    同端点直接切、跨端点确认后压缩并重启；右边的“模式”列出全部权限模式，也可以在终端或
    输入框里按 `Shift+Tab` 切换，PowerShell 里的徽标与底栏显示保持一致。想使用“完全允许”时，
    需要工作台的“允许「完全允许」模式”开关处于开启状态（默认开启），首次进入时 Claude Code
    会弹一次自己的免责确认。“模式”右边的“思考”列出全部思考程度，选中即通过 `/effort` 应用到
    当前对话，不重启会话；也可以直接在终端运行 `/effort`，底栏会跟随状态行更新。需要联网
    搜索时，ClaudeDock 会让会话级搜索子代理以 `high` 执行 WebSearch/WebFetch，主会话保留
    当前思考档位负责后续分析；刚升级的已运行会话需要重启一次，才会载入该子代理。
14. 点击窗口关闭按钮只会隐藏面板，所有会话继续在后台运行；右键系统托盘图标可以恢复
    窗口、切换/添加项目、控制当前终端或彻底退出。

需要通用模型聊天时，点击活动栏最上方的“对话”，焦点会自动进入消息输入框；独立协议、接口、
模型和凭据从右上角齿轮弹窗填写，可以先点“测试连接”验证当前草稿，再保存并发送。左侧整栏
只显示对话历史。这里的模型与项目“接入”可以完全不同；
工具栏会随输入和流式回复更新上下文/Token，“停止”只中断当前生成，“新对话”开启空白记录，
旧记录仍在左侧历史中，可恢复或永久删除，不影响任何项目或 Claude Code session。回形针或
拖放可添加图片、PDF 和文本/表格文件；模型回复会渲染 Markdown、公式与代码。HTML 代码要由
用户点击“运行此可视化”才会进入隔离 Artifact；右上角“详细信息”可审计联网并立即断网。
全局主题、开机启动和版本信息位于左下角“设置 → 总设置”；“设置 → 高级设置”收纳中转站兼容开关，
默认全部关闭，只有中转站确实存在协议缺陷时才需要开启。

安装时可自行选择 `D:\ClaudeDock` 等目标路径，并可在“安装选项”页面勾选或取消
“在桌面创建快捷方式”（默认勾选）。

## 目录

```text
assets/              图标矢量源及生成图标
build/               electron-builder/NSIS 安装器自定义脚本
scripts/             清理、图标生成等工程脚本
src/main/            Electron 主进程、下载/代理/MCP/路由服务、项目工作区与 CLI 运行时
src/preload/         受限的渲染进程桥接 API
src/renderer/        控制面板界面与 xterm.js 终端
src/shared/          跨进程类型和纯函数
tests/               单元测试
assets/runtime/      Claude Code statusLine、运行期信号与 WebSearch 子代理路由钩子脚本
outputs/             安装包、校验元数据与解包产物，不纳入 Git
roadmap.md           本轮产品任务的实施结论、调研依据与后续演进项
THIRD_PARTY_NOTICES.md 外部规则/远程诊断来源与未引入第三方代码声明
```

## 安全与限制

- 界面只加载项目自带的本地内容，关闭 Node.js 集成并启用上下文隔离和沙箱。
- 用户在终端中输入的命令拥有当前 Windows 用户的权限，应用不会替用户审查命令。
- ClaudeDock 不把密钥写入项目、命令行或终端历史；保存的凭据使用 Electron
  `safeStorage`（Windows DPAPI）加密。受保护启动结束后会从 PowerShell 环境清理所有
  Claude 路由和凭据变量。
- Codex 的 ChatGPT OAuth 凭据完全由官方 Codex 保存与续期。ClaudeDock 只通过结构化
  `account/read`、`account/login/start`、`account/logout` 和额度事件显示必要状态，不读取
  `~/.codex/auth.json`，也不把账号信息转换成 Claude Code 可用的凭据。
- Codex 启动固定使用当前项目目录、`workspace-write` 和 `on-request`；这不是“完全允许”。
  官方 TUI 请求工作区外写入、网络或更高权限时仍会让用户确认。首版结构化 App Server 只用于
  账号与登录，任务交互保留在成熟的官方 TUI，协议不稳定时仍可独立回退。
- 接入历史保存在 `userData/claude/connection-history.json`（权限 `0600`，临时文件加重命名
  写入），其中的凭据同样只以 `safeStorage` 密文存放，文件本身不含明文密钥。安全存储不可用
  时该条记录恢复出来就是“没有凭据”，而不是回落成明文。每个项目最多 20 条，超出丢弃最旧的。
- 独立对话配置保存在 `userData/claude/chat-profile.json`，凭据同样只存
  `safeStorage` 密文；renderer 只能看到“是否已配置”。模型请求由主进程发出，远程地址必须
  使用 HTTPS（本机回环可使用 HTTP）。
- 独立对话正文与 Token 快照以明文写入 `userData/claude/chat-history.json`（权限 `0600`，
  临时文件加重命名写入）；这是本机可恢复历史，不是加密保险箱。每次发送及生成结束都会更新，
  最多保留最近 50 个对话、每个 100 条消息。共享 Windows 账户或设备时应在历史列表中删除
  敏感对话；删除不可撤销。
- 独立对话附件复制到 `userData/claude/chat-attachments/<uuid>/`；图片预览是主进程缩小后的
  data URL，完整附件字节不会为预览跨 IPC。删除或裁剪历史会回收不再引用的附件。同一消息
  草稿跨多次/并发导入累计最多 10 个、32 MiB，并在发送前由主进程再次校验；不支持目录、
  符号链接和未列入白名单的扩展名。
- Markdown 原始 HTML 永远不会直接注入宿主 DOM；外链经主进程协议校验后交给系统浏览器，
  远程图片也不会在消息出现时自动加载或静默外发。
  Artifact 是显式执行边界：iframe 没有 `allow-same-origin`，也不能访问宿主 preload、
  cookie 或 localStorage。主进程把 Artifact 绑定到 `WebContents + frameTreeNodeId` 稳定身份，
  阻止 iframe 导航逃离自定义协议，并按该身份归因默认 session 中的外部请求；
  Chromium 未提供可靠响应体长度时界面明确显示“字节数未知”，不把 `Content-Length` 猜成
  实际下载量。
- 历史文件只在确实不存在时视为空；JSON 损坏、权限或读取错误会保留
  `chat-history.json.corrupt.bak` 并停止覆盖历史和附件回收，在界面报告错误，不把损坏误当
  成“用户清空了历史”。
- 第三方接入固定 `ANTHROPIC_BASE_URL` 与主/小模型别名，同时关闭遥测、错误上报、反馈、
  调查问卷以及 WebFetch 的 Anthropic 域名预检；这能阻断已知的非必要 Anthropic 流量，
  但不能替用户审计第三方网关，也不能证明网关没有在服务端替换模型。
- Claude Code 会按优先级合并用户、项目和命令行设置。ClaudeDock 的临时 settings 会明确
  覆盖 `ANTHROPIC_API_BASE_URL`、`CLAUDE_AGENT_API_BASE_URL`、`CCR_CLAUDE_CODE_MODEL`
  等第三方 Router 遗留别名，以及继承的 thinking / effort 环境覆盖；使用推荐的
  “ClaudeDock 单一凭据”策略时还会用空覆盖停用继承的 `apiKeyHelper`，避免与显式
  `ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` 并存。临时文件只含地址、模型、会话级 thinking
  开关和空覆盖值，不含 API 凭据，也不修改用户或项目设置。
- WebSearch/WebFetch 隔离通过启动命令中的会话级 `--agents` 定义、追加系统规则与临时
  `PreToolUse` 钩子完成；不会创建或改写项目 `.claude/agents`、用户 `~/.claude/agents` 或系统
  API 路由。搜索子代理只拿到 WebSearch/WebFetch，不能编辑项目文件或继续生成子代理。
- ClaudeDock 拒绝受保护启动已披露含隐藏地区/代理检测逻辑的 Claude Code
  2.1.91–2.1.196。Anthropic 当前仍不向中国大陆/香港及受不支持地区控制的实体提供官方
  服务；ClaudeDock 不会伪造位置、绕过地区限制或保证官方账号可用。
- 经中转时，项目代码、提示词和输出会经过该网关。只使用有明确数据处理、日志和模型路由
  说明的服务，不要把生产代码交给来源不明的低价中转。
- 自动发现每 6 秒检查已知本机端口，只发送不带凭据的连通性/模型列表探测；它不会扫描全部
  端口，也不会自动调用远程模型。只有用户点击“真实测试”后才会向当前表单地址发送
  1-token 请求。
- 首屏后台更新检查会读取受信任 registry 的软件版本并调用 Claude CLI 刷新插件市场；它不
  发送模型请求。标题栏刷新图标会重复该检查，只有确认存在更新后才显示更新操作。
- 会话启动前只对当前项目确实指向的本机 Router 做阻断检查；其他远程直连项目不会因为
  本机 CCR 故障被错误阻止。运行中只识别 Claude Code 明确输出的 API Error 类错误，并将
  常见原因映射为中文；未识别的英文错误不会直接进入界面，原始终端输出仍原样保留，不保存
  终端正文或提示词。
- cURL 分析发生在本地 renderer 中，不写日志；分析结果永远不显示完整密钥。cURL 输入框
  在当前项目切换或一键导入成功后清空。已经粘贴到对话、工单或其他第三方位置的密钥仍应
  立即撤销。
- Router 管理凭据只在 Electron 主进程读取并用于本机回环 RPC，不会传给 renderer。
  Provider 列表只显示“是否已配置密钥”。保存 Provider 时只改 CCR 的 `Providers` 和
  `preferredProvider`，不会应用或改写 CCR 中的 Codex profile、Claude profile 或系统代理。
- 代理节点凭据只以 `safeStorage` 密文写入 `userData/proxy/`。Xray 配置在启动前临时生成，停止后
  删除；作用域开关默认只注入 Claude/Codex CLI 子进程。应用代理只影响 Electron 自己的 session，
  任何路径都不写 Windows Internet Settings、路由表或桌面客户端配置。外部代理检测只读取进程名、
  代理决策和虚拟网卡类别；检测到 TUN 时只要求确认，不自动关闭 V2RayN/Clash/sing-box。
- MCP 发现只读 `~/.claude.json`、项目 `.mcp.json` 与 `~/.codex/config.toml`，明确不读取 Claude
  Desktop 配置。安装/卸载通过 `claude mcp` argv 执行；项目共享启停写入前校验文件未被并发修改，
  保留最近 10 份完整备份并支持逐字节还原。Codex MCP 在本版只读显示，避免越权改写其配置。
- 官方 MCP Registry 当前仍是 preview；在线目录失败时自动退回内置精选，不影响本地发现与已安装
  MCP。远程健康检查只接受 HTTPS，stdio 健康检查会启动一次进程完成 `initialize` 后立即终止。
- Router 的 GitHub 安装方式只接受官方仓库当前 Release 中命名匹配的 Windows `.exe`，
  限制最大 250 MiB，并在打开前校验文件大小与 SHA-256。网络受限时可改用 npm 官方源或
  npmmirror；镜像仅作为包下载源，不代替代码签名或供应链审查。
- ClaudeDock 不再通过自身 Electron 可执行文件运行 npm 版 CCR。它会定位 CCR npm 前缀
  旁或 `PATH` 中的系统 `node.exe`，并先验证该运行时可以加载 CCR 自带的
  `better_sqlite3.node`。已被旧版 ClaudeDock 错误启动的服务可由界面定向停止和重启。
- CCR 3.x 的 `preferredProvider` 按 Provider 名称路由。ClaudeDock 同时识别旧配置中遗留的
  Provider ID，并在用户保存该 Provider 时迁移为当前名称，避免以后增加多个 Provider 后
  选错上游。
- 关闭项目会终止该项目的 PowerShell 进程；切换项目不会影响其他项目的运行。
- 「完全允许」（`bypassPermissions`）会让 Claude Code 跳过所有权限确认，风险由用户自行
  承担。ClaudeDock 只在启动时把它加入 `Shift+Tab` 循环，不会替用户进入该模式；工作台的
  开关关闭后，启动命令里不会出现 `--allow-dangerously-skip-permissions`。首次进入时
  Claude Code 自己的一次性免责确认由用户回答，ClaudeDock 不代答。
- 跨端点切换模型和「仅预批准」都需要重启 PowerShell 会话，因为接口地址与凭据是 PTY
  启动时注入的环境变量。重启前的 `/compact` 与重启后的 `--continue` 都是 Claude Code
  自己的官方能力；终端画面会重绘，该终端里原有的其他进程会被终止。
- 底栏模型列表只列出当前配置和该项目的接入历史，凭据不出主进程，列表里只有“是否已配置
  凭据”。被点击的选项在主进程重新核对一遍，模型标识不合法就拒绝写入终端；`/model`
  的斜杠命令白名单没有因此放宽。
- 项目会话只在本次应用运行期间保留，彻底退出后不会恢复终端进程或历史缓冲。
- Claude Code 自己会按项目目录把会话明文保存在 `~/.claude/projects/`，默认约 30 天；
  ClaudeDock 不复制对话正文，只读取标题等元数据和 statusLine 提供的结构化状态。自动命名
  复用 Claude Code 自己根据首条提示词生成并放入 `session_name` 的标题，不另发隐藏提示词；
  用户重命名历史对话时只向对应 UUID JSONL 追加 Claude 兼容的 `custom-title` 记录。删除
  历史对话是不可恢复操作：当前 Claude Code CLI 没有公开的单会话删除命令，因此 ClaudeDock
  使用已有的严格兼容路径，只接受项目路径加合法 UUID 并删除对应 JSONL；绝不调用会清空整个
  项目的 `claude project purge`，也不把这一兼容路径描述为 Claude Code 官方 API。
- 本地构建默认没有代码签名，Windows SmartScreen 可能显示未知发布者提示。
- 当前 GitHub 更新通道只有在仓库和 Release 对用户公开时可用；私有仓库普通用户无法自动更新。
- 中国大陆公开发行前必须决定通用代理模块的处理方式，并把实际发行主体与联系方式写入
  `docs/PRIVACY.md` 和应用内“隐私与合规”；当前文档不是正式法律意见。
- 当前仅打包 Windows x64。
- 自动发现覆盖 Claude Code Router 的默认 `3456/3458`、LiteLLM 的常用 `4000` 和当前
  项目已保存的本机端口；自定义端口或改名进程可能需要手动填写。
- `@lydell/node-pty` 提供与上游 node-pty API 兼容的按平台预编译包，避免最终用户安装
  Visual Studio C++ 构建组件。

项目面向公开用户使用与评审；维护信息以仓库提交记录和后续发布说明为准。
